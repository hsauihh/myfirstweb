# RAG 方案建议

给 AI 对话加检索增强（RAG）的设计建议。

> 状态：**阶段 1 已实现**（`RAGdata/` 入库 + 本地 fastembed 向量化 + 检索 + 注入提示词 + 独立额度），已实现**个人知识库**（登录用户在 `/knowledge` 把博客文章选入自己的知识库、来源管理、懒同步，问答时与站内公共库合并，支持「问答/上下文」模式与「使用系统知识库」开关）、**阶段 5 GraphRAG**（LLM 抽实体关系 + 一跳图谱扩展，见第 12 节）与**可溯源引用**（标题感知切块 + 结构化来源 + 前端跳原文，见第 14 节）。阶段 3（增量入库）与阶段 4 里的重排/混合检索仍未做。

## 1. 目标

让 AI 回答基于「我们自己的资料」（站内文案、说明文档等），减少瞎编；资料里没有的就直说。

## 2. 知识来源

先做**站内 Markdown**（改动最小、内容可控）：

- `README.md`、`struct.md`、`RAG.md`
- 页面文案：`data/site.js`、`data/quotes.js`
- 博客文章：登录用户把自己或他人的公开文章选入个人知识库（`kb_sources` + `documents.source_id`），不再依赖文件目录

后续再扩展本地文档（PDF/Word），需要额外的解析（`pypdf` / `python-docx`）。

## 3. 切块（chunking）

- 按标题/段落切，目标 **约 500 字**，相邻块重叠 **约 50 字**，避免答案被切断。
- 每块保留元数据：来源文件、标题路径、块序号。
- 太短（<50 字）的块丢弃，太长的按句号/换行再切。

## 4. 向量化（embedding）

- 走 **OpenAI 兼容的 embeddings 接口**，新增环境变量：
  - `EMBEDDING_BASE_URL`、`EMBEDDING_API_KEY`、`EMBEDDING_MODEL`
  - 可沿用现有 `CHAT_*` 配置，也可指向支持 embeddings 的厂商（通义 `text-embedding-v3`、OpenAI `text-embedding-3-small` 等）。
- 暂不引入本地 `sentence-transformers`：依赖重（torch）、下载模型大，收益不如先跑通链路。
- 同一批入库必须用同一个模型与维度；换模型要 `--rebuild` 重建。

## 5. 存储与检索

- SQLite 新表：
  ```
  documents(id, source, chunk_index, title, content, embedding TEXT, created_at)
  ```
  向量以 JSON 字符串存 `embedding` 列。
- 检索用 **numpy 暴力余弦相似度**：个人站知识库通常几百到几千块，全量算一次毫秒级，不需要向量数据库。
- 量大（>10 万块）再换 `sqlite-vec`（SQLite 扩展）或 FAISS。
- 返回 **top-k（k=4）**，并设**相似度阈值**（如 0.3），低于阈值的不注入，避免答非所问。

## 6. 入库脚本

- 新增 `backend/ingest.py <目录>`：
  1. 遍历目录下 `.md` / `.txt`；
  2. 切块；
  3. 批量调用 embeddings；
  4. 写 `documents` 表。
- 支持 `--rebuild` 清空重建；日常可增量（按文件 mtime/哈希跳过没变的文件）。

## 7. 接入点

- `chat.build_messages` 增加可选参数 `context: str`，拼在 system 提示词里。
- `chat_api.send_message_endpoint` 在拼消息前：
  1. 用用户问题做 embedding；
  2. 检索 top-k；
  3. 拼成带来源的上下文。
- 不改动现有 SSE / 落库逻辑。

## 8. 提示词建议

```
你是文字实验室的中文助手。优先依据下面的「参考资料」回答；
资料不足或与问题无关时，直接说明没有相关资料，不要编造。
回答末尾标注引用的来源文件名。

参考资料：
[1] （来源：README.md）
...
```

## 9. 成本与延迟

- 每次提问多一次 embedding 调用（通常几十毫秒）；可用内存 LRU 缓存高频问题。
- 检索本身几乎无成本；知识库重建才需要批量 embedding。

## 10. 评估

- 准备 10–20 条站内问题（如「这个项目怎么启动后端」「消息中心有哪些功能」）。
- 人工看三件事：**是否检索到正确资料**、**答案是否引用来源**、**资料缺失时是否老实说不知道**。

## 11. 分期建议

| 阶段 | 内容 |
|------|------|
| 阶段 1 | 站内 Markdown + 切块 + embedding + 向量检索 + 注入提示词（已完成） |
| 阶段 2 | 个人知识库（博客文章选入 + 来源管理 + 懒同步 + 按用户检索隔离）（已完成） |
| 阶段 3 | 增量入库、按来源过滤、检索结果去重 |
| 阶段 4 | 重排（rerank）、混合检索（关键词 + 向量）（引用高亮已完成，见第 14 节） |
| 阶段 5 | GraphRAG：实体关系抽取 + 图谱扩展检索（已完成，见下） |
| 阶段 6 | 可溯源引用：标题感知切块 + 结构化来源 + 向量存储分层（已完成，见下） |

## 12. 阶段 5：GraphRAG（已实现）

纯向量检索只能拿到「语义相近的块」，拿不到「资料里互相关联但用词不同的块」。GraphRAG 用一层实体关系图把这类块连起来。

### 12.1 分层口径（取舍）

只做 **local search**（向量召回 + 图谱一跳扩展），不做社区检测与社区摘要：

- 社区摘要（global search）要跑社区发现 + 逐社区摘要生成，抽图阶段成本再翻几倍，而本项目的提问基本都是「这份资料怎么说某个概念」，local search 就够。
- 无图谱数据时（没配 key、抽图失败、新入库还没抽）**自动降级为纯向量检索**，行为与阶段 1/2 一致；所以图谱是纯增强项，不会成为新的失败点。
- 扩展固定 **1 跳**：2 跳在教材类资料上会把大量噪块拉进上下文。加深跳数只需改常量。

### 12.2 抽取

- 入口 `backend/graph.py`；模型配置复用 `CHAT_BASE_URL` / `CHAT_API_KEY`，可用 `GRAPH_MODEL` 单独指定。
- 一次请求抽 `CHUNKS_PER_CALL=6` 个块（摊薄往返延迟），要求严格 JSON；解析失败整批重试一次，仍失败则跳过该批并计入 `failed`，**不抛出**。
- 规范化：NFKC（全角转半角）+ 折叠空白；匹配用 `key`（去空白 + 大小写折叠），展示用 `name`。每块最多 12 实体 / 12 关系，关系两端必须都在同一块的实体集合内。
- **缓存**：抽图结果按块内容 `sha256` 存 `graph_extractions`。重建公共库、多个用户加同一篇文章都会命中缓存，不重复调模型。

### 12.3 存储与可见性

```
graph_entities(id, key UNIQUE, name, kind, created_at)
graph_mentions(chunk_id → documents, entity_id → graph_entities)      -- 块↔实体
graph_relations(id, src_id, dst_id, relation, chunk_id → documents)    -- 关系（带证据块）
graph_extractions(content_hash PRIMARY KEY, payload, created_at)       -- 抽取缓存
```

实体与关系本身是**全局**的（同一概念跨用户复用）；用户隔离不另存 `user_id`，而是靠 `chunk_id` 连回 `documents`，复用 `rag_store.visibility_clause`（`source_id IS NULL` = 站内公共库，非空 = 该用户的个人库）。块被替换/删除时 mentions 与 relations 随外键级联清理，实体留给 `prune_orphans()` 在重建后清。

### 12.4 检索

`rag.retrieve()` 替代原来的 `rag.search()` 作为问答入口：

1. 一次 embedding + 一次矩阵乘法得到全部块的余弦分（与阶段 1 相同，不额外增加成本）；
2. 过阈值（0.3）取 top-6 作为**种子**；
3. 取种子块提到的实体，`neighbor_chunks()` 找出提到同一批实体、且该用户可见的其它块（按命中实体数排序）；
4. 合并去重：种子在前、扩展块在后，总数封顶 8 块；`build_context()` 额外附上两端都在命中实体里的关系（最多 10 条）。

扩展块不再受相似度阈值约束（它本来就不是靠相似度进来的），但必须命中至少一个种子实体。

### 12.5 触发时机

- 公共库：`ingest.py` 入库后逐文件抽图，默认开启，`--no-graph` 关闭；`--rebuild` 重建后清孤立实体。约 400 块的公共库首次建图需要几十次模型调用（几分钟）。
- 个人库：`kb.add_source` / `kb.sync_source` 重建块后抽图，单篇最多 `SOURCE_CHUNK_LIMIT=24` 块（超出的块仍可被向量检索命中，只是没有图增益）。`/knowledge` 的按钮在等待期间会显示「加入中…/同步中…」。

### 12.6 本阶段的取舍

- **优点**：拿回跨章节的关联块；图谱是增强项，坏了也不影响问答；抽取结果可缓存，重建不重复花钱。
- **风险**：抽图质量取决于模型（关系短语可能不精确，所以关系只作提示词补充、不对外展示）；个人库首次加入要等几秒到十几秒；一跳扩展会引入少量弱相关块，靠命中实体数排序与 8 块封顶控制。
- **未做**：社区摘要/全局检索、多跳、重排（图谱可视化见第 14 节）。

## 13. 阶段 6：可溯源引用（已实现）

目标：让回答能说清「这句话来自哪篇文章、哪一段」，而且能点回去。

### 13.1 切块带上结构

原实现只按空行向 500 字聚合，`## 4.7.2 cache 的读、写流程` 被当成普通正文混在块里，所以无法定位到节。现在 `ingest.chunk_markdown` 维护标题栈：

- 遇到层级 ≤ 当前栈顶的新标题就先刷出当前块（**小节之间绝不合块**）；
- 小节内部仍按段落聚合到 500 字、重叠 50；
- `documents.section` 存「标题路径」（如 `4.7 高速缓冲存储器cache / 4.7.2 cache的读、写流程`），标题行仍保留在块正文里供模型看结构。

代价：块数变多（公共库 416 → 513），首次重建会重新切块，内容哈希全变 → 图谱会重抽。

### 13.2 引用数据流

`rag.retrieve()` 的每个块都是一条可渲染引用（与上下文序号 `[n]` 一一对应）：

```
{index, kind: post|public, title, author, label, section,
 post_id, snippet(≤600字), score, via}
```

- `chat.RAG_INSTRUCTION` 要求模型用 `[n]` 标注依据、不得编造编号；
- `chat_api` 把 `rag.citations()` 的结果随助手消息写入 `messages.sources`（JSON），所以 SSE `done`、历史接口都能拿到，**刷新不丢**；
- 前端 `remarkCitations`（自写 remark 插件，无新依赖）只把本轮真实存在的编号改成链接，`CodeBlock`/已有链接里的 `[1]` 不动；点角标弹层看命中片段，气泡下方按文章聚合出来源卡片。

### 13.3 跳转

- 个人文章：`/blog/post?id=N#小节`。标题 id 与引用链接都由同一个 `headingSlug` 函数从标题文本生成，后端**不**存 slug，避免两套实现漂移；`BlogPostView` 在文章加载后按 hash 滚动并短暂高亮。
- 站内公共资料：`RAGdata/` 不随站点发布（部署时不上服务器），没有网页可跳，改为弹层展示命中片段（内容就在 `documents.content` 里）。

### 13.4 向量存储分层

向量从 `documents.embedding`（JSON 字符串）搬到 `document_vectors`（float32 BLOB）：

- 原先每次提问都要把该用户可见的全部块读出来并逐行 `json.loads`——418 块就约 3MB JSON，这是真实的瓶颈；
- 现在检索层持有「整库矩阵 + id→行号映射」，用 `vector_fingerprint()`（条数 + 最大 id）判断是否重建，只对入选的少量块再取正文；
- 迁移在 `schema_rag.migrate()` 里幂等完成（搬向量 → `DROP COLUMN embedding`）；同一处给老库补 `section` 并把 `kb_sources.post_updated_at` 置空，让存量个人库懒重建。

### 13.5 为何暂不引入 Vector DB

向量库与元数据同库能避免双写不一致、单文件部署直接备份，而当前规模（公共库几百块 + 个人库几十块）下 numpy 全量余弦是毫秒级。什么时候该换：单库块数到万级以上、或需要多进程/多实例共享索引。届时只需替掉 `rag_store.all_vectors` / `rag._vector_index` 这一层（sqlite-vec / FAISS 都是候选），引用与切块逻辑不受影响。

### 13.6 本阶段的取舍

- **优点**：回答可验证、可回溯；引用落库后重开会话仍在；向量解析成本从「每次提问全量」降到「库变化时一次」。
- **风险**：块数变多会抬高首次重建（含抽图）的时间与 token；标题内嵌格式（如 `**粗体**`）会让锚点对不上——找不到元素就不滚，不报错。
- **未做**：段落级精确高亮（依赖文本匹配，编辑后易失准）、公共资料整篇查看页（需部署原文）。

## 14. 阶段 7：知识库图谱页（已实现）

目标：把已经抽好的图谱摆给用户看，让「知识库里有啥、概念怎么连」一目了然。不引入任何离线产物，页面刷新就是最新数据。

### 14.1 接口

`GET /api/rag/graph`：`{ groups, nodes, edges, stats }`。

- `groups`：分组骨架（`chapter` = 章、`source` = 个人文章 / 无章号的公共文件）；
- `nodes`：`{ id, entity_id, name, kind, group, degree }`，`degree` 是可见提及数；
- `edges`：`{ source, target, relation, weight }`，两端都是已展示的节点；
- `stats`：片段/实体/关系总数、展示的节点与边数、是否发生截断。

`GET /api/rag/graph/entities/{id}`：概念详情（`kind` / `degree` / `groups` / `relations` / `chunks`）。实体对当前用户没有任何可见提及时返回 404。

两个接口都用 `rag_store.visibility_clause`：未登录只看站内公共库，登录后叠加自己的个人库；可见性判断与向量检索完全同一套。

### 14.2 选取规则（确定性）

- **分组**：个人库块按来源（《标题》· 作者）成组；公共库块从 `documents.section` 第一级或文件名取章号（`4.存储器`、`4 存储器`、`4.7 高速缓冲存储器cache`、`4.7.3.1 直接映射` 都归第 4 章），章名取该章下「章级标题」中出现最多的那个，规范化成 `4 存储器`；所以单文件版与图谱版不会出现重复章。取不到章号的公共文件（README、assets）各自成组。
- **节点**：每个实体只挂一个分组（提及最多的那个）；每组按组内提及数取前 8，总节点封顶 72；并列时比全局提及数、再比实体 id，保证可重复。
- **边**：两端都在展示集内的关系，按 `(src, dst, relation)` 聚合计数，按权重取前 150。

### 14.3 前端

`/knowledge` 第三个页签「知识图谱」：`@xyflow/react` 的分组节点（`parentId` + `extent: "parent"`），分组框按 3 列码放、概念框内纵向堆叠——**坐标由数据算出来，不用力导向布局**，所以同一份数据每次都是同一张图。图谱面板首次点击才加载（`next/dynamic`），不进首屏 chunk；`colorMode` 跟随站点 `data-theme`。悬停/选中时相邻边高亮并显示关系文案，其余淡化（悬停高亮在画布内保持，离开画布或点空白处才清除——挂在节点 `mouseleave` 上会让指针移动时整图明暗振荡，见 `README.md` 踩坑）；点概念拉详情，详情里可点关系的另一端继续跳，也可「用这个概念提问」切回问答页并预填。

### 14.4 本阶段的取舍

- **优点**：零额外产物（不需要重新生成任何图）；可见性直接复用检索规则；600 块以内一次查询即可返回，无缓存也够快。
- **风险**：公共资料没有站点页面，命中片段只能看文件名与小节（与引用行为一致）；抽图质量直接决定图好不好看（关系短语可能有噪声）。
- **未做**：搜索 / 按章筛选 / 图层开关、多跳展开、服务端结果缓存（真要变慢再按可见性指纹加）。

## 14.5 阶段 8：随心一记（已实现）

目标：用户随手记的一句话（「钥匙放在玄关柜第二层」）立刻进个人知识库，之后直接问 AI 就能查到；随时可加、可删。

### 数据模型：笔记是一种新的「个人来源类型」

`kb_sources` 升级为多态来源表：加 `kind`（`post` / `note`）、`note_content`，并把 `post_id` 改成可空。笔记就是一行 `kind='note'` 的来源，块仍旧写在 `documents`（`source_id` 指向它）。

这样做的理由：

- **可见性、配额、引用规则全部不用改**。`documents.source_id` 仍是唯一的归属列，`rag_store.visibility_clause` 的 `source_id IS NULL = 站内公共库` 语义保持成立，不存在「笔记被当成公共资料」的风险；
- 删除笔记就是删来源行，外键级联清掉块与向量，不用写专门的清理逻辑；
- 代价是一次 SQLite 表重建（改不了列约束），重建要点见 `README.md` 踩坑。

### 为什么笔记不抽图

笔记是「一句话事实」，抽实体关系既慢又没收益（一句话里往往只有一个主体），而 GraphRAG 的价值在长文档的概念网络。所以笔记**单块入库、不调用 `graph.build_for_source`**：

- 保存即时、不消耗模型调用；
- 没有 `graph_mentions`，知识图谱页自然不会出现它（无提及的分组本来就会被过滤）；
- 检索仍然命中——向量召回是主路径，图谱扩展只是增强项。

### 切块：笔记必须绕开 `chunk_markdown`

`ingest.chunk_markdown` 按 ATX 标题切块，并且丢弃清洗后不足 `MIN_CHUNK=30` 字的内容。「钥匙在玄关柜」13 个字会被直接丢掉，因此笔记走自己的单块写入（`kb.add_note` → `rag_store.replace_source`，`chunk_index=0`，`section`/`label` 都是 `随心一记`）。向量化失败会删掉刚写的来源行，不留「查不到的空来源」。

### 引用形态

命中笔记时 `Citation` 为：`kind="note"`、`note_id=<来源 id>`、`post_id=null`、`title=` 笔记首行（截断 24 字）、`label=section="随心一记"`。前端因此把笔记和站内公共资料一样处理成「点开看片段」，而不是跳文章；来源卡片按 `note:${note_id}` 分组，两条笔记不会挤在同一张卡里。提示词里的参考资料长这样：

```text
[1]（来源：随心一记 · 小节：随心一记）
钥匙放在玄关柜第二层
```

### 接口与容量

`GET /api/kb/notes`（分页 30/页 + 总数）、`POST /api/kb/notes`（1–500 字）、`DELETE /api/kb/notes/{id}`，都需要登录且只作用于本人。写笔记不消耗对话额度；笔记片段计入 `/api/rag/status` 的 `personal`，所以「只记一条笔记」也算知识库非空，不会被 409 拦下。

## 15. 取舍

- **优点**：答案有据可依、可更新、实现成本低（无新服务）。
- **风险**：embedding 依赖外部接口（需 key）；知识库不更新会答旧内容（靠 `ingest.py` 定期跑）；暴力检索在超大数据下会变慢（届时再升级）。
