- 虚拟局域网（Virtual Local Area Network，VLAN）通过以太网交换机对帧添加 VLAN 标记并按端口规则转发来实现。
- 电气与电子工程师协会（Institute of Electrical and Electronics Engineers，IEEE）802.1Q 在以太网 MAC 帧中插入 `4 B` VLAN 标签，使交换机能够识别帧所属的 VLAN。
- 端口 VLAN 标识符（Port VLAN Identifier，PVID）决定未标记帧进入交换机时所属的 VLAN，并参与出方向的标签处理。

==交换机依据帧的 VLAN 标识符限制转发范围：Access 端口面向单个 VLAN，Trunk 端口则可承载多个 VLAN。==

### 3.7.2.1 IEEE 802.1Q 帧

- IEEE 802.1Q 帧在[[3.6 以太网的MAC帧格式#3.6.1 MAC 帧的字段|源 MAC 地址]]与类型字段之间插入 `4 B` VLAN 标签。
- 插入标签后，IEEE 802.1Q 帧的最大长度由 `1518 B` 增加到 `1522 B`。

| VLAN 标签字段                               | 长度       | 取值或作用                                          |
| --------------------------------------- | -------- | ---------------------------------------------- |
| 标签协议标识符（Tag Protocol Identifier，TPID）   | `16 bit` | 固定为 `0x8100`，表示该帧是 IEEE 802.1Q 帧               |
| 优先级（Priority，PRI）                       | `3 bit`  | 取值为 `0~7`，值越大优先级越高；网络拥塞时优先发送高优先级帧              |
| 规范格式指示符（Canonical Format Indicator，CFI） | `1 bit`  | `0` 表示 MAC 地址按规范格式封装，`1` 表示按非规范格式封装；以太网中取 `0`  |
| VLAN 标识符（VLAN Identifier，VID）           | `12 bit` | 标识帧所属 VLAN，取值范围为 `0~4095`，其中 `0` 和 `4095` 保留不用 |

- 可用的 VID 为 `1~4094`。
- 广播帧只在 VID 所标识的同一 VLAN 内转发，从而把广播域限制在一个 VLAN 内。

![[3.7.2-802.1Q标签字段.png|900]]

- VLAN 标签由 `16 bit` TPID 和 `16 bit` 标签控制信息组成，后者进一步包含 PRI、CFI 和 VID。

![[3.7.2-VLAN标识符与广播域.png|900]]

- 交换机利用 `12 bit` VID 识别帧所属的 VLAN，并阻止广播帧进入其他 VLAN。

### 3.7.2.2 Access 与 Trunk 端口

- 以太网交换机的端口按连接对象和标签处理方式分为 Access 端口与 Trunk 端口。
- 两类端口的 PVID 默认值均为 `1`。

| 对比维度 | Access 端口 | Trunk 端口 |
| --- | --- | --- |
| 典型连接对象 | 用户计算机 | 交换机 |
| 可承载的 VLAN | 只能属于一个 VLAN，PVID 与所属 VLAN 的 VID 相同 | 可以属于多个 VLAN，通过不同 VID 的帧 |
| 接收入方向 | 一般只接收未标记的普通以太网 MAC 帧，并按端口 PVID 添加 VLAN 标签 | 既可接收未标记帧，也可接收已标记的 IEEE 802.1Q 帧；未标记帧按端口 PVID 添加标签 |
| 发送出方向 | 仅当帧的 VID 等于端口 PVID 时转发，并在转发前去除标签 | 帧的 VID 等于端口 PVID 时去除标签后转发；不等于 PVID 时保留标签直接转发 |
| 发出的帧 | 不带 VLAN 标签的普通以太网 MAC 帧 | 可能是不带标签的普通以太网 MAC 帧，也可能是带标签的 IEEE 802.1Q 帧 |

- 互连的两个 Trunk 端口应使用相同的 PVID，否则可能出现转发错误。

![[3.7.2-Access与Trunk端口规则.png|900]]

- Access 端口在交换机内部用 PVID 标记帧，在面向用户计算机发送时去除匹配标签；Trunk 端口则根据 VID 与 PVID 是否相等决定去除或保留标签。

### 3.7.2.3 帧的标签处理过程

#### 单台交换机内的 Access 端口

1. 用户计算机向 Access 端口发送未标记帧。
2. Access 端口按自身 PVID 为帧添加 VLAN 标签。
3. 交换机只把帧转发到属于同一 VID 的端口。
4. 帧从 PVID 相同的 Access 端口发出前被去除标签，用户计算机收到普通以太网 MAC 帧。

![[3.7.2-Access端口标签处理.png|900]]

- 图中来自 VLAN3 的广播帧进入端口 3 后被标记为 `VID=3`，只转发到同属 VLAN3 的端口 4，不会进入 `PVID=2` 的端口 1 和端口 2。

#### Trunk 端口转发本征 VLAN 的帧

- 当帧的 VID 等于 Trunk 端口的 PVID 时，Trunk 端口去除 VLAN 标签后再发送。
- 对端 Trunk 端口收到未标记帧后，再按自己的 PVID 添加标签。

![[3.7.2-Trunk本征VLAN转发.png|900]]

- 两端 Trunk 端口的 PVID 都为 `1`，因此 VLAN1 的广播帧以未标记形式通过交换机间链路，并在进入对端交换机后重新标记为 `VID=1`。

#### Trunk 端口转发其他 VLAN 的帧

- 当帧的 VID 不等于 Trunk 端口的 PVID 时，Trunk 端口保留 VLAN 标签直接发送。
- 对端交换机根据帧中保留的 VID 继续限制转发范围。

![[3.7.2-Trunk带标签转发.png|900]]

- 两端 Trunk 端口的 PVID 都为 `1`，VLAN2 的广播帧以带有 `VID=2` 标签的形式通过交换机间链路，之后只从 VLAN2 的 Access 端口发出。

### 3.7.2.4 Hybrid 端口

- Hybrid 端口是华为交换机私有的端口类型。
- Hybrid 端口既可以用于交换机之间互连，也可以用于交换机与用户计算机之间互连。
- Hybrid 端口的大部分功能与 Trunk 端口相同，主要区别在发送处理：
  - 帧的 VID 位于端口的“去标签列表”中时，去除标签后转发。
  - 帧的 VID 不在端口的“去标签列表”中时，保留标签直接转发。

![[3.7.2-Hybrid端口规则.png|900]]

- Trunk 端口只以 PVID 判断是否去标签，Hybrid 端口则可通过去标签列表为多个 VLAN 分别指定发送形式。
