- 路由器是一种具有多个输入接口和输出接口的专用计算机，主要完成路由选择与分组转发。
- 路由选择部分运行路由选择协议并维护路由表，分组转发部分按转发表高速处理到达的数据报。
- 路由器内部通过交换结构把输入接口连接到选定的输出接口。

==路由选择属于控制功能，决定转发表内容；分组转发属于数据处理功能，必须对每个到达的数据报快速执行。==

### 4.4.6.1 路由选择部分

- 路由选择处理机运行路由选择协议。
- 路由选择处理机根据网络状态计算和维护路由表。
- 转发表由路由信息生成，供输入接口执行快速查找。
- 路由选择功能的重点是协议正确性和路由计算效率。

### 4.4.6.2 分组转发部分

1. 输入接口在物理层接收比特流，并在数据链路层处理帧。
2. 输入接口取出 IP 数据报并查找转发表。
3. 交换结构把数据报送到选定的输出接口。
4. 输出接口对数据报排队，并使用输出链路的帧格式重新封装。
5. 物理层把帧发送到下一跳。

- 输入或输出速度暂时不匹配时，需要缓冲区保存等待处理的数据报。
- 缓冲区溢出时会发生分组丢失。
- 转发表查找与交换结构的处理速度直接影响路由器转发性能。
- 实现交换结构的三种基本方式是：**通过存储器、通过总线、通过互连网络**。这三种交换结构可实现的路由器转发速率依次提高。
![[4.4.6-路由器结构.png|900]]

- 图中上方路由选择部分维护路由信息，下方输入接口、交换结构和输出接口共同完成逐分组转发。

