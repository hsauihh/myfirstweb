- 封装成帧是在网络层协议数据单元前后添加首部和尾部，形成数据链路层的帧。
- 帧定界使接收方能够从物理层交付的比特流中识别每个帧的开始和结束。
- 透明传输使数据链路层能够传送任意内容的上层协议数据单元，而不会把数据载荷误判为帧边界。

==封装成帧确定帧的结构与边界，透明传输保证数据载荷中的任意内容都不会破坏帧定界。==

### 3.2.1.1 封装成帧

- 封装成帧是在网络层交付的[[1.6.4 计算机网络体系结构中的专用术语#1.6.4.4 PDU 与 SDU|协议数据单元]]（PDU）前后分别添加帧首部和帧尾部，使其成为[[3.1 数据链路层概述#3.1.2 链路、数据链路和帧|帧]]。
- 帧首部和帧尾部包含必要的控制信息，其中一个作用是帧定界。
- 点对点协议（Point-to-Point Protocol，PPP）在帧的两端使用标志字段定界，数据载荷之外还包含地址、控制、协议和帧检验序列等字段。

![[3.2.1-封装成帧与帧定界.png|900]]

- PPP 使用首尾标志字段确定帧边界，接收方据此从连续比特流中分离各个帧。

#### 最大传送单元

- 为提高传输效率，帧的数据载荷长度通常应大于帧首部与帧尾部的长度之和。
- 最大传送单元（Maximum Transmission Unit，MTU）规定帧的数据载荷长度上限。
- PPP 帧的数据载荷最大为 `1500 B`。
- 以太网 V2 MAC 帧的数据载荷范围为 `46~1500 B`，帧的最大长度为 `1518 B`。

![[3.2.1-最大传送单元.png|900]]

- MTU 限制的是帧的数据载荷，不包含数据链路层添加的首部和尾部。
- 并非所有协议都在首部和尾部中设置专门的定界标志；以太网 V2 MAC 帧使用帧间间隔区分相邻帧。

### 3.2.1.2 透明传输

- 透明传输是指数据链路层对上层交付的 PDU 内容没有限制，如同数据链路层不存在一样。
- 如果数据载荷中出现与帧定界标志相同的内容，接收方可能把它误判为帧边界，因此必须进行填充处理。

#### 字节填充

- 面向字节的链路使用转义字符 `ESC` 实现字节填充。
- 发送方在数据载荷中的每个 `FLAG` 和 `ESC` 前插入一个 `ESC`。
- 接收方删除为转义而插入的 `ESC`，恢复原始数据载荷。

![[3.2.1-字节填充.png|900]]

- 字节填充用 `ESC` 区分数据载荷中的 `FLAG`、`ESC` 与真正的帧定界标志。

#### 比特填充

- 高级数据链路控制（High-level Data Link Control，HDLC）协议使用 `01111110` 作为帧定界标志。
- 发送方扫描数据载荷，每出现五个连续的比特 `1` 就插入一个比特 `0`。
- 接收方每遇到五个连续的比特 `1` 就删除其后的比特 `0`。

![[3.2.1-比特填充.png|900]]

- 红色比特 `0` 是发送方插入的填充位；零比特填充保证数据载荷中不会出现定界模式 `01111110`。
