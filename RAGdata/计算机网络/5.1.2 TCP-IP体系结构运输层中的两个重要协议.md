- TCP/IP 体系结构的运输层有两个重要协议：传输控制协议（Transmission Control Protocol，TCP）和用户数据报协议（User Datagram Protocol，UDP）。
- TCP 为上层提供面向连接的可靠数据传输服务；UDP 为上层提供无连接的不可靠数据传输服务。
- 二者共同承载 TCP/IP 应用层协议，应用可按通信需求选择运输层协议。

==TCP 以连接管理和可靠传输机制换取更强的服务保证，UDP 不建立连接并保持较简单的传输方式。==

### 5.1.2.1 传输控制协议 TCP

- TCP（Transmission Control Protocol，传输控制协议）向上层提供面向连接的可靠数据传输服务。
- 使用 TCP 通信时，双方在传送数据前必须先建立 TCP 连接；数据传输结束后必须释放 TCP 连接。
- TCP 为实现可靠传输使用连接管理、确认机制、超时重传、流量控制和拥塞控制等措施。
- TCP 的实现较复杂，TCP 报文段首部较大，占用的处理机资源较多。

### 5.1.2.2 用户数据报协议 UDP

- UDP（User Datagram Protocol，用户数据报协议）向上层提供无连接的不可靠数据传输服务。
- 使用 UDP 通信时，双方在传送数据前不需要建立连接。
- UDP 不为上层补充连接建立、确认和重传等可靠传输过程，具体应用是否需要可靠性由上层需求决定。

![[5.1.2-TCP与UDP特征.png|900]]

- 图中并列列出 TCP 的连接建立/释放与可靠传输措施，以及 UDP 的无连接特征；这也是两种运输层服务的基本分界。

### 5.1.2.3 典型应用与协议承载关系

| 因特网应用 | TCP/IP 协议 | 运输层协议或封装方式 |
| --- | --- | --- |
| 域名解析 | 域名系统（Domain Name System，DNS） | UDP |
| 文件传送 | 简单文件传送协议（Trivial File Transfer Protocol，TFTP） | UDP |
| 路由选择 | 路由信息协议（Routing Information Protocol，RIP） | UDP |
| 网络参数配置 | 动态主机配置协议（Dynamic Host Configuration Protocol，DHCP） | UDP |
| 网络管理 | 简单网络管理协议（Simple Network Management Protocol，SNMP） | UDP |
| 远程文件服务器 | 网络文件系统（Network File System，NFS） | UDP |
| IP 电话 | 专用协议 | UDP |
| 流媒体通信 | 专用协议 | UDP |
| IP 多播 | 网际组管理协议（Internet Group Management Protocol，IGMP） | 直接封装在 IPv4 数据报中（协议字段为 `2`） |
| 电子邮件 | 简单邮件传送协议（Simple Mail Transfer Protocol，SMTP） | TCP |
| 远程终端接入 | 电传机网络（TELNET） | TCP |
| 万维网 | 超文本传送协议（Hypertext Transfer Protocol，HTTP） | TCP |
| 文件传送 | 文件传送协议（File Transfer Protocol，FTP） | TCP |

![[5.1.2-典型应用协议.png|900]]

- 图中表格中的 IGMP 行把运输层协议写成 UDP，此项不正确；其余各行展示了典型应用协议与 UDP 或 TCP 的对应关系。
- ==IGMP 不经过 TCP 或 UDP，其报文直接封装在 IPv4 数据报中，IPv4 首部协议字段值为 `2`。==
