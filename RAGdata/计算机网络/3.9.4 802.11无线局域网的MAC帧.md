- 802.11 无线局域网的媒体接入控制（Media Access Control，MAC）帧分为数据帧、控制帧和管理帧。
- 数据帧可以包含四个地址字段，以同时表示无线收发站、接入点和端到端源站、目的站。
- 去往分配系统和来自分配系统两个控制位共同决定各地址字段的含义。

==802.11 数据帧中的地址 1 和地址 2 始终描述当前无线链路的接收方与发送方，端到端源地址和目的地址则由 To DS、From DS 的组合确定。==

### 3.9.4.1 MAC 帧类型

| 帧类型 | 作用 | 示例 |
| --- | --- | --- |
| 数据帧 | 在站点之间传输数据 | 802.11 数据帧 |
| 控制帧 | 配合数据帧清空区域、维护虚拟载波监听、接入信道并确认数据帧 | 确认帧（Acknowledgement，ACK）、请求发送帧（Request To Send，RTS）、允许发送帧（Clear To Send，CTS） |
| 管理帧 | 管理无线站与接入点（Access Point，AP）的关系 | 信标帧、关联相关帧 |

- 控制帧中的 ACK、RTS 和 CTS 与[[3.9.3 CSMA-CA协议|CSMA/CA]]的确认和信道预约过程配合使用。

### 3.9.4.2 数据帧格式

| 字段 | 长度 | 作用 |
| --- | --- | --- |
| 帧控制 | `2 B` | 指明协议版本、帧类型、分配系统方向及其他控制信息 |
| 持续期 | `2 B` | 指明信道将被占用的时间，供虚拟载波监听使用 |
| 地址 1 | `6 B` | 当前无线链路的接收方地址 |
| 地址 2 | `6 B` | 当前无线链路的发送方地址 |
| 地址 3 | `6 B` | 根据分配系统方向表示源地址、目的地址或 BSSID |
| 序号控制 | `2 B` | 对数据帧及其分片编号，支持可靠传输 |
| 地址 4 | `6 B` | 仅在无线分配系统中使用 |
| 数据载荷 | `0~2312 B` | 承载站点之间的数据 |
| 帧检验序列（Frame Check Sequence，FCS） | `4 B` | 检测 MAC 帧传输差错 |

- 帧控制字段由协议版本、类型、子类型、去往 DS、来自 DS、更多分片、重试、功率管理、更多数据、有线等效保密（Wired Equivalent Privacy，WEP）和顺序等子字段组成。
- 去往分配系统（To DS）与来自分配系统（From DS）各占 `1 bit`。
- 持续期字段用于数据帧、RTS 和 CTS 的虚拟载波监听。

![[3.9.4-MAC帧格式.png|900]]

- 数据帧可包含四个 `6 B` 地址字段，帧控制字段中的 To DS 和 From DS 决定它们的具体含义。

### 3.9.4.3 四个地址字段

- 分配系统（Distribution System，DS）连接 AP，并在无线局域网与其他网络之间传递帧。
- 基本服务集标识符（Basic Service Set Identifier，BSSID）用于标识 BSS。

| To DS | From DS | 地址 1 | 地址 2 | 地址 3 | 地址 4 |
| --- | --- | --- | --- | --- | --- |
| `0` | `0` | 目的地址 | 源地址 | BSSID | 未使用 |
| `0` | `1` | 目的地址 | 发送 AP 地址 | 源地址 | 未使用 |
| `1` | `0` | 接收 AP 地址 | 源地址 | 目的地址 | 未使用 |
| `1` | `1` | 接收 AP 地址 | 发送 AP 地址 | 目的地址 | 源地址 |

- 地址 1 是当前无线链路的直接接收方，地址 2 是当前无线链路的直接发送方。
- 无线站 A 通过 AP1 向分配系统中的站点 R 发送数据时：
  - To DS 为 `1`，From DS 为 `0`。
  - 地址 1 是 AP1 的 MAC 地址，地址 2 是 A 的 MAC 地址，地址 3 是 R 的 MAC 地址。
- 站点 R 通过 AP1 向无线站 A 发送数据时：
  - To DS 为 `0`，From DS 为 `1`。
  - 地址 1 是 A 的 MAC 地址，地址 2 是 AP1 的 MAC 地址，地址 3 是 R 的 MAC 地址。
- AP 具有网桥功能，在 802.11 数据帧与以太网帧之间转换时保持端到端源 MAC 地址和目的 MAC 地址不变。

![[3.9.4-无线帧地址字段.png|900]]

- R 发往 A 的以太网帧由 AP1 转换为 802.11 数据帧后，地址 1 指向 A，地址 2 标识 AP1，地址 3 保留源站 R。
