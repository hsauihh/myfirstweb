- 共享式以太网的多个站点共享同一传输媒体，通常通过总线或集线器连接；交换式以太网通过交换机按 MAC 帧转发。
- 集线器只在物理层复制比特，交换机在数据链路层（包括物理层）处理 MAC 帧。
- 交换机可以隔离碰撞域，但二层交换机不会隔离广播域。

==交换机把不同接口连接的网段分成独立碰撞域；在未划分 VLAN 的普通二层交换网络中，广播帧仍会在整个广播域内泛洪。==

### 3.5.2.1 单播、广播与并发通信

| 对比维度 | 共享式以太网（集线器） | 交换式以太网（交换机） |
| --- | --- | --- |
| 工作层次 | 物理层 | 数据链路层，并包含物理层功能 |
| 处理对象 | 比特 | MAC 帧 |
| 已知单播 | 把收到的比特复制到其他接口；只有目的网卡接收 | 查转发表，只从目的接口明确转发 |
| 广播帧 | 复制到所有其他接口，所有网卡接收 | 从除入接口外的其他接口泛洪，所有网卡接收 |
| 多对主机同时通信 | 共享同一信道，可能发生碰撞 | 不同接口对可同时存储转发，互不占用同一碰撞域 |

![[3.5.2-已知单播帧转发.png|900]]

- A 发给 B 的已知单播帧在集线器处到达其他接口，在交换机处只到达 B 所在接口。

![[3.5.2-广播帧转发.png|900]]

- 目的 MAC 地址为广播地址 `FF-FF-FF-FF-FF-FF` 时，集线器和交换机都会让同一广播域内的主机收到该帧。

![[3.5.2-并发通信对比.png|900]]

- 共享式以太网中多台主机同时发送会竞争同一信道并产生碰撞；交换式以太网可以在不同接口对之间并行转发。

### 3.5.2.2 碰撞域和广播域

| 设备 | 隔离碰撞域 | 隔离广播域 | 依据 |
| --- | --- | --- | --- |
| 集线器 | 否 | 否 | 只复制比特，所有接口共享同一信道 |
| 以太网交换机 | 是 | 否 | 按接口转发 MAC 帧，但广播帧仍向其他接口泛洪 |
| 路由器 | 是 | 是 | 在网络层转发，不把链路层广播跨接口转发 |

![[3.5.2-碰撞域与广播域.png|900]]

- 两个由集线器组成的网段经未划分 VLAN 的交换机连接后，形成一个更大的广播域和两个独立的碰撞域。
- 经集线器扩展会同时扩大广播域和碰撞域；经交换机扩展只扩大广播域并隔离碰撞域。
- 交换机配置[[3.7.1 虚拟局域网VLAN概述|虚拟局域网（VLAN）]]后，可以把一个物理交换网络划分为多个逻辑广播域。

![[3.5.2-碰撞域与广播域计数.png|900]]

- 图示网络中，四个 `100BASE-T` 集线器分别形成四个独立碰撞域；中间路由器把左右两侧分成两个广播域。
- 结论：==该网络的碰撞域数为 `4`，广播域数为 `2`。==

### 3.5.2.3 交换机与集线器混合连接

- 交换机可以把一个接口连接到集线器，因此该接口连接的仍是共享式半双工网段。
- H2 向 H4 发送数据时，交换机根据 MAC 地址把帧明确转发到集线器所在接口，集线器再把比特复制到 H3 和 H4。
- H4 返回确认帧时，集线器先把帧复制给 H3 和交换机，交换机再把帧明确转发给 H2；除 H4 外，从物理层能够收到该确认帧的主机为 H2、H3。

![[3.5.2-混合网络帧转发.png|900]]

- 混合拓扑中，交换机只决定跨网段的出口，集线器仍会在其共享网段内向其他接口复制比特。
