- 公钥密码体制为每个用户生成一对相互关联但用途不同的密钥：公开的公钥和保密的私钥。
- 公钥密码体制解决了大规模网络中对称密钥分发不便的问题，还可用于[[7.3.2 数字签名|数字签名]]。

==公钥可以公开，是因为无法在可行时间内由公钥推导出私钥，且公钥不能解密由自身加密的密文。==

### 7.2.3.1 公钥与私钥

- 公钥密码体制使用不同的加密密钥和解密密钥。
- 加密密钥称为公钥（Public Key，`PK`），可以向公众公开。
- 解密密钥称为私钥或秘钥（Secret Key，`SK`），必须由密钥所有者保密。
- 加密算法 `E` 和解密算法 `D` 都是公开的。
- 私钥由公钥决定，但不能根据公钥在可行时间内计算出私钥。
- 使用公钥加密并用对应私钥解密：$D_{SK}(E_{PK}(X))=X$。
- 加密与解密运算可以对调：$E_{PK}(D_{SK}(X))=X$。

![[7.2.3-公钥密码体制.png|900]]

- 接收方在本地生成一对公钥和私钥，公开公钥供发送方加密，私钥始终由自己保管并用于解密。
- 1978 年提出的 RSA 算法是公钥密码体制中最著名的算法之一。

### 7.2.3.2 与对称密钥密码体制结合

- 公钥密码体制的加密和解密速度比对称密钥密码体制慢得多，不适合直接加密大量数据。
- 实际通信通常使用公钥密码体制[[7.5 密钥分发|安全传送一次性会话密钥]]，再使用对称密钥密码体制保护会话数据。

1. 用户 A 选择一个与用户 B 共享的对称会话密钥。
2. A 使用 B 的公钥加密会话密钥并发送给 B。
3. B 使用自己的私钥解密，取得会话密钥。
4. A 与 B 使用该会话密钥和对称密钥算法进行后续加密通信。

![[7.2.3-混合加密建立会话密钥.png|900]]

- ==公钥算法解决会话密钥的安全传送问题，对称密钥算法承担大量数据的高效加密；两者是互补关系。==
