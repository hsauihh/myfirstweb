- 计算机网络的性能指标用于从不同方面度量网络性能。
- 常用指标包括速率、带宽、吞吐量、时延、时延带宽积、往返时间、利用率和丢包率。
- 这些指标可用于分析 [[1.3 电路交换、分组交换和报文交换#1.3.2 分组交换|分组交换]] 中分组的传输效率和等待时间。

==端到端数据传输速率由路径上的瓶颈速率决定；当链路利用率接近 1 时，排队时延会迅速增大。==

### 1.5.1 速率

- 速率表示数据传输的快慢，也称为比特率或数据率。
- 比特（bit，记为小写 `b`）是数据量的基本单位，一个比特就是二进制数字中的一个 `1` 或 `0`。
- 字节（byte，记为大写 `B`）及其常用换算关系：
  - $1\,\mathrm{B}=8\,\mathrm{bit}$
  - $1\,\mathrm{KB}=2^{10}\,\mathrm{B}$
  - $1\,\mathrm{MB}=2^{20}\,\mathrm{B}$
  - $1\,\mathrm{GB}=2^{30}\,\mathrm{B}$
  - $1\,\mathrm{TB}=2^{40}\,\mathrm{B}$
- 速率的基本单位是比特每秒（bit/s 或 b/s），常用换算关系：
  - $1\,\mathrm{kb/s}=10^3\,\mathrm{b/s}$
  - $1\,\mathrm{Mb/s}=10^6\,\mathrm{b/s}$
  - $1\,\mathrm{Gb/s}=10^9\,\mathrm{b/s}$
  - $1\,\mathrm{Tb/s}=10^{12}\,\mathrm{b/s}$

==数据量单位中的 `K`、`M`、`G`、`T` 按 $2$ 的幂换算，速率单位中的 `k`、`M`、`G`、`T` 按 $10$ 的幂换算。==

### 1.5.2 带宽与吞吐量

| 对比维度 | 带宽 | 吞吐量 |
| --- | --- | --- |
| 含义 | 通信线路所能传送数据的能力 | 单位时间内通过某个网络或接口的实际数据量 |
| 表示内容 | 线路可达到的最高数据传送速率 | 实际有多少数据通过网络 |
| 单位 | b/s | b/s |
| 限制关系 | 路径中各环节的带宽共同形成上限 | 受网络带宽限制 |

- 在模拟信号系统中，带宽表示信号所包含的各种不同频率成分所占据的频率范围，单位为 `Hz`。
- 在计算机网络中，数据传输速率取主机接口速率、线路带宽、交换机或路由器接口速率中的最小值：
  - $R=\min(R_{\text{主机接口}},B_{\text{链路}},R_{\text{转发接口}})$
- 变量：
  - $R$：端到端数据传输速率，单位为 `b/s`。
  - $R_{\text{主机接口}}$：主机接口速率，单位为 `b/s`。
  - $B_{\text{链路}}$：线路带宽，单位为 `b/s`。
  - $R_{\text{转发接口}}$：交换机或路由器接口速率，单位为 `b/s`。
- 同一接口同时接收和发送数据时，吞吐量可由各方向的实际速率相加得到。

==一条端到端路径的最高传输速率取决于速率最低的环节，即木桶效应。==

### 1.5.3 时延

- 时延由发送时延、传播时延、排队时延和处理时延组成：
  - $d=d_{\text{send}}+d_{\text{prop}}+d_{\text{queue}}+d_{\text{proc}}$

| 时延类型 | 产生原因 | 计算 |
| --- | --- | --- |
| 发送时延 | 将一个分组的全部比特送入链路 | $d_{\text{send}}=\dfrac{L}{R}$ |
| 传播时延 | 信号在链路中传播 | $d_{\text{prop}}=\dfrac{s}{v}$ |
| 排队时延 | 分组在交换节点等待转发 | 随网络通信量变化，不便直接计算 |
| 处理时延 | 交换节点检查首部、差错并确定转发方向 | 随设备处理过程变化，不便直接计算 |

- 变量：
  - $L$：分组长度，单位为 `bit`。
  - $R$：发送速率，单位为 `b/s`。
  - $s$：信道长度，单位为 `m`。
  - $v$：信号传播速率，单位为 `m/s`。
- 发送时延取决于分组长度和发送速率，传播时延取决于信道长度和信号传播速率。

![[1.5-分组时延组成.png|900]]

- 路由器完整接收分组后，还会经历排队和处理，再把分组转发到下一条链路。

### 1.5.4 时延带宽积与往返时间

- 时延带宽积是传播时延和带宽的乘积：
  - $V=d_{\text{prop}}\times B$
- 变量：
  - $V$：链路中已经存在的比特数，单位为 `bit`。
  - $d_{\text{prop}}$：传播时延，单位为 `s`。
  - $B$：链路带宽，单位为 `b/s`。
- 时延带宽积表示发送端连续发送数据时，链路能够容纳的比特数量。
- 例：链路长 $1\,\mathrm{km}$，信号传播速率为 $2\times10^8\,\mathrm{m/s}$，带宽为 $1\,\mathrm{Gb/s}$：
  - $d_{\text{prop}}=\dfrac{10^3}{2\times10^8}=5\,\mu\mathrm{s}$
  - $V=5\,\mu\mathrm{s}\times1\,\mathrm{Gb/s}=5000\,\mathrm{bit}$
- 往返时间（Round-Trip Time，RTT）是从发送端开始发送数据分组，到发送端收到接收端返回的相应确认分组为止所耗费的总时间。
- `ping` 命令可以测量主机之间的 RTT。

### 1.5.5 利用率与丢包率

- 链路利用率增大时，该链路引起的时延会迅速增加。
- 网络通信量不断增大时，交换节点中的排队时延随之增大。
- 利用率与时延的关系：
  - $D=\dfrac{D_0}{1-U}$
- 变量：
  - $D$：当前网络时延，单位为 `s`。
  - $D_0$：网络空闲时的时延，单位为 `s`。
  - $U$：网络利用率，取值范围为 $0\le U<1$。
- 当 $U=0.5$ 时，$D=2D_0$；当 $U$ 趋近于 $1$ 时，$D$ 急剧增大。
- 丢包率是在一定时间范围内，传输过程中丢失的分组数量与总分组数量的比率：
  - $p=\dfrac{N_{\text{丢失}}}{N_{\text{总计}}}\times100\%$
- $N_{\text{丢失}}$ 和 $N_{\text{总计}}$ 必须在同一统计时间范围和统计对象内计数。
- 丢包率可以按接口、节点、链路、路径或整个网络统计。
