- 传输控制协议（Transmission Control Protocol，TCP）是面向连接的协议，传送数据前必须建立连接，数据传输结束后还要释放连接。
- 建立连接使通信双方确认彼此存在、协商参数，并分配和初始化运输实体资源。
- TCP 通过“三报文握手”建立连接，通过“四报文挥手”释放连接，并使用保活计时器发现失去响应的对端。

==三报文握手只有在双方都确认对方的收发能力后才进入连接已建立状态；四报文挥手允许两个方向的连接分别关闭。==

### 5.3.2.1 建立 TCP 连接的目的

- TCP 连接是一条逻辑连接。
- 建立连接需要解决三个问题：
  - 使 TCP 双方确认对方存在。
  - 协商最大报文段长度、最大窗口大小和时间戳选项等参数。
  - 分配并初始化缓存、状态变量和连接表项目等运输实体资源。

![[5.3.2-建立TCP连接的目的.png]]

- 图中列出了建立 TCP 连接时需要完成的对端确认、参数协商和资源初始化。

### 5.3.2.2 三报文握手

- 参与者：主动打开连接的 TCP 客户进程和被动打开连接的 TCP 服务器进程。
- 前置条件：客户端处于 `CLOSED` 状态，服务器完成传输控制块（Transmission Control Block，TCB）初始化并处于 `LISTEN` 状态。
- `SYN` 是同步标志位，`ACK` 是确认标志位；`seq` 表示序号，`ack` 表示确认号，大写 `ACK` 与小写 `ack` 不是同一字段。

1. 客户端发送 TCP 连接请求报文段：`SYN=1, seq=x`，随后进入 `SYN-SENT` 状态。
2. 服务器收到连接请求后发送连接请求确认报文段：`SYN=1, ACK=1, seq=y, ack=x+1`，随后进入 `SYN-RCVD` 状态。
3. 客户端收到确认后发送普通 TCP 确认报文段：`ACK=1, seq=x+1, ack=y+1`，客户端进入 `ESTABLISHED` 状态；服务器收到该报文段后也进入 `ESTABLISHED` 状态。

- `SYN=1` 的报文段不能携带数据，但需要消耗一个序号。
- 第三个报文段可以携带数据；不携带数据时不消耗序号，客户端下一个数据报文段的序号仍为 `x+1`。

![[5.3.2-三报文握手.png]]

- 图中两端依次经历 `CLOSED -> SYN-SENT -> ESTABLISHED` 与 `LISTEN -> SYN-RCVD -> ESTABLISHED`，三个报文段同时完成初始序号交换和双向确认。

### 5.3.2.3 失效连接请求与资源占用

- 两报文握手无法防止早已失效的连接请求在网络中滞留后到达服务器。
- 如果服务器收到失效请求并在发送确认后立即进入连接已建立状态，客户端会忽略服务器的确认，服务器却会为不存在的连接持续占用资源。
- 第三个报文段使服务器在收到客户端对其初始序号的确认后，才进入 `ESTABLISHED` 状态。

![[5.3.2-失效连接请求造成资源浪费.png]]

- 图中失效连接请求使采用两报文握手的服务器错误进入连接已建立状态，而客户端已关闭且不会使用该连接，最终造成资源浪费。

### 5.3.2.4 四报文挥手

- TCP 连接是全双工连接，一个方向的数据传输结束后，反方向仍可继续传输数据，因此两个方向需要分别释放。
- 以下流程以客户端主动关闭、服务器被动关闭为例。

1. 客户端发送连接释放报文段：`FIN=1, ACK=1, seq=u, ack=v`，进入 `FIN-WAIT-1` 状态。
2. 服务器发送普通确认报文段：`ACK=1, seq=v, ack=u+1`，进入 `CLOSE-WAIT` 状态；客户端收到后进入 `FIN-WAIT-2` 状态，此时客户端到服务器方向的连接已经释放，TCP 连接处于半关闭状态，服务器仍可向客户端发送数据。
3. 服务器没有数据需要发送时，发送连接释放报文段：`FIN=1, ACK=1, seq=w, ack=u+1`，进入 `LAST-ACK` 状态。
4. 客户端发送普通确认报文段：`ACK=1, seq=u+1, ack=w+1`，进入 `TIME-WAIT` 状态；服务器收到后进入 `CLOSED` 状态并撤销 TCB。

- `FIN` 是终止标志位；`FIN=1` 的报文段即使不携带数据也要消耗一个序号。

![[5.3.2-四报文挥手.png]]

- 图中服务器确认客户端的释放请求后仍可继续发送数据，直到服务器自身发送 `FIN`；这就是释放连接通常需要四个报文段的原因。

#### TIME-WAIT 状态

- 客户端发送最后一个确认后，需要在 `TIME-WAIT` 状态等待 `2MSL`；最长报文段寿命（Maximum Segment Lifetime，MSL）表示报文段在网络中的最长存活时间。
- 等待 `2MSL` 可以保证服务器收到最后一个确认；若该确认丢失，客户端仍能收到服务器重传的 `FIN` 并再次确认。
- 等待 `2MSL` 还可以使本次连接持续时间内产生的报文段从网络中消失，避免旧连接报文段出现在新的同端点连接中。
- 征求意见稿（Request for Comments，RFC）793 建议 MSL 取 `2 min`，即 `TIME-WAIT` 等待 `4 min`；具体实现可以采用更小的 MSL。

![[5.3.2-TIME-WAIT状态.png|900]]

- 图中最后确认丢失时，客户端在 `TIME-WAIT` 期间仍可接收服务器重传的连接释放报文段并重新发送确认。

### 5.3.2.5 TCP 保活计时器

- 保活计时器用于发现已建立连接中因故障而长期不再响应的对端，避免服务器无限期保留连接资源。

1. 服务器每收到一次客户端数据，就重新设置保活计时器，通常为 `2 h`。
2. 保活计时器到期后，服务器向客户端发送 TCP 探测报文段。
3. 服务器随后每隔 `75 s` 再发送一个探测报文段。
4. 连续发送 `10` 个探测报文段仍未收到客户端响应时，服务器认为客户端所在主机出现故障并关闭连接。

![[5.3.2-TCP保活计时器.png|900]]

- 图中服务器在保活计时器到期后按 `75 s` 间隔连续探测，达到 `10` 次仍无响应时关闭连接。
