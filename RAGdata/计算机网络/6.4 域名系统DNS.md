- 域名系统（Domain Name System，DNS）用于把便于记忆的域名转换为对应的 IP 地址。
- DNS 使用层次树状的域名结构和分布式域名服务器共同完成域名解析。

==DNS 通过分布式、层次化的域名服务器体系完成域名到 IP 地址的映射，查询通常由主机到本地域名服务器递归进行，其余过程迭代进行。==

==DNS报文使用运输层的UDP进行封装，运输层端口号为53==

### 6.4.1 域名结构

- 域名由若干分量组成，各分量之间用点号分隔。
- 最低级域名写在最左侧，顶级域名写在最右侧。
- 每一级域名由英文字母和数字组成，不区分大小写，最长 `63` 个字符。
- 完整域名最长 `255` 个字符。
- 每一级域名由上一级域名管理机构管理，顶级域名由因特网名称与数字地址分配机构（Internet Corporation for Assigned Names and Numbers，ICANN）管理。

![[6.4-域名结构.png|900]]

- `nic.hnust.edu.cn` 从左到右依次表示四级、三级、二级和顶级域名。

- 顶级域名（Top Level Domain，TLD）包括：
  - 国家顶级域名，如 `cn`、`us`、`uk`。
  - 通用顶级域名，如 `com`、`net`、`org`、`edu`、`gov`、`mil`、`int`。
  - 反向域 `arpa`，用于反向域名解析，即由 IP 地址查询域名。
- 我国二级域名可分为类别域名和行政区域名。

![[6.4-域名空间.png|900]]

- 域名空间从根向下逐层划分，各管理机构只负责其授权范围内的域名。

### 6.4.2 域名服务器

- DNS 使用分布式数据库，任何一台域名服务器都只保存域名空间的一部分信息。
- DNS 服务器保存所管辖范围内域名与 IP 地址的映射。

| 服务器类型 | 作用 |
| --- | --- |
| 根域名服务器 | 知道所有顶级域名服务器的地址，通常返回下一步应查询的顶级域名服务器 |
| 顶级域名服务器 | 管理在该顶级域名下注册的二级域名，并返回下一步应查询的权限域名服务器 |
| 权限域名服务器 | 负责一个区，保存该区内域名的权威映射信息 |
| 本地域名服务器 | 代表主机进行域名查询，通常由网络服务提供者或组织配置 |

- 根域名服务器采用**任播技术**，在全球部署多台具有相同 IP 地址的实例。
- 一个区可以小于或等于一个域，但不能跨越域的边界。

### 6.4.3 域名解析过程

- 递归查询要求被查询服务器代替请求者继续查询并返回最终结果。
- 迭代查询允许被查询服务器返回下一台应查询服务器的地址，由请求者继续查询。
- 典型查询中，主机向本地域名服务器发送递归查询；本地域名服务器依次向根、顶级和权限域名服务器发送迭代查询。

![[6.4-DNS查询方式.png|900]]

- 左图全部使用递归查询；右图是常用模式，只有主机到本地域名服务器为递归查询，其余为迭代查询。

### 6.4.4 高速缓存

- 域名服务器缓存最近查询结果及其来源，以减少查询次数并提高效率。
- 缓存中的记录设置生存时间，到期后必须删除，避免长期保存失效映射。
- 主机也可以缓存最近查询到的域名与 IP 地址映射。
