- 数据链路层需要解决封装成帧与透明传输、差错检测和可靠传输三个重要问题。
- 封装成帧使接收方能够识别帧的边界，透明传输使任意上层协议数据单元都能作为帧的数据载荷。
- 差错检测用于发现传输差错；是否进一步恢复丢失或出错的数据，取决于是否提供可靠传输服务。

==封装和透明传输规定“怎样形成并识别帧”，差错检测回答“帧是否出错”，可靠传输机制负责“出错或丢失后怎样恢复”。==

### 3.2.1 封装成帧和透明传输

- 封装成帧通过添加首部和尾部形成帧，并使接收方能够识别帧边界。
- 透明传输通过字节填充或比特填充，使数据载荷可以包含任意内容。
- 详细原理见[[3.2.1 封装成帧和透明传输|封装成帧和透明传输]]。

### 3.2.2 差错检测

- 差错检测码用于判断帧在传输过程中是否发生比特差错。
- 常用方法包括奇偶校验和循环冗余校验。
- 详细原理见[[3.2.2 差错检测|差错检测]]。

### 3.2.3 可靠传输

- 不可靠传输服务只丢弃检测出误码的帧，可靠传输服务还要恢复出错或丢失的数据。
- 停止-等待协议、回退 N 帧协议和选择重传协议使用确认、超时与重传实现可靠传输。
- 详细机制见[[3.2.3 可靠传输|可靠传输]]。
