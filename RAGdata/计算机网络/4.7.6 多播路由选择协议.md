- 多播路由选择协议为每个多播组建立连接多播源与组成员所在路由器的多播转发树。
- 转发树必须随多播组成员的加入和退出动态变化。
- 转发树可以采用面向单个源的基于源树，也可以采用由所有多播源共用的组共享树。

==基于源树先按到源点的反向最短路径形成广播树，再通过剪枝得到多播树；组共享树则让成员路由器沿单播路径接入核心路由器。==

![[4.7.6-多播路由选择方法.png]]
### 4.7.6.1 基于源树的多播路由选择

- 反向路径多播（Reverse Path Multicasting，RPM）是基于源树的典型算法。

1. 使用反向路径广播（Reverse Path Broadcasting，RPB）建立无环路的广播转发树。
2. 使用剪枝（Pruning）删除广播转发树中的下游非成员路由器，得到多播转发树。

![[4.7.6-反向路径多播RPM.png]]

- RPM 先形成覆盖范围较大的广播树，再按实际成员分布剪去无用分支。
![[4.7.6-洪泛法.png]]
- 洪泛法会导致产生大量广播分组，在有环路的网络中无限制地复制转发。
#### 反向路径广播RPB

1. 路由器收到广播分组后，检查分组是否从源点到本路由器的最短路径方向到达。
2. 若分组来自该最短路径上的接口，路由器从除入接口外的其他接口转发分组。
3. 若分组不是沿最短路径到达，路由器丢弃该分组。
4. 存在多条等长最短路径时，选择相邻路由器 IP 地址最小的那条路径。

![[4.7.6-反向路径广播规则.png]]

- 反向路径检查使每台路由器只接受一个方向来的副本，从而避免转发环路和重复广播分组。

#### 剪枝与嫁接

- 下游路由器若没有该多播组成员，也没有需要继续转发的下游分支，就向上游发送剪枝报文并退出转发树。
- 被剪枝分支后来出现组成员时，下游路由器向上游发送嫁接报文，把该分支重新加入多播转发树。

![[4.7.6-剪枝.png|900]]

- R8 没有组成员时向 R5 发送剪枝报文，R5 随后停止向该分支转发多播数据。

![[4.7.6-嫁接.png|900]]

- R8 连接的局域网出现组成员后发送嫁接报文，恢复 R5 到 R8 的转发分支。

### 4.7.6.2 组共享树与核心路由器

- 组共享树采用基于核心的分布式生成树算法。
- 每个多播组指定一个核心路由器，并以核心路由器为根建立连接所有成员所在路由器的转发树。
- 成员所在路由器向核心路由器单播发送加入报文。
- 加入报文到达核心路由器或已经属于该转发树的节点后停止，所经过的路径成为转发树的新分支。
![[4.7.6-组共享树工作原理.png]]

![[4.7.6-基于核心的转发树.png|900]]

- 图中各成员路由器沿加入报文的路径接入核心路由器 R5，所有多播源共用同一棵组共享树。

### 4.7.6.3 因特网多播路由选择协议

| 协议 | 英文名称与缩写 | RFC |
| --- | --- | --- |
| 距离向量多播路由选择协议 | Distance Vector Multicast Routing Protocol，DVMRP | RFC 1075 |
| 开放最短路径优先的多播扩展 | Multicast Extensions to OSPF，MOSPF | RFC 1585 |
| 协议无关多播-稀疏方式 | Protocol Independent Multicast-Sparse Mode，PIM-SM | RFC 2362 |
| 协议无关多播-密集方式 | Protocol Independent Multicast-Dense Mode，PIM-DM | RFC 3973 |
| 基于核心的转发树 | Core Based Tree，CBT | RFC 2189、RFC 2201 |

- 没有在整个因特网范围统一使用的多播路由选择协议。
- 因特网工程任务组推动过全球多播主干网（Multicast Backbone On the Internet，MBONE）的建设。
- 改变已经成功运行并广泛部署的网络层协议非常困难，因此 IP 多播主要应用于局部的园区网、专用网或[[4.6 虚拟专用网VPN和网络地址转换NAT|虚拟专用网]]。

![[4.7.6-因特网多播路由选择协议.png]]

- 图中列出了建议使用的多播路由选择协议及其 RFC，并说明 IP 多播尚未在整个因特网上大规模统一部署。
