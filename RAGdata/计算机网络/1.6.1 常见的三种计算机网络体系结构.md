- 计算机网络体系结构是计算机网络各层及其协议的集合。
- 分层体系结构把复杂的网络通信功能划分为职责明确的层次。
- 常见体系结构包括开放系统互连（Open Systems Interconnection，OSI）参考模型、TCP/IP 参考模型和适于教学的五层原理体系结构。

==OSI 参考模型是法律上的国际标准，TCP/IP 参考模型是事实上的国际标准，五层原理体系结构用于教学。==

### 1.6.1.1 OSI 参考模型

- OSI 参考模型自上而下分为七层：

| 层次 | 名称 |
| --- | --- |
| 7 | 应用层 |
| 6 | 表示层 |
| 5 | 会话层 |
| 4 | 运输层 |
| 3 | 网络层 |
| 2 | 数据链路层 |
| 1 | 物理层 |

- OSI 标准没有得到广泛应用的原因：
  - 专家缺少实际经验，完成标准时没有商业驱动力。
  - 协议实现过分复杂，运行效率很低。
  - 标准制定周期太长，产品无法及时进入市场。
  - 层次划分不太合理，有些功能在多个层次中重复出现。

### 1.6.1.2 TCP/IP 参考模型

- TCP/IP 参考模型自上而下分为四层：

| 层次 | 典型协议或接口 |
| --- | --- |
| 应用层 | HTTP、SMTP、DNS、RTP 等 |
| 运输层 | TCP、UDP |
| 网际层 | IP |
| 网络接口层 | 以太网、Wi-Fi 等多种网络接口 |

- TCP/IP 的网络接口层没有规定具体内容，可以使用不同的网络接口。
- IP 协议处于体系结构的核心：
  - `Everything over IP`：IP 可以为多种网络应用提供服务。
  - `IP over everything`：IP 可以互连不同的网络接口。

### 1.6.1.3 五层原理体系结构

- 五层原理体系结构自上而下包括应用层、运输层、网络层、数据链路层和物理层。
- 它由 OSI 与 TCP/IP 两种体系结构折中得到：
  - 将 OSI 的应用层、表示层和会话层合并为应用层。
  - 将 TCP/IP 的网络接口层重新划分为数据链路层和物理层。

![[1.6.1-三种体系结构对照.png|900]]

- OSI 模型强调完整的法律标准，TCP/IP 模型反映实际使用的协议族，五层模型便于学习计算机网络原理。
