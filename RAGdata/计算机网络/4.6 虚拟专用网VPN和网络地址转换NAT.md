- 虚拟专用网（Virtual Private Network，VPN）利用公用因特网作为不同专用网之间的通信载体。
- 网络地址转换（Network Address Translation，NAT）在专用地址与全球地址之间进行转换，使专用网主机能够访问因特网。
- 网络地址与端口号转换（Network Address and Port Translation，NAPT）进一步使用端口号区分不同通信，使多台主机能够共用一个全球 IPv4 地址。

==VPN 通过隧道在公用网络上传送专用网数据；NAT/NAPT 通过修改地址或端口复用全球 IPv4 地址，但通信通常必须由专用网内部发起。==

### 4.6.1 虚拟专用网VPN

- VPN 使用公用因特网把同一机构的不同专用网在逻辑上连接起来。
- 专用地址由机构自行分配，只在机构内部有效，无需向因特网的管理机构申请。
- RFC 1918 规定以下三个 [[4.2.2 IPv4地址及其编址方法#4.2.2.4 无分类编址 CIDR|无分类域间路由选择（CIDR）]] 地址块作为专用 IPv4 地址块：

| CIDR 地址块 | 地址范围 |
| --- | --- |
| `10.0.0.0/8` | `10.0.0.0~10.255.255.255` |
| `172.16.0.0/12` | `172.16.0.0~172.31.255.255` |
| `192.168.0.0/16` | `192.168.0.0~192.168.255.255` |

- 不同机构可以重复使用相同的专用地址，因为这些地址只在各自机构内部使用。
- ==因特网中的路由器不转发目的地址为专用地址的 [[4.2.7 IPv4数据报的首部格式|IPv4 数据报]]。==

![[4.6-VPN专用地址范围.png]]

- 图中列出了三个专用地址块；它们不能作为公用因特网上的可路由目的地址。

#### 隧道传输

1. 源主机生成源地址和目的地址均为专用地址的内部 IP 数据报。
2. 入口路由器加密整个内部 IP 数据报，并添加以两端路由器全球地址为源地址和目的地址的外部首部。
3. 因特网中的路由器根据外部首部转发封装后的数据报。
4. 出口路由器去除外部首部并解密，恢复内部 IP 数据报后送入目的专用网。

![[4.6-VPN隧道传输.png|900]]

- 外部首部使封装后的数据报能够通过因特网转发，加密的内部数据报则保持两端专用网之间的逻辑通信关系。

#### VPN 类型

| 类型 | 连接对象 | 接入方式 |
| --- | --- | --- |
| 内联网 VPN | 同一机构内不同部门的专用网 | 两端路由器通过因特网建立隧道 |
| 外联网 VPN | 机构专用网与合作伙伴等外部机构 | 把外部机构纳入 VPN |
| 远程接入 VPN | 外地员工与公司专用网 | 员工主机运行 VPN 软件，通过因特网与公司建立隧道 |

![[4.6-内联网VPN与外联网VPN.png|900]]

- 内联网 VPN、外联网 VPN 和远程接入 VPN 的区别在于参与者与接入位置，三者都使用公用网络承载逻辑上的专用连接。

### 4.6.2 网络地址转换NAT

- NAT 路由器至少拥有一个有效的全球 IPv4 地址，并维护专用地址与全球地址之间的转换表。
- 安装在路由器中的 NAT 软件在数据报经过时查表并修改相应地址。
- 基本 NAT 对 [[4.2.2 IPv4地址及其编址方法|IPv4 地址]]进行一对一转换。

#### 基本NAT的转换过程

1. 专用网主机向因特网主机发送数据报。
2. NAT 路由器查找或建立转换表项，把源专用地址改为对应的全球地址。
3. 因特网主机把响应数据报发送到该全球地址。
4. NAT 路由器根据转换表把响应数据报的目的全球地址改回专用地址，再转发给内部主机。

![[4.6-NAT出站地址转换.png|900]]

- 出站转换修改源地址，使带专用源地址的数据报能够在因特网上转发。

![[4.6-NAT入站地址转换.png]]

- 入站转换根据 NAT 表把目的全球地址还原为内部主机的专用地址。

#### 网络地址与端口号转换NAPT

- 大多数基于 TCP/IP 的网络应用使用 TCP 或 UDP，运输层端口号可以区分不同应用进程的通信。
- NAPT 同时转换 IP 地址和端口号，使多台专用网主机能够同时共用 NAT 路由器上的一个全球 IPv4 地址。

| 对比维度 | 基本 NAT | NAPT |
| --- | --- | --- |
| 转换字段 | IP 地址 | IP 地址和端口号 |
| 表项标识 | 专用地址与全球地址 | 内部地址和端口与全球地址和端口 |
| 全球地址复用 | 一个活动映射占用一个全球地址 | 多个活动通信可共用一个全球地址 |

1. 内部主机发送数据报时，NAPT 把源专用地址和源端口号改为全球地址和新的端口号。
2. 不同内部通信使用不同的全球端口号，NAPT 表据此保持唯一映射。
3. 响应数据报到达后，NAPT 根据目的全球地址和目的端口号查表。
4. NAPT 把目的地址和端口号恢复为内部值，再把数据报转发给相应主机。

![[4.6-NAPT地址端口转换.png|900]]

- 图中 `192.168.0.6:30000` 和 `192.168.0.9:30000` 分别映射为 `113.218.175.235:50001` 和 `113.218.175.235:50002`，端口号使两个通信能够共用同一全球地址。

#### NAT与NAPT的特点

- NAT 和 NAPT 在很大程度上缓解了 IPv4 地址资源紧张。
- NAT 和 NAPT 对网络应用并不完全透明，可能影响部分应用。
- 通信必须由专用网内部发起，因此拥有专用地址的主机不能直接充当因特网中的服务器。
- 外网主机不能首先向内网主机发起通信。

![[4.6-NAT与NAPT特点.png|900]]

- 图中外网到内网的主动通信没有对应的既有转换表项，NAPT 路由器无法确定应把数据报交给哪台内部主机。
![[4.6-NAT与NAPT基本特征.png]]
