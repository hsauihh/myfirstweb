- [[#1.3.1 电路交换|电路交换]]在通信前建立端到端连接，并在通信期间持续占用已分配的通信资源。
- [[#1.3.2 分组交换|分组交换]]把报文划分为较小的数据段并添加首部，以分组为单位逐段存储转发。
- [[#1.3.3 报文交换|报文交换]]以完整报文为传送单位，各交换节点接收完整报文后再存储转发。
- 三种交换方式说明数据如何经过交换节点到达通信对端，[[1.5 计算机网络的性能指标|性能指标]]可用于比较其传输时延和通信线路利用率。

==计算机数据具有突发性，通常采用分组交换，以提高通信线路的利用率。==

### 1.3.1 电路交换

- 传统电话网采用电路交换，电话交换机为通信双方建立端到端连接，并为该连接分配通信资源。
- 电路交换分为三个阶段：

1. 建立连接：主叫方拨号请求连接；被叫方摘机后，网络在双方之间建立专用物理通路并分配通信资源。
2. 通话：双方通过已建立的连接通信，整个通话期间始终占用该连接的通信资源，其他用户不能使用。
3. 释放连接：通信结束并挂机后，交换机释放专用物理通路，将占用的通信资源归还给电信网。

![[1.3-电路交换三阶段.png|900]]

- 电路交换先分配通信资源，通信期间由双方独占，通信结束后才释放资源。

#### 用于计算机数据传输

- 计算机之间的数据传送具有突发性，实际发送和接收数据通常是间歇的。
- 若为突发通信长期建立专用电路，通信资源会在无数据可传时被闲置。
- 线路上真正用于传送数据的时间往往不到 `10%`，甚至不到 `1%`，线路利用率很低。
- 电路交换适合连续传送大量数据，并且数据传送时间远大于建立连接时间的场景。

### 1.3.2 分组交换

#### 报文、数据段和分组

- 报文是表示一条完整消息的整块数据。
- 源主机把较长报文划分为若干较小的等长数据段，并在每个数据段前添加首部，构成分组。
- 首部包含必要的控制信息，其中至少包含分组的目的地址。
- 交换节点根据首部中的目的地址决定分组的下一跳。

#### 存储转发

1. 源主机构造分组，并将分组逐个发送给直接相连的路由器。
2. 路由器完整接收一个分组后先将其存储，再从首部提取目的地址。
3. 路由器根据目的地址查找转发表，确定转发接口和下一跳。
4. 分组经过多个路由器逐段存储转发，最终到达目的主机。
5. 目的主机去掉各分组的首部，将数据段重新组合，还原原始报文。

![[1.3-分组交换存储转发.png|900]]

- 分组在哪一段链路上传送，就只占用该段链路，网络中的其他链路仍可用于其他通信。

#### 特点

- 源主机负责构造并发送分组。
- 路由器负责缓存、查表和转发分组。
- 目的主机负责接收分组并还原报文。
- 数据报式分组交换不需要建立和释放连接，也不预先分配端到端通信资源。
- 分组在传输过程中逐段占用通信链路，适合传送突发数据，通信线路利用率较高。
- 不同分组可以独立选择转发路径，使网络具有较好的生存性。

#### 带来的问题

- 分组首部会产生额外的传输开销。
- 交换节点存储转发分组会产生时延。
- 网络不能保证端到端所需资源随时可用；通信量较大时可能发生网络拥塞。
- 分组可能出现失序、丢失和误码，需要相应机制处理。

### 1.3.3 报文交换

- 报文交换是分组交换的前身。
- 报文交换不拆分报文，而是把完整报文作为传送单位。
- 每个交换节点必须先完整接收并存储整个报文，再查找转发表并转发到下一节点。
- 与分组交换相比，报文交换的转发时延更长，交换节点需要的缓存空间也更大。

### 1.3.4 三种交换方式的对比

| 对比维度 | 电路交换 | 报文交换 | 分组交换（数据报方式） |
| --- | --- | --- | --- |
| 通信前是否建立连接 | 需要，预先分配端到端通信资源 | 不需要 | 不需要 |
| 通信后是否释放连接 | 需要 | 不需要 | 不需要 |
| 数据传送方式 | 连接建立后，数据通过中间交换节点直通 | 整个报文逐节点存储转发 | 各分组逐节点存储转发，可形成流水式传输 |
| 交换节点接收范围 | 不必缓存完整报文后再转发 | 必须接收完整报文后再转发 | 收到一个完整分组后即可转发该分组 |
| 链路占用 | 通信期间持续占用整条端到端连接 | 转发报文时逐段占用 | 转发分组时逐段占用 |
| 缓存需求 | 不以存储完整报文为前提 | 需容纳完整报文 | 只需容纳一个完整分组，通常小于完整报文 |
| 主要时延来源 | 建立连接；建立后数据直通 | 整个报文在各节点的存储转发 | 各分组在各节点的存储转发 |
| 适用情况 | 连续传送大量数据，且传送时间远大于连接建立时间 | 分组交换出现前的存储转发方式 | 计算机网络中的突发数据传送 |

![[1.3-三种交换方式对比.png|900]]

- 在链路速率相同且忽略排队、处理、传播、重传和首部开销的条件下，分组可以流水式存储转发，因此整个报文比采用报文交换更早到达目的主机。
- 较小的分组可以避免过长报文长时间占用链路，也有利于差错控制。

==三种交换方式的主要区别在于是否预先分配端到端通信资源，以及存储转发的单位是完整报文还是分组。==
