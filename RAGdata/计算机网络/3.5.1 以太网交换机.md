- 以太网交换机是工作在数据链路层（包括物理层）的多接口网桥，使用转发表按目的 MAC 地址转发帧。
- 交换机的每个接口可以连接计算机、[[3.4.3 使用集线器的共享式以太网|集线器]]或另一个交换机。
- 交换机通过自学习逐步建立 MAC 地址与接口的对应关系，并据此决定明确转发、盲目转发或丢弃。

==交换机直连计算机或交换机时可使用全双工并行通信，不需要 CSMA/CD；接口连接集线器时仍只能半双工并使用 CSMA/CD。==

### 3.5.1.1 接口与工作方式

- 载波监听多址接入/碰撞检测（Carrier Sense Multiple Access with Collision Detection，CSMA/CD）用于共享信道上的半双工通信，具体过程见[[3.4.2 CSMA-CD协议|CSMA/CD 协议]]。
- 当交换机接口连接计算机或另一个交换机时，可以工作在[[2.3 传输方式#2.3.3 单工、半双工和全双工|全双工方式]]。
- 全双工接口之间可以在交换机内部同时连通多对接口，使相互通信的计算机像独占传输媒体一样传输数据，不发生碰撞，也不需要使用 CSMA/CD。
- 当交换机接口连接集线器时，该接口只能使用 CSMA/CD，并且只能工作在半双工方式。
- 现在的交换机和计算机中的网卡能够自动识别上述两种连接情况，并自动切换到相应的工作方式。
- 交换机通常具有多种速率的接口，例如 `10 Mb/s`、`100 Mb/s`、`1 Gb/s` 甚至 `10 Gb/s`；大部分接口支持多速率自适应。

![[3.5.1-交换机接口工作方式.png|900]]

- 图中连接计算机或交换机的接口在交换机内部可以同时建立多条独立通道；连接集线器的接口仍属于共享半双工网段。

### 3.5.1.2 自学习与转发

- 交换机自学习和转发帧的流程与透明网桥相同，透明网桥的基本规则见[[3.4.5 在数据链路层扩展以太网#3.4.5.2 透明网桥的自学习与转发|透明网桥自学习与转发]]。
- 协议数据单元（Protocol Data Unit，PDU）在以太网交换机处通常是 MAC 帧。
- 交换机在转发表中查找帧的目的 MAC 地址；MAC 地址也常称物理地址或硬件地址，但它属于数据链路层，不是物理层地址。
- 交换机收到一个无误码帧后，先把帧的源 MAC 地址和接收该帧的接口登记到转发表。

1. 目的 MAC 地址是广播地址时，从除入接口外的其他接口转发。
2. 目的 MAC 地址是单播地址且已在转发表中时，从对应接口明确转发；对应接口就是入接口时丢弃该帧。
3. 目的 MAC 地址尚未登记时，从除入接口外的所有接口盲目转发。
4. 转发表中的地址会随着各主机通信逐步学习到相应接口。

![[3.5.1-自学习与转发.png|900]]

- 练习中的通信顺序为 `A→B`、`H→A`、`E→X`、`X→E`，两个交换机按源地址学习并记录接口。

| 主机通信 | 交换机 1 的处理 | 交换机 2 的处理 |
| --- | --- | --- |
| `A→B` | 登记 A，盲目转发 | 登记 A，盲目转发 |
| `H→A` | 登记 H，明确转发 | 登记 H，明确转发 |
| `E→X` | 登记 E，盲目转发 | 登记 E，盲目转发 |
| `X→E` | 登记 X，因目的接口与入接口相同而丢弃 | 收不到该帧 |

- 目的地址未知时的盲目转发会帮助交换机学习后续通信所需的地址；已知目的地址时只向对应接口发送。

### 3.5.1.3 存储转发与直通交换

- 一般交换机采用“存储转发”方式，先接收完整帧，再进行转发处理。
- 为减小转发时延，部分交换机采用直通（Cut-Through）交换方式。
- ==直通交换机在接收帧的同时读取目的 MAC 地址，立即决定转发接口，再通过内部基于硬件的交叉矩阵转发，不必把整个帧先缓存完毕。==

![[3.5.1-存储转发与直通交换.png|900]]

| 对比维度 | 存储转发 | 直通交换 |
| --- | --- | --- |
| 开始转发 | 接收完整帧后 | 读出目的 MAC 地址后 |
| 内部处理 | 先缓存整个帧 | 通过硬件交叉矩阵直接转发 |
| 转发时延 | 较大 | 较小 |

![[3.5.1-直通交换最短时延.png|900]]

- 例题条件：输出端口无排队，采用直通交换，且不把物理层前导码计入帧。
- 直通交换引入的最短转发时延等于读取目的 MAC 地址所需的时间：
  - 公式：$T_{min}=L_{dst}/R$
  - `T_min`：最短转发时延，单位为 `s`。
  - `L_dst`：目的 MAC 地址长度，单位为 `bit`。
  - `R`：接口传输速率，单位为 `bit/s`。
  - 计算：$T_{min}=(6\times8)/(100\times10^6)=0.48\,\mu\mathrm{s}$。
  - 适用条件：输出端口无排队、采用直通交换、帧不含前导码。
  - 常见变形：$R=L_{dst}/T_{min}$。
