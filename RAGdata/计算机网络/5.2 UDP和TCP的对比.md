- UDP（User Datagram Protocol，用户数据报协议）和 TCP（Transmission Control Protocol，传输控制协议）都是 TCP/IP 体系结构的运输层协议。
- UDP 以无连接、较简单的用户数据报方式传输；TCP 以面向连接、可靠的报文段方式传输。
- 选择协议时，需要同时考虑连接方式、通信模式、应用数据处理、可靠性和首部开销。
![[5.2-TCP与UDP在体系结构中的位置.png]]
==UDP 的服务简单、支持单播/多播/广播但不保证可靠交付；TCP 只支持面向连接的点对点可靠传输，并承担更多控制开销。==

### 5.2.1 连接方式与通信模式

| 对比维度 | UDP | TCP |
| --- | --- | --- |
| 连接方式 | 无连接；传送数据前不需要建立连接 | 面向连接；传送数据前通过“三报文握手”建立 TCP 连接，数据传输结束后通过“四报文挥手”释放连接 |
| 支持的通信模式 | 支持单播、多播和广播 | 只支持一对一的点对点通信；每条 TCP 连接只能有两个端点 |
| 服务类型 | 向上层提供无连接的不可靠数据传输服务 | 向上层提供面向连接的可靠数据传输服务 |

![[5.2-连接方式对比.png|900]]

- 图中左侧 UDP 直接传送数据，右侧 TCP 通过“三报文握手”建立连接、基于连接传输数据，再通过“四报文挥手”释放连接。

![[5.2-通信方式支持.png]]

- 图中 UDP 可进行单播、多播和广播；TCP 通过连接建立点对点通信。

### 5.2.2 应用数据处理方式

| 对比维度 | UDP | TCP |
| --- | --- | --- |
| 面向对象 | 面向应用报文，应用报文作为用户数据报的数据部分 | 面向字节流，应用数据作为连续字节流交给 TCP |
| 发送处理 | 每个应用报文分别封装进 UDP 用户数据报 | TCP 可把应用字节流分段，也可把多个小块数据组合后再传输 |
| 接收交付 | 按用户数据报交付给接收进程 | 按字节流交付，接收进程读取的是连续字节序列 |

![[5.2-应用数据处理.png]]

- 图中 UDP 每个用户数据报都保留一个应用报文边界；TCP 把字节流分割为多个报文段，接收端再按字节流处理。

### 5.2.3 数据传输可靠性

| 对比维度 | UDP | TCP |
| --- | --- | --- |
| 网际层服务基础 | 在无连接、不可靠的网际层服务之上直接传送 | 同样使用网际层的无连接、不可靠服务 |
| 运输层保证 | 不确认、不重传；不恢复丢失的数据，对检出误码的用户数据报仅丢弃 | 在连接基础上提供可靠传输，使用确认、超时重传、流量控制和拥塞控制等机制 |
| 适用侧重点 | 适合 IP 电话、视频会议等实时应用对及时性的要求 | 适合需要可靠交付的应用 |

![[5.2-可靠传输支持.png]]

- 图中 UDP 一侧标出“丢失、误码，仅丢弃，其他什么也不做”，TCP 一侧标出面向连接的可靠传输服务；两者的网际层基础仍是不可靠服务。

### 5.2.4 首部开销与实现复杂度

| 对比维度 | UDP | TCP |
| --- | --- | --- |
| 首部字段 | 源端口、目的端口、长度、检验和，各占 `2 Byte` | 固定首部包含源端口、目的端口、序号、确认号、数据偏移、保留、控制位、窗口、检验和、紧急指针等字段 |
| 首部长度 | 固定 `8 Byte` | 固定首部最小 `20 Byte`，含选项时最大 `60 Byte` |
| 实现与资源 | 首部短、处理过程较简单 | 为可靠传输需要更多控制机制，实现较复杂并占用更多处理机资源 |

![[5.2-首部开销.png|900]]

- 图中 UDP 首部由四个 `2 Byte` 字段组成，共 `8 Byte`；TCP 报文段首部最小 `20 Byte`、最大 `60 Byte`。

- 共同点：二者都属于 TCP/IP 运输层，使用端口号识别应用进程，并把数据交给 IP 完成跨网络传送。
- 本质区别：==UDP 以简单的无连接用户数据报承载应用报文，TCP 以较大首部和连接控制机制提供可靠字节流。==
![[5.2-UDP与TCP特征对比.png]]