- 集线器使用大规模集成电路替代共享总线，并允许站点使用双绞线构成星型物理拓扑。
- 使用集线器的以太网在物理拓扑上是星型，在逻辑上仍是总线型。
- 集线器只工作在物理层，所有接口仍属于同一个碰撞域。

==集线器改变了布线方式，但没有改变共享信道的本质：所有站点共享带宽并继续使用 CSMA/CD。==

### 3.4.3.1 集线器的工作特点

- 集线器（Hub）使用更便宜、更灵活的[[2.2 物理层下面的传输媒体#2.2.3 双绞线|双绞线]]连接站点。
- 所有站点共享总线资源，仍使用 CSMA/CD。
- 集线器只转发比特，不检查帧，也不执行碰撞检测。
- 碰撞检测由各站点的网卡完成。
- 集线器具有少量容错能力和网络管理功能，可以断开不停发送帧的故障网卡。

![[3.4.3-集线器工作特点.png|900]]

- 集线器把一个接口收到的比特转发到其他接口，因此碰撞会传播到整个逻辑总线。

### 3.4.3.2 10BASE-T

- IEEE 在 1990 年制定 10BASE-T 星型以太网标准 802.3i。
- `10` 表示传输速率为 `10 Mb/s`。
- `BASE` 表示采用基带信号传输。
- `T` 表示使用双绞线作为传输媒体。
- 站点到集线器的距离不超过 `100 m`。
- 集线器之间需要更远距离连接时，可以使用 10BASE-F 光纤链路；`F` 表示[[2.2 物理层下面的传输媒体#2.2.4 光纤|光纤]]。

![[3.4.3-10BASE-T.png|900]]

- 10BASE-T 改变了共享式以太网的物理布线方式，但没有改变逻辑总线和共享碰撞域。
