- 网络层负责把分组从源主机经过一个或多个网络与路由器传送到目的主机。
- 网络层的核心功能可以分为路由选择和分组转发：前者确定路径，后者按转发表把分组送往正确输出接口。
- 网络层向运输层提供的服务可分为面向连接的虚电路服务和无连接的数据报服务。

==路由选择决定分组应走的路径，分组转发执行路由选择产生的转发表；二者共同完成跨网络交付。==

### 4.1.1 路由选择与分组转发

- 路由选择处理机运行路由选择协议，并根据收集到的网络状态生成路由表。
- 转发表由路由表导出，记录目的网络与输出接口或下一跳之间的映射。
- 路由器收到[[4.2 网际协议IP|网际协议（Internet Protocol，IP）]]数据报后，根据首部中的目的地址查找转发表，再从相应接口转发。
- 路由选择可以由管理员手工配置，也可以由路由选择协议自动完成。

![[4.1-路由选择与分组转发.png|900]]

- 图中路由选择协议负责形成路由信息，转发表负责在收到分组时作出逐跳转发决定。

### 4.1.2 网络层提供的两种服务

==虚电路先建立、再传送、后释放，传输阶段按短虚电路标识符沿既定逻辑路径转发；
数据报无需建立或释放连接，每个分组携带完整目的地址并独立选路。==

#### 面向连接的虚电路服务

- 虚电路（Virtual Circuit，VC）是分组交换网络中的逻辑连接，分组仍然逐跳存储转发，并没有建立端到端的物理线路。
- 它把可靠通信的主要责任放在网络内部，通信双方必须先建立网络层连接才能发送数据。
- 参与者包括源主机、目的主机和虚电路沿途的路由器或分组交换节点。

1. 源主机发送连接建立请求，请求中通常包含完整的源、目的网络地址以及所需服务参数。
2. 沿途节点选择下一跳，为当前链路分配短虚电路标识符（Virtual Circuit Identifier，VCI），并建立“输入接口、输入 VCI”到“输出接口、输出 VCI”的转发表项。
3. 若服务模型承诺资源保障，沿途节点还要执行接纳控制，并为该虚电路预留带宽、缓存等资源；任一节点拒绝请求时，已经建立的临时状态必须回退。
4. 目的主机接受请求后返回连接确认，确认到达源主机后，虚电路进入可传送数据的状态。
5. 数据传输期间，分组首部携带本段链路使用的 VCI，而不必重复携带完整的源、目的地址；每个节点查询虚电路转发表，并把输入 VCI 替换为下一段链路使用的输出 VCI。
6. 通信结束后，任一端发出连接释放请求；沿途节点删除虚电路状态并释放所占资源，完成正常释放。节点或链路故障时，相关虚电路失效，通常需要清除旧状态并重新建立连接。

- 同一虚电路的分组沿既定路径转发，通常可按发送顺序到达；但“建立虚电路”本身不等于“可靠传输”，是否承诺按序、无丢失和无差错仍取决于具体网络协议。
- 可靠的虚电路协议可组合差错检测、分组序号、确认、超时重传和重复抑制，使分组无差错、按序、不丢失且不重复地到达。
- [[3.2.2 差错检测#3.2.2.1 比特差错与误码率|差错检测]]只能发现传输错误；确认、重传和序号控制等[[3.2.3 可靠传输#3.2.3.1 可靠传输的基本概念|可靠传输机制]]才负责恢复错误。

![[4.1-虚电路服务.png|900]]

- 图中所有分组沿红色逻辑路径逐跳转发；虚电路不是像电路交换那样建立一条真正的物理连接。

#### 无连接的数据报服务

- 数据报服务把网络核心设计得相对简单，只提供无连接、尽最大努力的分组交付，把端到端可靠性的主要责任交给用户主机及其运输层。
- 源主机无需建立网络层连接，也无需等待资源预留完成，可以直接发送数据报。

1. 源主机把待发送数据封装成相互独立的数据报，每个数据报都必须携带完整的目的网络地址。
2. 对于实际使用的 IP 数据报，首部还同时携带完整的源 IP 地址；路由器逐跳转发所必需的是目的地址，源地址用于标识发送端并支持返回报文和差错报告。
3. 每台路由器收到一个数据报后，都独立依据其目的地址查询转发表并选择下一跳，不建立或查询虚电路状态。
4. 后续数据报会根据当时的路由和网络状态重新作出转发决定，因此同一通信的数据报可能经过不同路径。
5. 目的主机把实际收到的数据报交给上层；网络层没有连接状态需要释放。

- 不同路径具有不同的排队和传播时延，因此数据报不保证按发送顺序到达，也可能丢失、重复或带有差错。
- 某台路由器或链路发生故障时，后续数据报可以随路由更新改走其他路径；已经丢失的数据报不会由无连接网络层自动恢复。
- 即使某段数据链路提供检错与重传，它也只能保护这一段链路，不能自动形成端到端可靠交付。
- IPv4 使用[[4.2.7 IPv4数据报的首部格式#4.2.7.4 首部检验和|首部检验和]]检测首部差错，发现错误后丢弃数据报；[[4.5 网际控制报文协议ICMP#4.5.2 差错报告报文|ICMP 差错报告]]只能反馈部分交付问题，不能确认、重传或恢复原数据报。
- 需要端到端可靠性时，由上层协议实现确认、重传、排序和去重，例如[[5.3.6 TCP可靠传输的实现|TCP 可靠传输]]。

![[4.1-数据报服务.png|900]]

- 图中各分组可以经过不同路径，因而每个分组首部都必须携带目的主机的完整地址。

#### 两种服务的对比

| 对比维度 | 面向连接的虚电路服务 | 无连接的数据报服务 |
| --- | --- | --- |
| 核心思想 | 由网络承担主要可靠性责任 | 由端系统承担端到端可靠性责任 |
| 通信阶段 | 先建立连接，再传送数据，最后释放连接 | 直接发送数据报，无建立和释放阶段 |
| 传输阶段首部 | 携带较短的 VCI；完整地址主要用于连接建立 | 每个数据报携带完整目的地址；IP 还携带源地址 |
| 路径选择 | 建立连接时确定路径，同一虚电路的分组沿既定路径传送 | 每个数据报独立选路，同一通信的分组可走不同路径 |
| 网络内部状态 | 沿途节点维护虚电路转发表项，必要时预留资源 | 路由器不维护端到端连接状态，不为某次通信预留固定资源 |
| 到达顺序 | 通常可按序到达；是否保证可靠交付取决于协议 | 不保证按序到达，也不保证不丢失、不重复和无差错 |
| 故障影响 | 路径上的节点或链路故障会使虚电路失效，通常需要重建 | 后续数据报可在路由更新后改走其他路径 |
| 转发开销 | 建立和释放连接有额外开销，传输阶段查短 VCI | 没有连接控制开销，但每个数据报都要携带地址并独立查表 |

![[4.1-网络层两种服务.png|900]]

- TCP/IP 体系结构的网际层采用无连接的数据报服务，将复杂的可靠性处理放在网络边缘，使网络核心保持简单和灵活。
