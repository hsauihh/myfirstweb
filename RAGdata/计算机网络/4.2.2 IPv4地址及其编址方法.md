- 网际协议第 4 版（Internet Protocol version 4，IPv4）地址是分配给因特网上主机或路由器接口的 `32 bit` 标识符。
- IPv4 地址由网络前缀和主机号组成，网络前缀标识所在网络，主机号标识该网络中的接口。
- IPv4 编址先后采用分类编址、划分子网和无分类编址三种方法。

==判断一个 IPv4 地址所属网络的通用方法是把地址与子网掩码逐位相与；结果就是该地址的网络地址。==

### 4.2.2.1 IPv4 地址表示方法

- IPv4 地址通常写成点分十进制形式，把 `32 bit` 地址分成四个 `8 bit` 字节，并把每个字节转换为 `0~255` 的十进制数。
- 一个字节的二进制值为：$N=\sum_{i=0}^{7}b_i2^i$，其中 $b_i\in\{0,1\}$。
- 十进制数转换为二进制数时，可以连续除以 `2` 并逆序排列余数，也可以按 `128、64、32、16、8、4、2、1` 的位权分解。
- IPv4 地址由因特网名称和数字地址分配机构（Internet Corporation for Assigned Names and Numbers，ICANN）统一协调分配，各地区由相应的地址注册机构负责具体分配。

![[4.2.2-二进制与十进制转换.png|900]]

- 图中展示了一个字节按位权展开以及二进制数与十进制数之间的转换方法。

### 4.2.2.2 分类编址

- 分类编址使用固定长度的网络号，并由地址最高位的类别标志区分地址类别。

| 类别 | 最高位 | 网络号长度 | 主机号长度 | 第一个字节范围 | 每个网络可分配地址数 |
| --- | --- | --- | --- | --- | --- |
| A 类 | `0` | `8 bit` | `24 bit` | `1~126` | $2^{24}-2=16777214$ |
| B 类 | `10` | `16 bit` | `16 bit` | `128~191` | $2^{16}-2=65534$ |
| C 类 | `110` | `24 bit` | `8 bit` | `192~223` | $2^8-2=254$ |
| D 类 | `1110` | 不划分 | 不划分 | `224~239` | 用作多播地址 |
| E 类 | `1111` | 不划分 | 不划分 | `240~255` | 保留 |
![[4.2.2-分类编址结构.png]]
- 网络号为 `0` 的 A 类网络不用作普通网络号，网络号 `127` 用于本地软件环回测试。
- 主机号全为 `0` 的地址是网络地址，主机号全为 `1` 的地址是广播地址，二者都不能分配给普通主机接口。
![[4.2.2-特殊IPv4地址.png]]
![[4.2.2-分类地址范围.png]] 

- 分类编址以固定边界划分网络号和主机号，容易造成地址块规模与实际需求不匹配。

### 4.2.2.3 划分子网

- 划分子网从原主机号中借用若干位作为子网号，使一个分类网络能够划分成多个较小网络。
- 子网掩码中与网络号和子网号对应的位为连续的 `1`，与主机号对应的位为 `0`。
- 网络地址计算公式为：$网络地址=IPv4地址\mathbin{\&}子网掩码$。
- 若原主机号有 $h$ 位并借用 $k$ 位作为子网号：
  - 可划分的子网数为 $2^k$。
  - 每个子网可分配的主机地址数为 $2^{h-k}-2$。
  - 公式中的 `-2` 用于排除子网地址和广播地址。

![[4.2.2-子网掩码结构.png|900]]

- 子网掩码只改变网络前缀和主机号的分界位置，不改变 IPv4 地址的总长度。

![[4.2.2-划分子网地址范围.png|900]]

- 同一子网内最小地址是子网地址，最大地址是广播地址，中间地址才可分配给主机接口。

### 4.2.2.4 无分类编址 CIDR

- 无分类域间路由选择（Classless Inter-Domain Routing，CIDR）使用任意长度的网络前缀，不再使用 A、B、C 类地址的固定边界。
- CIDR 使用斜线记法 `IPv4地址/前缀长度`，例如 `128.14.35.7/20` 表示前 `20 bit` 是网络前缀。
- 前缀长度为 $n$ 的 CIDR 地址块：
  - 地址总数为 $2^{32-n}$。
  - 最小地址的主机号全为 `0`，它是网络地址。
  - 最大地址的主机号全为 `1`，它是广播地址。
  - 普通网络可分配的主机地址数为 $2^{32-n}-2$。

![[4.2.2-CIDR地址块.png|900]]

- 图中 `128.14.35.7/20` 所在地址块由前 `20 bit` 共同决定，==可从网络前缀推得最小地址、最大地址、地址数量和聚合某类网络的数量==。

- 路由聚合把多个连续、具有相同前缀的较小地址块合并成一个较短前缀的地址块。
- 聚合路由可以减少路由表项目并降低路由信息交换量，但聚合后的地址块必须覆盖连续且前缀一致的原地址块。

![[4.2.2-路由聚合.png|900]]

- 图中多条具体网络路由可以聚合为共同前缀 `172.1.4.0/22`，从而只向外通告一条路由。

### 4.2.2.5 三种编址方法的关系

| 对比维度 | 分类编址 | 划分子网 | CIDR |
| --- | --- | --- | --- |
| 网络前缀长度 | 由地址类别固定 | 在分类网络号后借用主机位 | 可按需要取任意长度 |
| 地址结构 | 网络号、主机号 | 网络号、子网号、主机号 | 网络前缀、主机号 |
| 表示网络范围 | 地址类别 | 子网掩码 | 斜线记法与掩码 |
| 地址利用率 | 较低 | 高于分类编址 | 可按地址块需求灵活分配 |
| 路由聚合 | 不灵活 | 仍受分类边界限制 | 支持按共同前缀聚合 |

![[4.2.2-IPv4编址方法演进.png|900]]

- 分类编址和划分子网保留了地址类别概念，CIDR 则用网络前缀彻底取代类别边界。
