- [[4.2.2 IPv4地址及其编址方法|网际协议第 4 版（IPv4）地址]]由网络层使用，用于标识数据报的源接口和目的接口以及进行跨网络路由选择。
- [[3.4.1 网络适配器和MAC地址#3.4.1.2 MAC 地址的结构|媒体接入控制（Media Access Control，MAC）地址]]由数据链路层使用，用于在一段具体链路上把帧交付给下一跳接口。
- IP 数据报经过多个网络时，端到端源、目的 IPv4 地址保持不变，各段链路上的源、目的 MAC 地址逐跳改变。

==IPv4 地址回答“数据报最终从哪里到哪里”，MAC 地址回答“这一跳的帧从哪个接口交给哪个接口”。==

### 4.2.4.1 地址的封装位置

| 地址 | 所在首部 | 作用范围 | 主要用途 |
| --- | --- | --- | --- |
| IPv4 地址 | IP 数据报首部 | 端到端 | 标识源接口、目的接口并供路由器选择路径 |
| MAC 地址 | 链路层帧首部 | 一段链路 | 标识当前链路上的发送接口和接收接口 |

- 发送方先把运输层报文段封装成 IP 数据报，再把 IP 数据报封装成适合当前网络的链路层帧。
- 路由器收到帧后去除链路层首部和尾部，处理 IP 数据报，再为下一段链路重新封装帧。

### 4.2.4.2 逐跳传输中的地址变化

- 源主机发往异地网络中的目的主机时，IP 数据报首部始终记录两台端系统接口的 IPv4 地址。
- 每经过一台路由器，输出帧的源 MAC 地址变为路由器输出接口的 MAC 地址，目的 MAC 地址变为下一跳接口的 MAC 地址。
- 路由器需要通过[[4.2.5 地址解析协议ARP|地址解析协议 ARP]]获得同一链路上下一跳 IPv4 地址对应的 MAC 地址。

![[4.2.4-IP地址与MAC地址变化.png|900]]

- 图中 IP 数据报在端到端传输期间保持源、目的 IPv4 地址不变，外层帧则在每段链路上重新封装。

![[4.2.4-下一跳MAC地址解析.png]]

- 每段链路只需要使用当前发送接口和下一跳接收接口的 MAC 地址，不会把端到端路径上的所有 MAC 地址同时写入一个帧。

### 4.2.4.3 两种地址不可相互替代

- 仅使用 MAC 地址时，路由器难以按网络前缀聚合路由，路由表需要记录大量接口地址，需要具备极大存储空间，带来非常大的时延。
- 仅使用 IPv4 地址时，每个链路层接口都需要人工配置地址映射，且无法直接使用现有局域网的硬件寻址机制。
- 分层使用 IPv4 地址和 MAC 地址，使网络层能够按网络前缀选择路径，数据链路层能够在当前链路完成实际交付。
