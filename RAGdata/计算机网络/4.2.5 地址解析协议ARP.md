- 地址解析协议（Address Resolution Protocol，ARP）用于根据同一链路上接口的[[4.2.2 IPv4地址及其编址方法|网际协议第 4 版（IPv4）地址]]获得其[[3.4.1 网络适配器和MAC地址#3.4.1.2 MAC 地址的结构|媒体接入控制（MAC）地址]]。
- 主机和路由器把已经解析出的 IPv4 地址与 MAC 地址映射保存在 ARP 高速缓存中，以减少重复广播。
- ARP 只能在当前广播域中直接解析地址，==不能跨越路由器工作==。

==发送跨网络数据报时，源主机解析的是默认网关的 MAC 地址，而不是远端目的主机的 MAC 地址。==

### 4.2.5.1 ARP 地址解析过程

- 参与者：请求方接口、同一广播域中的其他接口和目标接口。
- 前置条件：请求方已知目标或下一跳的 IPv4 地址，但 ARP 高速缓存中没有有效映射。

1. 请求方发送 ARP 请求，帧的目的 MAC 地址为==广播地址== `FF-FF-FF-FF-FF-FF`。
2. 请求报文携带请求方的 IPv4 地址、MAC 地址以及待解析的目标 IPv4 地址。
3. 广播域中的所有接口都能收到请求，只有目标 IPv4 地址匹配的接口生成 ARP 响应。
4. 目标接口把自己的 MAC 地址放入 ARP 响应，并向请求方==单播==发送。
5. 请求方把映射写入 ARP 高速缓存，再使用该 MAC 地址封装待发送的帧。

![[4.2.5-ARP请求与响应.png|900]]

- **ARP 请求使用广播寻找目标接口，ARP 响应使用单播把目标 MAC 地址返回给请求方。**

### 4.2.5.2 ARP 高速缓存

| 表项类型 | 形成方式 | 生命周期 |
| --- | --- | --- |
| 动态表项 | 通过 ARP 请求和响应自动获得 | 到期后自动删除 |
| 静态表项 | 管理员手工配置 | 不随动态老化自动删除 |

- 接口可以从收到的 ARP 报文中学习发送方的 IPv4 地址与 MAC 地址映射。
- **动态表项设置有效期是因为接口地址或网络拓扑可能发生变化。**

![[4.2.5-ARP高速缓存.png|900]]

- 图中的缓存把 IPv4 地址映射到 MAC 地址，后续发送可直接查表而不必再次广播请求。

### 4.2.5.3 ARP 的作用范围

- 同一网络中的两台主机通信时，发送方直接解析目的主机的 MAC 地址。
- 目的主机位于其他网络时，发送方先解析默认网关接口的 MAC 地址。
- 路由器转发数据报时，在每个输出网络分别解析下一跳接口的 MAC 地址。
- ARP 请求不会被路由器转发，因此一次 ARP 解析只解决当前链路的下一跳交付问题。
