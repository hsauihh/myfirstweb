- 信道复用是在一条[[2.2 物理层下面的传输媒体|传输媒体]]上同时或交替传输多个用户的信号。
- 当传输媒体的容量大于多个用户所需容量的总和时，可以划分共享信道以提高传输媒体利用率。
- 常用方法包括频分复用、时分复用、波分复用和码分复用。

==四种复用方法分别用频率、时间、光波波长和正交码片序列区分用户。==

### 2.6.1 信道复用的基本原理

- 参与者：多个发送用户、复用器、大容量共享信道、分用器和多个接收用户。
- 前置条件：一条传输媒体的传输容量大于多个用户所需容量的总和。

1. 多个用户分别向复用器发送信号。
2. 复用器按照选定的复用规则把多路信号合成为一路信号。
3. 合成信号通过大容量共享信道传输。
4. 分用器按照相同规则分离各路信号，并交付对应用户。

- 流程结果：多个用户共享同一传输媒体，传输媒体的利用率提高。
- 复用器和分用器必须采用一致的信道划分与恢复规则。

![[2.6-信道复用基本原理.png|900]]

- 复用器在发送端合并多个用户的信号，分用器在接收端恢复各用户信号。
- ==信道复用共享的是传输媒体，逻辑上仍为每个用户保留可区分的子信道。==

### 2.6.2 频分复用

- 频分复用（Frequency Division Multiplexing，FDM）把传输媒体的频率带宽划分为多个互不重叠的子频带。
- 每个用户在通信期间始终占用一个固定子频带。
- 相邻子频带之间设置隔离频带，以减小相互干扰。
- 所有用户在相同时间使用不同的频率资源。

![[2.6-频分复用.png|900]]

- 图中的三个用户分别占用子频带 1、子频带 2 和子频带 3。
- ==频分复用按频率分隔用户，各用户可以同时连续传输。==

### 2.6.3 时分复用

- 时分复用（Time Division Multiplexing，TDM）把传输时间划分为长度相等的时隙。
- 每个用户周期性地占用固定时隙。
- 一个 TDM 帧是各用户完成一轮时隙使用所对应的一段时间。
- 所有用户在不同时间使用相同的频率带宽。

![[2.6-时分复用.png|900]]

- 用户 A、B、C 的时隙按固定次序周期性出现，每一轮构成一个 TDM 帧。
- ==时分复用按时间分隔用户，同一时刻只有对应时隙的用户使用共享信道。==

### 2.6.4 波分复用

- 波分复用（Wavelength Division Multiplexing，WDM）是在一根光纤中同时传输多个波长相近的光载波信号。
- WDM 是应用于光纤的频分复用技术。
- 发送端通过合波器把多个波长合入同一根光纤，接收端通过分波器恢复各波长信号。
- 密集波分复用（Dense Wavelength Division Multiplexing，DWDM）可以在一根光纤上使用几十个或更多波长信道。

![[2.6-波分复用.png|900]]

- 三棱镜示意多个波长在发送端合成，并在接收端重新分离。
- ==WDM 的各路信号同时传播，区分它们的资源是光载波波长。==

- 容量计算示例：一根光缆包含 $100$ 根光纤，每根光纤使用 $40$ 个波长信道，每个信道速率为 $2.5\,\mathrm{Gb/s}$：
  - $R=100\times40\times2.5\,\mathrm{Gb/s}=10\,\mathrm{Tb/s}$
  - $R$：整根光缆的总传输速率，单位为 `b/s`。

### 2.6.5 码分复用

- 码分复用（Code Division Multiplexing，CDM）常称为码分多址（Code Division Multiple Access，CDMA）。
- CDMA 允许各用户在相同时间使用相同频率进行通信。
- CDMA 最初用于军事通信，具有较强的抗干扰能力，并且频谱类似白噪声而不易被发现。
- CDMA 后来也用于民用移动通信。

#### 码片序列

- CDMA 把每个比特时间划分为 $m$ 个更短的时间片，称为码片（Chip）。
- $m$ 的常见取值为 $64$ 或 $128$，示例取 $m=8$。
- 每个站点被分配一个唯一的 $m$ 比特码片序列。
- 计算时把二进制 `0` 写为 $-1$，把二进制 `1` 写为 $+1$。
- 站点发送比特 `1` 时发送自己的码片序列，发送比特 `0` 时发送该码片序列的反码。
- 多个站点同时发送时，信道中的信号是各站点发送码片向量的叠加。

#### 正交性质

- 不同站点的码片向量必须相互正交：
  - $\boldsymbol{A}\cdot\boldsymbol{B}=\dfrac{1}{m}\sum_{i=1}^{m}A_iB_i=0$
- 码片向量与自身的规格化内积为：
  - $\boldsymbol{A}\cdot\boldsymbol{A}=1$
- 码片向量与自身反码的规格化内积为：
  - $\boldsymbol{A}\cdot(-\boldsymbol{A})=-1$
- 变量：
  - $m$：每个比特划分的码片数，无单位。
  - $A_i$、$B_i$：站点 A、B 的第 $i$ 个码片值，取值为 $+1$ 或 $-1$。
  - $\boldsymbol{A}\cdot\boldsymbol{B}$：两个码片向量的规格化内积，无单位。

![[2.6-码片序列正交性质.png|900]]

- 正交码片序列的规格化内积为 $0$，因此其他站点的信号不会影响目标站点的相关运算结果。
- 一个站点的码片向量与自身或自身反码的内积分别为 $1$ 和 $-1$。

#### 信号分离

- 参与者：发送站点、叠加信号和已知各站点码片序列的接收端。

1. 多个站点按照比特值发送自己的码片序列或反码。
2. 信道把同时到达的码片向量按分量相加，形成叠加信号 $\boldsymbol{R}$。
3. 接收端用 $\boldsymbol{R}$ 分别与各站点的码片向量做规格化内积。
4. 内积为 $+1$ 表示该站点发送比特 `1`，为 $-1$ 表示发送比特 `0`，为 $0$ 表示该站点没有发送数据。

![[2.6-CDMA信号分离.png|900]]

- 叠加信号分别与 A、B、C 的码片向量相关后，结果为 $+1$、$-1$、$0$。
- ==接收端依靠正交码片序列从同一时间、同一频带中的叠加信号恢复各用户数据。==

### 2.6.6 四种信道复用技术的比较

| 对比维度 | FDM | TDM | WDM | CDM/CDMA |
| --- | --- | --- | --- | --- |
| 区分用户的资源 | 频率 | 时间 | 光波波长 | 正交码片序列 |
| 用户使用信道的方式 | 同时使用不同子频带 | 轮流使用固定时隙 | 同时使用不同波长 | 同时使用相同时间和频带 |
| 关键条件 | 子频带互不重叠并设置隔离频带 | 收发双方保持时隙与帧同步 | 合波器和分波器按波长合并与分离 | 各用户码片序列相互正交 |
| 适用对象 | 一般模拟或带通信道 | 按时隙发送的数字信号 | 光纤通信 | 多用户扩频通信 |

- FDM 和 WDM 都让各路信号同时传输，WDM 是光纤中的 FDM。
- TDM 让各用户在不同时间使用相同频带。
- CDM 允许各用户同时占用相同时间和频带，接收端使用码片相关运算区分用户。
