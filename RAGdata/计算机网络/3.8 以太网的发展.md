- 以太网从 `10 Mb/s` 传统以太网发展到百兆、千兆、万兆以及 40/100 吉比特以太网，传输速率和覆盖场景不断扩展。
- 各代高速以太网继续使用电气与电子工程师协会（Institute of Electrical and Electronics Engineers，IEEE）802.3 的媒体接入控制（Media Access Control，MAC）帧格式，高速版本主要扩展物理层标准并调整双工与媒体接入方式。
- 百兆和千兆以太网可保留半双工方式并使用载波监听多址接入/碰撞检测（Carrier Sense Multiple Access with Collision Detection，CSMA/CD），万兆及更高速率的以太网只使用全双工方式。

==以太网在提高传输速率和扩展传输距离的同时保持 MAC 帧格式兼容；全双工链路没有碰撞，不使用 CSMA/CD。==

### 3.8.1 速率演进

- 以太网的典型速率依次为：
  1. 传统以太网：`10 Mb/s`。
  2. 100BASE-T 快速以太网：`100 Mb/s`。
  3. 吉比特以太网：`1 Gb/s`。
  4. 10 吉比特以太网：`10 Gb/s`。
  5. 40/100 吉比特以太网：`40 Gb/s` 或 `100 Gb/s`。
- `100 Mb/s` 及以上的版本统称为高速以太网。

![[3.8-以太网速率演进.png|900]]

- 高速以太网沿用传统以太网的技术体系，并通过新的物理层标准逐级提高传输速率。

### 3.8.2 100BASE-T 快速以太网

- 100BASE-T 又称快速以太网（Fast Ethernet），在双绞线上以 `100 Mb/s` 传输基带信号。
- 1995 年，IEEE 802.3u 成为 100BASE-T 的正式标准，它是对 IEEE 802.3 的补充。
- 100BASE-T 继续使用 IEEE 802.3 帧格式和[[3.4.2 CSMA-CD协议|CSMA/CD 协议]]，并保持 `64 B` 最小帧长。
- 为在速率提高到 `100 Mb/s` 后保持最小帧长不变，其参数调整为：
  - 网段最大电缆长度由 `1000 m` 缩短到 `100 m`。
  - 争用期缩短到 $5.12\,\mu\mathrm{s}$。
  - 帧间最小间隔缩短到 $0.96\,\mu\mathrm{s}$。
- 使用以太网交换机时，100BASE-T 可以在全双工方式下无碰撞工作，因此不使用 CSMA/CD。

![[3.8-百兆以太网参数.png|900]]

- 半双工 100BASE-T 通过缩短网段长度和争用期维持碰撞检测；交换式全双工 100BASE-T 则不受碰撞检测约束。

| 物理层标准 | 传输介质 | 网段最大长度 | 说明 |
| --- | --- | --- | --- |
| 100BASE-TX | 铜缆 | `100 m` | 两对 5 类非屏蔽双绞线（Unshielded Twisted Pair，UTP）或屏蔽双绞线（Shielded Twisted Pair，STP） |
| 100BASE-T4 | 铜缆 | `100 m` | 四对 3 类或 5 类 UTP |
| 100BASE-FX | 光缆 | `2000 m` | 使用两根光纤，发送与接收各用一根 |

![[3.8-百兆以太网物理层标准.png|900]]

- 百兆以太网可按距离与布线条件选择双绞线或光纤物理层标准。

### 3.8.3 吉比特以太网

- 吉比特以太网又称千兆以太网（Gigabit Ethernet）。
- 1998 年，IEEE 802.3z 成为吉比特以太网的正式标准。
- 吉比特以太网的速率为 `1000 Mb/s`，即 `1 Gb/s`，并继续使用 IEEE 802.3 帧格式。
- 吉比特以太网支持两种工作方式：
  - 半双工方式使用 CSMA/CD。
  - 全双工方式不使用 CSMA/CD。

![[3.8-千兆以太网特点.png|900]]

- 千兆以太网在保持帧格式兼容的同时支持半双工和全双工，但只有半双工链路需要处理碰撞。

#### 载波延伸

- 半双工千兆以太网使用载波延伸（Carrier Extension），把争用期增大为发送 `512 B` 所需的时间，同时保持 MAC 最小帧长仍为 `64 B`。
- 待发送 MAC 帧不足 `512 B` 时，在帧尾之后添加特殊字符，使整个发送过程延伸到 `512 B`。
- 对 `64 B` 最短帧，需要添加 `448 B` 载波延伸字符。
- 载波延伸字符不属于 MAC 帧，不改变接收方交付的数据帧内容。

![[3.8-载波延伸.png|900]]

- 图中的 `64 B` 最短 MAC 帧与 `448 B` 载波延伸字符共同占用 `512 B` 的发送时间，从而扩大碰撞检测范围。

#### 分组突发

- 连续发送多个短帧时，吉比特以太网可使用分组突发（Packet Bursting）提高效率。
- 只有第一个短帧使用载波延伸，后续短帧不再填充特殊字符，帧之间只保留必要的最小间隔。
- 一串分组累计发送到约 `1500 B` 或稍多时停止突发。

![[3.8-分组突发.png|900]]

- 分组突发把第一个短帧延伸到 `512 B`，随后连续发送短帧，减少每个短帧都执行载波延伸造成的开销。

| 物理层标准 | 传输介质 | 网段最大长度 | 说明 |
| --- | --- | --- | --- |
| 1000BASE-SX | 光缆 | `550 m` | 多模光纤，纤芯直径为 `50 μm` 或 `62.5 μm` |
| 1000BASE-LX | 光缆 | `5000 m` 或 `550 m` | 使用 `10 μm` 单模光纤时为 `5000 m`，使用 `50 μm` 或 `62.5 μm` 多模光纤时为 `550 m` |
| 1000BASE-CX | 铜缆 | `25 m` | 使用两对 STP 电缆 |
| 1000BASE-T | 铜缆 | `100 m` | 使用四对 5 类 UTP |

![[3.8-千兆以太网物理层标准.png|900]]

- 千兆以太网通过短波或长波光纤、短距离屏蔽铜缆以及 5 类双绞线覆盖不同传输距离。

### 3.8.4 10 吉比特以太网

- 2002 年 6 月，IEEE 802.3ae 通过 10 吉比特以太网（10 Gigabit Ethernet，10GE）正式标准。
- 10GE 又称万兆以太网，速率为 `10 Gb/s`。
- 10GE 把以太网的应用范围从校园网或企业网扩展到城域网与广域网。
- 10GE 继续使用 IEEE 802.3 帧格式，并保留最小帧长和最大帧长的规定，以便与较低速率以太网通信。
- 10GE 只工作在全双工方式，不存在争用媒体的问题，因此不使用 CSMA/CD，传输距离也不再受碰撞检测限制。

![[3.8-万兆以太网特点.png|900]]

- 万兆以太网以全双工链路和新增的长距离物理层标准支持城域网、广域网主干传输。

| 物理层标准 | 传输介质 | 网段最大长度 | 说明 |
| --- | --- | --- | --- |
| 10GBASE-SR | 光缆 | `300 m` | 波长为 `850 nm` 的多模光纤 |
| 10GBASE-LR | 光缆 | `10 km` | 波长为 `1300 nm` 的单模光纤 |
| 10GBASE-ER | 光缆 | `40 km` | 波长为 `1500 nm` 的单模光纤 |
| 10GBASE-CX4 | 铜缆 | `15 m` | 使用四对双芯同轴电缆（twinax） |
| 10GBASE-T | 铜缆 | `100 m` | 使用四对 6A 类 UTP 双绞线 |

![[3.8-万兆以太网物理层标准.png|900]]

- 10GE 的多模、单模光纤标准覆盖 `300 m~40 km`，铜缆标准用于较短距离连接。

### 3.8.5 40/100 吉比特以太网

- 2010 年，IEEE 发布 40/100 吉比特以太网（40/100 Gigabit Ethernet，40GE/100GE）的 IEEE 802.3ba 标准。
- IEEE 802.3ba 定义两种速率以满足不同需求：
  - `40 Gb/s` 主要用于计算应用。
  - `100 Gb/s` 主要用于汇聚应用。
- 40GE/100GE 只工作在全双工方式，不使用 CSMA/CD。
- 40GE/100GE 继续使用 IEEE 802.3 帧格式，并遵守最小帧长和最大帧长的规定。

![[3.8-40G和100G以太网特点.png|900]]

- 40GE 面向高速计算连接，100GE 面向流量汇聚，二者均以全双工方式覆盖局域网、城域网和广域网需求。

| 传输介质与距离 | 40GE 物理层标准 | 100GE 物理层标准 |
| --- | --- | --- |
| 背板，至少 `1 m` | 40GBASE-KR4 | — |
| 铜缆，至少 `7 m` | 40GBASE-CR4 | 100GBASE-CR10 |
| 多模光纤，至少 `100 m` | 40GBASE-SR4 | 100GBASE-SR10 |
| 单模光纤，至少 `10 km` | 40GBASE-LR4 | 100GBASE-LR4 |
| 单模光纤，至少 `40 km` | — | 100GBASE-ER4 |

![[3.8-40G和100G物理层标准.png|900]]

- IEEE 802.3ba 分别为背板、铜缆、多模光纤和单模光纤规定物理层标准，覆盖设备内部连接到 `40 km` 单模光纤链路。
