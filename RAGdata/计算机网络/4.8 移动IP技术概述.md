- 移动 IP（Mobile IP）是因特网工程任务组（Internet Engineering Task Force，IETF）制定的移动通信技术，相关规范为 RFC 3344。
- 移动 IP 使移动主机在不同网络之间漫游时仍能保留原来的 IP 地址，并继续已有的 IP 通信。
- 归属代理和外地代理通过地址注册、代理应答与 IP 隧道，把发往永久地址的数据报转交到移动主机当前所在的网络。

==移动主机的位置可以改变，但永久地址保持不变；归属代理依据“永久地址到转交地址”的注册信息完成重定向。==

### 4.8.1 移动 IP 的基本概念

| 概念 | 定义或作用 |
| --- | --- |
| 归属网络（Home Network） | 移动主机默认连接或最初申请接入的网络 |
| 永久地址（Permanent Address）或归属地址（Home Address） | 移动主机在归属网络中的 IP 地址，在整个移动通信过程中保持不变 |
| 归属代理（Home Agent） | 在归属网络中代表移动主机执行移动管理功能的实体，通常是连接归属网络的路由器 |
| 外地网络（Foreign Network）或被访网络（Visited Network） | 移动主机当前漫游所在的网络 |
| 外地代理（Foreign Agent） | 在外地网络中帮助移动主机执行移动管理功能的实体，通常是连接外地网络的路由器 |
| 转交地址（Care-of Address） | 外地代理提供给移动主机临时使用、属于外地网络的地址 |

![[4.8-移动IP基本概念.png|900]]

- 归属代理和外地代理虽然通常由路由器承担，但其代理功能在网络层完成。
- 多台移动主机使用同一个外地代理时，可以共享该外地代理的转交地址。

### 4.8.2 代理发现与注册

- 参与者：移动主机、外地代理和归属代理。
- 触发条件：移动主机离开归属网络并接入外地网络。

1. 移动主机使用代理发现协议与外地代理建立联系。
2. 移动主机从外地代理取得转交地址，并向外地代理登记自己的永久地址和归属代理地址。
3. 外地代理向归属代理注册移动主机的转交地址；移动主机也可以直接向归属代理注册。
4. 归属代理在注册表中保存移动主机永久地址与转交地址的对应关系。
5. 移动主机不在归属网络时，归属代理使用 [[4.2.5 地址解析协议ARP|地址解析协议（Address Resolution Protocol，ARP）]]代理，用自己的 [[4.2.4 IPv4地址与MAC地址|媒体接入控制（Media Access Control，MAC）地址]]应答针对移动主机的 ARP 请求。
6. 归属代理主动发送 ARP 广播，使归属网络中的主机和路由器尽快把发往移动主机的帧交给归属代理。

![[4.8-代理发现与注册.png|900]]

- 图中移动主机在外地网络完成注册后，归属代理负责截获发往永久地址的数据报，外地代理负责接收隧道数据报并完成最终交付。

### 4.8.3 固定主机向移动主机发送数据报

1. 固定主机把原 IP 数据报的目的地址设置为移动主机的永久地址，并按正常流程发送。
2. 数据报到达归属网络后，由归属代理截获。
3. 归属代理把原 IP 数据报封装在新的 IP 数据报中，新首部的目的地址为转交地址。
4. 封装后的数据报通过 IP 隧道到达外地代理。
5. 外地代理解封装得到原 IP 数据报，在代理注册表中查找永久地址对应的移动主机 MAC 地址。
6. 外地代理把原 IP 数据报封装成发往该 MAC 地址的帧，并直接交付移动主机。

![[4.8-IP隧道封装.png|900]]

- 外层 IP 首部把数据报送到转交地址，内层原 IP 数据报的目的地址仍是移动主机的永久地址。

![[4.8-外地代理转发.png|900]]

- 外地代理必须按移动 IP 的代理注册信息直接交付原数据报，不能再次依据原目的 IP 地址执行普通路由转发，否则数据报会被送回归属网络。

### 4.8.4 移动主机向固定主机发送数据报

- 移动主机仍以永久地址作为源地址，通过外地代理按普通发送流程向固定主机发送 IP 数据报。
- 路由器转发数据报时依据目的地址，不依据源地址，因此该方向不必绕经归属代理。

![[4.8-移动主机直接发送.png|900]]

- 移动主机到固定主机的路径可以直接转发，而固定主机到移动主机的路径通常要先经过归属代理。

### 4.8.5 同址转交地址

- 同址转交地址（Co-Located Care-of Address）方式要求移动主机运行额外的外地代理软件。
- 外地网络提供相应机制，使移动主机自动取得一个外地网络地址，同时把该地址作为自己的临时 IP 地址和外地代理地址。

![[4.8-同址转交地址.png|900]]

- 同址转交地址方式把外地代理功能放到移动主机中，使移动主机能够自行接收并处理发往转交地址的隧道数据报。

### 4.8.6 三角形路由问题

- 固定主机发往移动主机的数据报必须先到归属代理，再通过隧道到外地代理；即使固定主机和移动主机处于同一外地网络，也会形成绕行路径。
- 移动主机发往固定主机的数据报可以直接交付，因此往返路径不对称。

![[4.8-三角形路由.png|900]]

- 固定主机、归属代理和移动主机当前所在位置构成三角形路径，绕行会降低转发效率。

- 一种优化方法是在固定主机侧配置通信代理：
  1. 通信代理先从归属代理获取移动主机的转交地址。
  2. 固定主机发往移动主机的数据报均交给通信代理。
  3. 通信代理使用转交地址建立 IP 隧道，直接把数据报发送给外地代理。
- 该方法以增加复杂性为代价，并要求固定主机侧感知移动通信机制。

![[4.8-三角形路由优化.png|900]]

- 通信代理绕过归属代理后缩短了数据报路径，但固定主机不再对移动 IP 机制透明。
