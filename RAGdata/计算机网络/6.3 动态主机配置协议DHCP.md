- 动态主机配置协议（Dynamic Host Configuration Protocol，DHCP）为网络中的主机自动分配 IP 地址和其他网络配置信息。
- DHCP 采用客户—服务器方式，使用[[5.1.2 TCP-IP体系结构运输层中的两个重要协议#5.1.2.2 用户数据报协议 UDP|用户数据报协议 UDP]]封装报文。
- DHCP 客户使用 UDP 端口 `68`，DHCP 服务器使用 UDP 端口 `67`。

==新接入网络且尚无 IP 地址的 DHCP 客户以广播方式完成“发现、提供、请求、确认”，从服务器取得有期限的地址租约。==

### 6.3.1 DHCP 的作用

- DHCP 可以自动配置 IP 地址、子网掩码、默认网关和 DNS 服务器地址等信息。
- 主机开机后启动 DHCP 客户，无需人工逐台配置。

![[6.3-DHCP自动配置.png|900]]

- 图中 DHCP 服务器集中提供地址、掩码、默认网关和 DNS 服务器信息，使局域网主机能够访问其他网络服务。
 
### 6.3.2 获取网络配置

- 参与者：DHCP 客户和一个或多个 DHCP 服务器。
- 前置条件：客户尚未取得可用的 IP 地址。
- 触发条件：客户接入网络并启动 DHCP。
- 以下地址按客户首次获取配置、客户与 DHCP 服务器处于同一广播域的典型场景说明。

1. 客户发送 `DHCP DISCOVER`：源 IP 为 `0.0.0.0`，目的 IP 为 `255.255.255.255`。客户尚无可用地址，只能广播寻找 DHCP 服务器。
2. 服务器发送 `DHCP OFFER`：源 IP 为 DHCP 服务器的单播 IP 地址，目的 IP 为 `255.255.255.255`。服务器广播可租用的 IP 地址和其他配置，供客户选择。
3. 客户发送 `DHCP REQUEST`：源 IP 为 `0.0.0.0`，目的 IP 为 `255.255.255.255`。客户广播所选服务器和请求地址，使其他服务器撤回各自的 OFFER。
4. 所选服务器发送 `DHCP ACK`：源 IP 为 DHCP 服务器的单播 IP 地址，目的 IP 为 `255.255.255.255`，用于确认租约；若拒绝请求，则以相同的源、目的 IP 地址发送 `DHCP NACK`。

- DHCP 客户在使用所分配的 IP 地址前，使用地址解析协议 ARP 检测该地址是否已被占用。
- 若地址已被占用，客户发送 `DHCP DECLINE`：源 IP 为 `0.0.0.0`，目的 IP 为所选 DHCP 服务器的单播 IP 地址；随后重新发送 `DHCP DISCOVER`。
- 客户可通过 `DHCP RELEASE` 主动释放租用的地址：源 IP 为客户当前租用的 IP 地址，目的 IP 为 DHCP 服务器的单播 IP 地址。

### 6.3.3 地址续租

- 客户在租用期达到 `0.5` 倍时，向提供租约的服务器发送 `DHCP REQUEST` 请求续租。
- 服务器发送 `DHCP ACK` 时，客户取得新的租用期。
- 服务器发送 `DHCP NACK` 时，客户立即停止使用该地址并重新发现服务器。
- 服务器不响应时，客户继续使用地址，并在租用期达到 `0.875` 倍时广播 `DHCP REQUEST`，尝试向任一服务器续租。
- 租用期届满仍未续租成功时，客户停止使用该地址并重新发送 `DHCP DISCOVER`。

![[6.3-DHCP工作流程.png]] 

- 图中上半部分展示首次获取地址的四报文交换，下半部分展示续租、拒绝、无响应和释放租约的分支。

### 6.3.4 DHCP 中继代理

- 路由器不会转发目的地址为 `255.255.255.255` 的广播报文，因此不同网络通常无法直接共用同一台 DHCP 服务器。
- 每个网络都部署 DHCP 服务器会增加数量和维护成本。
- DHCP 中继代理接收所在网络的 DHCP 广播报文并以单播方式转发给远端 DHCP 服务器，使多个网络可以共享服务器。
![[6.3-DHCP报文与中继代理.png]]
