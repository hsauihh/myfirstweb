- 网际协议第 4 版（Internet Protocol version 4，IPv4）数据报由首部和数据载荷组成，固定首部长度为 `20 B`，带可选字段时最长为 `60 B`。
- 首部记录版本、长度、分片、寿命、上层协议、检验和以及源和目的 IPv4 地址等控制信息。
- 路由器根据首部完成转发、分片限制、寿命控制和差错检查。

==IPv4 数据报首部以 `4 B` 为一行；首部长度字段和片偏移字段都必须结合各自的计量单位解释。==

### 4.2.7.1 首部字段

| 字段         | 长度       | 作用                                           |
| ---------- | -------- | -------------------------------------------- |
| 版本         | `4 bit`  | IPv4 的值为 `4`                                 |
| 首部长度       | `4 bit`  | **以 `4 B` 为单位表示首部长度**，取值 `5~15` 对应 `20~60 B` |
| 区分服务       | `8 bit`  | 在需要区分服务时表示数据报的服务要求                           |
| 总长度        | `16 bit` | **以字节为单位表示首部与数据载荷总长度**，最大为 `65535 B`         |
| 标识         | `16 bit` | 标识同一原始数据报产生的各分片                              |
| 标志         | `3 bit`  | 包含不分片 DF 和更多分片 MF 等控制位                       |
| 片偏移        | `13 bit` | **以 `8 B` 为单位表示分片数据在原数据报数据载荷中的位置**           |
| 生存时间 TTL   | `8 bit`  | 限制数据报最多经过的路由器数量                              |
| 协议         | `8 bit`  | 指明数据载荷交付给哪个上层协议                              |
| 首部检验和      | `16 bit` | 仅检验 IPv4 首部                                  |
| 源 IPv4 地址  | `32 bit` | 标识源接口                                        |
| 目的 IPv4 地址 | `32 bit` | 标识目的接口                                       |
| 可选字段和填充    | `0~40 B` | 提供可选控制功能，并把首部补齐为 `4 B` 的整数倍                  |

![[4.2.7-IPv4数据报首部格式.png|900]]

- 图中每行宽 `32 bit`，固定部分共 `5` 行，因此固定首部长度为 `20 B`。

### 4.2.7.2 数据报分片

- 链路能够承载的最大数据报长度称为最大传送单元（Maximum Transfer Unit，MTU）。
- 当数据报总长度超过输出链路 MTU 且 DF 为 `0` 时，路由器可以对数据报分片。
- 除最后一个分片外，分片数据长度必须是 `8 B` 的整数倍。
- 分片最大数据长度为：$L_f=\left\lfloor\frac{MTU-H}{8}\right\rfloor\times8$。
- 变量：
  - $MTU$：输出链路最大传送单元，单位为 `B`。
  - $H$：每个分片的 IPv4 首部长度，单位为 `B`。
  - $L_f$：非末尾分片最多承载的数据长度，单位为 `B`。
- 片偏移为：$Offset=\frac{分片数据在原数据载荷中的起始位置}{8}$。

| 控制位 | 值 | 含义 |
| --- | --- | --- |
| DF | `1` | 不允许分片 |
| DF | `0` | 允许分片 |
| MF | `1` | 后面还有分片 |
| MF | `0` | 这是最后一个分片或数据报未分片 |

- 同一原始数据报的所有分片使用相同标识字段。
- 分片只在最终目的主机处重组，中间路由器不负责重组。

![[4.2.7-IPv4数据报分片.png|900]]

- 图中各分片使用相同标识，并由总长度、MF 和片偏移共同描述其大小与原始位置。

### 4.2.7.3 生存时间与协议字段

- 路由器每转发一次数据报，都把生存时间（Time To Live，TTL）减 `1`。
- TTL 减到 `0` 时，路由器丢弃数据报并向源主机发送[[4.5 网际控制报文协议ICMP|ICMP 时间超过报文]]。
- TTL 可以防止路由环路中的数据报永久占用网络资源。
- 协议字段指出数据载荷类型，例如 ICMP 的值为 `1`、TCP 的值为 `6`、UDP 的值为 `17`。

![[4.2.7-生存时间TTL.png|900]]

- 图中错误路由形成环路后，TTL 仍会逐跳减小，使数据报最终被丢弃。

### 4.2.7.4 首部检验和

因特网检验和：
1. 发送方把首部检验和字段置为 `0`。
2. 把 IPv4 首部划分为若干个 `16 bit` 字。
3. 使用反码算术对所有字求和，并把最高位产生的进位回卷到最低位。
4. 对结果逐位取反，得到首部检验和。
5. 接收方用同样方法把首部各字连同检验和相加；结果全为 `1` 时认为首部未检测出差错。

- 路由器修改 TTL 后必须重新计算首部检验和。
- **首部检验和不覆盖数据载荷**，数据载荷的差错检测由相应上层协议负责。

![[4.2.7-首部检验和.png|900]]

- **由于网际层并不向高层提供可靠传输的服务，并且计算首部检验和是一项耗时的操作，因此在 IPv6 中，路由器不再计算首部检验和，从而更快转发 IP 数据报**。
