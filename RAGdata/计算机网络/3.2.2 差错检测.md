- 实际通信链路不理想，传输中的比特可能发生变化，因此接收方需要使用差错检测码判断帧是否出错。
- 误码率描述一段时间内错误比特占传输比特总数的比例。
- 奇偶校验结构简单但检错能力有限，循环冗余校验的检错能力更强。

==差错检测只能判断传输是否可能出错；检测结果无误不等于绝对无错，检错码本身也不能纠正差错。==

### 3.2.2.1 比特差错与误码率

- 比特由 `0` 变为 `1` 或由 `1` 变为 `0`，称为比特差错。
- 误码率（Bit Error Rate，BER）的计算公式为：
  - $BER=\dfrac{N_e}{N}$
- 变量：
  - $N_e$：传输错误的比特数，单位为 `bit`。
  - $N$：传输的比特总数，单位为 `bit`。
  - $BER$：无量纲比率。
- 提高信道的[[2.5 信道的极限容量#2.5.3 香农公式|信噪比]]可以降低误码率，但实际信道的误码率不可能降为零。

![[3.2.2-比特差错与误码率.png|900]]

- 差错检测码使接收方能够判断帧在传输过程中是否发生比特差错。

### 3.2.2.2 奇偶校验

- 奇校验添加一个校验位，使校验后的整个数据中比特 `1` 的个数为奇数。
- 偶校验添加一个校验位，使校验后的整个数据中比特 `1` 的个数为偶数。
- 发生奇数个比特差错时，奇偶性改变，可以检测出差错。
- 发生偶数个比特差错时，奇偶性不变，无法检测出差错。

![[3.2.2-奇偶校验检测能力.png|900]]

- ==奇偶校验只能检测奇数个比特发生变化的情况，不能检测偶数个比特差错，也不能纠正差错。==

### 3.2.2.3 循环冗余校验

- 数据链路层广泛使用循环冗余校验（Cyclic Redundancy Check，CRC）检错技术。
- 收发双方约定生成多项式 $G(x)$，其最高次项和最低次项的系数必须为 `1`。
- 设 $G(x)$ 的最高次数为 $r$，发送方按以下过程计算 $r$ 位冗余码：

1. 在待发送数据后添加 $r$ 个比特 `0`，构造被除数。
2. 把 $G(x)$ 各项系数组成的比特串作为除数。
3. 使用模二除法计算余数；模二加法和减法都执行按位异或。
4. 余数不足 $r$ 位时在高位补 `0`，并将所得余数附加在数据后发送。

- 接收方用同一生成多项式除接收到的比特串：
  - 余数不为 `0`，判定传输有误。
  - 余数为 `0`，接收该帧，但仍存在未检出差错的可能。
- 例：待发送数据为 `101001`，$G(x)=x^3+x^2+1$，除数为 `1101`；被除数为 `101001000`，模二除法所得三位余数为 `001`，发送比特串为 `101001001`。

![[3.2.2-循环冗余校验计算.png|900]]

- 图中模二除法的每一步均使用异或，最后的余数补足 $r$ 位后作为冗余码。
- 常用生成多项式包括：
  - $CRC\text{-}16=x^{16}+x^{15}+x^2+1$
  - $CRC\text{-}CCITT=x^{16}+x^{12}+x^5+1$
  - $CRC\text{-}32=x^{32}+x^{26}+x^{23}+x^{22}+x^{16}+x^{12}+x^{11}+x^{10}+x^8+x^7+x^5+x^4+x^2+x+1$
- CRC 检错能力强、漏检率很低并且容易用硬件实现，因此广泛用于数据链路层。
- 奇偶校验和 CRC 都不能定位或纠正差错；纠错需要加入更多冗余信息的纠错码。
