- 计算机网络是非常复杂的系统，通信过程涉及介质、信号、寻址、转发、进程通信和网络应用等问题。
- 分层把庞大复杂的问题转化为若干较小的局部问题。
- 五层原理体系结构依次解决信号表示、链路传输、跨网络转发、进程通信和网络应用问题。

==每一层只集中解决一类问题，并通过层间协作完成端到端通信。==

### 1.6.2.1 各层需要解决的问题

| 层次 | 需要解决的主要问题 | 典型内容 |
| --- | --- | --- |
| 物理层 | 采用何种传输媒体、物理接口和信号表示比特 `0` 与 `1` | 同轴电缆、双绞线、光纤、无线传输 |
| 数据链路层 | 在一个网络或一段链路上传输数据包 | MAC 地址、数据封装格式、媒体接入控制、交换机自学习和转发帧、差错检测、可靠与不可靠传输 |
| 网络层 | 在多个网络之间传输和路由分组 | IP 地址、路由选择协议、路由表和转发表 |
| 运输层 | 实现进程之间基于网络的通信 | 端口号、可靠传输和不可靠传输 |
| 应用层 | 通过应用进程交互完成特定网络应用 | 应用进程之间按应用层规则交换信息 |

### 1.6.2.2 从局部通信到端到端通信

1. 物理层确定传输介质、接口和比特的信号表示方法。
2. 数据链路层标识同一网络中的主机，把比特流组织成可识别的数据，并协调共享介质的使用。
3. 网络层用网络号和主机号共同标识不同网络中的主机，由路由器选择路径并转发分组。
4. 运输层通过端口号标识进程，并根据需要处理传输差错。
5. 应用层通过应用进程之间的交互完成具体的网络应用。

- 数据链路层中的主机地址以 MAC 地址为例。
- 网络层中的网络和主机共同编址以 IP 地址为例。
- 运输层中的进程标识以端口号为例。
- [[1.6.3 计算机网络体系结构分层思想举例|逐层封装与解封装]] 展示了这些层次如何共同完成一次 Web 通信。
