- 拥塞是指某段时间内网络中某一资源的需求超过该资源能够提供的可用部分，导致网络性能恶化。
- TCP 发送方通过拥塞窗口（Congestion Window，cwnd）限制发送速率，并根据网络拥塞程度动态调整 cwnd。
- TCP 使用慢开始、拥塞避免、快重传和快恢复四种方法进行拥塞控制。

==TCP 以报文段最大长度 MSS 为单位调整拥塞窗口：未拥塞时逐步增大，判断发生拥塞时减小发送速率。==

### 5.3.4.1 拥塞与拥塞控制

- 计算机网络中的链路容量、交换节点的缓存和处理机都是有限资源。
- 输入负载较低时，网络吞吐量随输入负载增大而增大。
- 输入负载超过网络可承受范围后，排队、丢包和重传会使吞吐量下降；若不控制，网络吞吐量可能降为零。

![[5.3.4-拥塞控制基本概念.png|900]]

- 图中“理想拥塞控制”把吞吐量稳定在网络可承受的上限；无拥塞控制时，输入负载增大将使网络依次进入轻度拥塞、拥塞和吞吐量降为零的死锁状态。
- 流量控制只涉及特定发送方与接收方之间的流量，拥塞控制需要作用于网络中的所有发送方。
![[5.3.4-流量控制与拥塞控制.png]]
![[5.3.4-流量控制与拥塞控制区别.png]]

| 对比维度 | 流量控制 | 拥塞控制 |
| --- | --- | --- |
| 直接目标 | 使发送速率不超过接收方的接收能力 | 防止过多数据注入网络 |
| 控制范围 | 特定点到点通信 | 整个网络 |
| 控制依据 | 接收窗口 `rwnd` | 网络拥塞程度形成的 `cwnd` |

### 5.3.4.2 拥塞控制方法的分类

- 按是否在运行中依据反馈调整网络，拥塞控制分为：

| 类型 | 工作方式 |
| --- | --- |
| 开环控制 | 设计网络时预先考虑拥塞因素，运行中不再中途修正 |
| 闭环控制 | 监测拥塞、传送拥塞信息，并据此调整网络运行 |
![[5.3.4-开环控制与闭环控制.png]]
- 闭环控制包括监测拥塞、把拥塞信息传送到能够采取行动的位置，以及调整网络运行三个环节。
- 闭环控制可以依据缓存溢出丢包比例、路由器平均队列长度、超时重传分组数量、平均分组时延及其标准差等指标判断拥塞。
- 闭环拥塞控制按拥塞信息的反馈形式进一步分为：

| 类型 | 拥塞信息来源 | 判断方式 |
| --- | --- | --- |
| 显式反馈 | 拥塞节点（路由器） | 拥塞节点向源点明确提供网络拥塞状态信息 |
| 隐式反馈 | 源点自身 | 源点通过超时重传或往返时间等网络行为推断拥塞 |

- TCP 采用隐式反馈算法。

![[5.3.4-显式与隐式反馈.png|900]]

- 图中数据由源点 `H1` 发往目的主机 `H2`，发生拥塞的路由器 `R2` 沿反方向向 `H1` 返回 ICMP 源点抑制报文，形成显式反馈路径。
- 隐式反馈不由路由器发送拥塞通知，而由源点根据超时重传或往返时间变化自行判断网络是否拥塞。

### 5.3.4.3 TCP 拥塞控制的基本条件

- 数据沿一个方向传送，确认沿反方向传送。
- 接收方具有足够大的接收缓存，因此发送窗口大小由网络拥塞程度决定。
- 以 TCP 报文段最大长度（Maximum Segment Size，MSS）作为拥塞窗口的单位。
- TCP 发送方的实际发送窗口为：$swnd=\min(cwnd,rwnd)$。
- 在不考虑流量控制的条件下，可取 $swnd=cwnd$。
- 发送方没有按时收到本应到达的确认时，推断网络发生拥塞。

![[5.3.4-TCP四种拥塞控制方法.png]]
![[5.3.4-发送窗口与拥塞窗口.png]]
![[5.3.4-慢开始门限.png]]
- TCP 的四种方法共同决定拥塞窗口的增长方式，以及出现超时或重复确认时如何减小窗口。

### 5.3.4.4 慢开始与拥塞避免

- 慢开始门限 `ssthresh` 决定使用慢开始还是拥塞避免：
  - 当 $cwnd<ssthresh$ 时，使用慢开始。
  - 当 $cwnd>ssthresh$ 时，停止慢开始并使用拥塞避免。
  - 当 $cwnd=ssthresh$ 时，两种方法均可使用。
![[5.3.4-慢开始指数增长.png]]
![[5.3.4-拥塞避免线性增长.png]]
![[5.3.4-拥塞避免超时.png]]
![[5.3.4-超时后再次慢开始.png]]
![[5.3.4-超时后拥塞避免.png]]
![[5.3.4-拥塞窗口变化.png]]

#### 慢开始

1. 建立 TCP 连接后，把 $cwnd$ 初始设置为 `1 MSS`。
2. 每收到一个对新报文段的确认，就把 $cwnd$ 增加 `1 MSS`。
3. 每经过一个传输轮次，$cwnd$ 近似加倍，呈指数增长。
4. 当 $cwnd$ 达到 `ssthresh` 时，转而使用拥塞避免。

- “慢开始”是指一开始向网络注入的报文段少，并不是拥塞窗口增长速度慢。

#### 拥塞避免

1. 每经过一个传输轮次，把 $cwnd$ 增加 `1 MSS`，使其线性增长。
2. 重传计时器超时时，判断网络发生拥塞。
3. 把 $ssthresh$ 更新为发生拥塞时 $cwnd$ 的一半。
4. 把 $cwnd$ 减为 `1 MSS`，重新执行慢开始。

- 慢开始使 `cwnd` 按传输轮次成倍增大，拥塞避免使 `cwnd` 按传输轮次线性增大；两者都不能完全避免拥塞，只能降低发生拥塞的概率。

### 5.3.4.5 快重传与快恢复

![[5.3.4-个别丢包误判拥塞.png]]
#### 快重传

- 接收方收到失序报文段时，应立即对已经按序收到的最后一个报文段发送重复确认。
- 接收方每收到一个失序报文段，就立即发送重复确认，不等待捎带确认。
- 发送方连续收到 `3` 个重复确认时，立即重传相应报文段，不等待重传计时器超时。
- 快重传可以尽早修复个别报文段丢失，避免发送方误以为网络发生严重拥塞。

![[5.3.4-快重传规则.png]]
![[5.3.4-快重传举例.png]]
#### 快恢复

1. 发送方收到 `3` 个重复确认后执行快重传。
2. 把 $ssthresh$ 和 $cwnd$ 调整为当前 $cwnd$ 的一半。
3. 直接执行拥塞避免，使 $cwnd$ 线性增大。
![[5.3.4-快恢复规则.png]]
- 也有的快恢复实现是把快恢复开始时的 $cwnd$ 值再增大一些，即 $cwnd$ =新$ssthresh$ +3.
  - 发送方收到3个重复确认，就表明有3个数据报文段已经离开了网络
  - 这三个报文段不再消耗网络资源而是停留在接收方的接收缓存中。
  - 可见网络中不是堆积了报文段而是减少了3个报文段，因此可以适当把 $cwnd$ 增大一些。 
- 快恢复不把 $cwnd$ 降到 `1 MSS`，因为连续的重复确认表明网络仍能传送部分报文段。

![[5.3.4-超时与快恢复窗口变化.png]]

- 图中超时重传后 `cwnd` 回到 `1 MSS` 并重新慢开始；收到 `3` 个重复确认后则把窗口减半并进入快恢复。

![[5.3.4-拥塞控制状态转换.png]]
