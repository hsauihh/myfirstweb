- 网际控制报文协议（Internet Control Message Protocol，ICMP）用于报告 IP 数据报处理中的差错并进行网络探测。
- 主机或路由器可以发送 ICMP 差错报告报文和询问报文。
- ICMP 报文封装在 IP 数据报中，IPv4 首部协议字段值为 `1`。

==ICMP 报告的是网络层交付过程中发现的问题，但它不能保证原始 IP 数据报一定得到可靠交付。==

### 4.5.1 ICMP 概述

![[4.5-ICMP概述.png|900]]

- ICMP 通过差错报告帮助源主机了解交付失败原因，通过询问报文支持连通性和时间信息探测。

### 4.5.2 差错报告报文

| 类型 | 触发情况 | 作用 |
| --- | --- | --- |
| 终点不可达 | 路由器或目的主机无法交付数据报 | 告知源主机目的网络、主机、协议或端口等不可达 |
| 源点抑制 | 路由器或主机因拥塞丢弃数据报 | 要求源主机降低发送速率 |
| 时间超过 | TTL 减为 `0`，或分片重组超过规定时间 | 告知源主机数据报因超时被丢弃 |
| 参数问题 | IPv4 首部字段存在错误 | 指出首部参数问题并丢弃数据报 |
| 改变路由 | 路由器发现主机可以使用更合适的下一跳 | 建议主机更新路由选择 |

![[4.5-ICMP终点不可达报文.png]]
![[4.5-ICMP源点抑制报文.png]]![[4.5-ICMP时间超过报文.png]]
![[4.5-ICMP参数问题报文.png]]
![[4.5-ICMP改变路由报文.png]]

#### 不发送差错报告的情况

- 对 ICMP 差错报告报文不再发送 ICMP 差错报告报文。
- 对一个分片数据报中除第一个分片以外的后续分片不发送 ICMP 差错报告报文。
- 对目的地址为多播地址的 IP 数据报不发送 ICMP 差错报告报文。
- 对源地址为 `127.0.0.0`、`0.0.0.0` 等特殊地址的 IP 数据报不发送 ICMP 差错报告报文。

![[4.5-不发送ICMP差错报文的情况.png|900]]

- 这些限制避免差错报文递归触发新的差错报文，并避免对无法明确定位源接口的数据报产生无效响应。

### 4.5.3 询问报文

| 类型 | 作用 |
| --- | --- |
| 回送请求与回答 | 测试特定目的主机是否可达，并测量往返时间 |
| 时间戳请求与回答 | 请求对方返回时间信息 |

### 4.5.4 分组网间探测 PING

- 分组网间探测（Packet InterNet Groper，PING）用于测试主机之间的连通性。
- PING 发送 ICMP 回送请求并等待目的主机返回 ICMP 回送回答。
- PING 没有使用 TCP 或 UDP，因此没有运输层端口号。
- 返回结果可以统计响应时间、分组丢失和往返时间变化。

![[4.5-PING工作原理.png|900]]

- 图中目的主机收到 ICMP 回送请求后返回回送回答，由此验证端到端 IP 连通性。

### 4.5.5 跟踪路由

- Windows 的 `tracert` 和类 Unix 系统的 `traceroute` 用于探测从源主机到目的主机所经过的路由器。
- 探测程序发送 TTL 依次为 `1、2、3、...` 的数据报。
- 第 $n$ 个路由器把 TTL 减为 `0` 后丢弃数据报，并返回 ICMP 时间超过报文，从而暴露该跳接口地址。
- 逐步增大 TTL，直到收到来自目的主机的响应，就能得到路径上的各跳路由器。

![[4.5-跟踪路由工作原理.png|900]]

- 图中 TTL 每增加 `1`，探测数据报就能多经过一台路由器，并由新的路由器返回 ICMP 时间超过报文。
