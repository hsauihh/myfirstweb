- 对称密钥密码体制使用相同的加密密钥和解密密钥，通信双方必须预先共享并保守该密钥。
- 对称密钥算法适合高效加密大量数据，但密钥的安全分发是其主要难题。

==对称密钥密码体制的算法可以公开，其安全性取决于共享密钥是否保密以及密钥空间是否足够大。==

### 7.2.2.1 数据加密标准 DES

- 数据加密标准（Data Encryption Standard，DES）由 IBM 公司研制，1977 年被确定为数据加密标准。
- DES 将明文划分为 `64` 比特的数据块，并逐块生成 `64` 比特的密文。
- DES 密钥长度为 `64` 比特，其中 `56` 比特是有效密钥，另外 `8` 比特用于奇偶校验。

![[7.2.2-DES分组加密.png|900]]

- DES 的保密性依赖密钥的保密和长度，其算法是公开的。
- `56` 比特密钥共有 $2^{56}\approx 7.6\times 10^{16}$ 种可能，但该密钥长度已经无法抵御现实中的穷举攻击。
- 1999 年，专用计算机已能在约 `22` 小时内破译 DES。

### 7.2.2.2 三重 DES

- 三重 DES（Triple DES，3DES）使用 `3` 个密钥依次执行三次 DES 运算，以解决 DES 有效密钥过短的问题。
- 加密按 `K_1` 加密、`K_2` 解密、`K_3` 加密的顺序执行。
- 解密按 `K_3` 解密、`K_2` 加密、`K_1` 解密的相反顺序执行。

![[7.2.2-3DES工作过程.png|900]]

- 当 `K_1=K_2=K_3` 时，3DES 等效于 DES，有利于从 DES 逐步迁移到 3DES。

### 7.2.2.3 高级加密标准 AES

- 高级加密标准（Advanced Encryption Standard，AES）支持 `128` 比特、`192` 比特和 `256` 比特的密钥长度。
- AES 可以用硬件和软件快速实现，且所需内存较少，适用于小型移动设备。
- AES 取代 DES 和 3DES 成为新的高级加密标准。

| 对比维度 | DES | 3DES | AES |
| --- | --- | --- | --- |
| 密钥 | `56` 比特有效密钥 | 使用 `3` 个 DES 密钥 | `128`、`192` 或 `256` 比特 |
| 工作方式 | 对每个 `64` 比特分组执行一次 DES | 对数据依次执行三次 DES | 可由硬件或软件快速实现 |
| 主要结论 | 密钥过短，已不再安全 | 增强 DES，但计算次数更多 | 密钥更长、实现高效 |

![[7.2.2-AES密钥长度.png|900]]

- ==DES 因 `56` 比特有效密钥过短而不再安全；AES 以更长密钥和高效实现取代 DES 与 3DES。==
