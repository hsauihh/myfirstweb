- 电子邮件是因特网上最早流行的应用之一，采用客户—服务器方式实现异步通信。
- 电子邮件系统由用户代理、邮件服务器以及发送和读取邮件所用的协议组成。

==简单邮件传送协议 SMTP 负责推送邮件，POP3 或 IMAP4 负责从接收方邮件服务器读取邮件。==

### 6.6.1 电子邮件系统的组成

- 用户代理是用户与电子邮件系统的接口，又称电子邮件客户端软件。
- 邮件服务器负责发送、接收和存储邮件，内部包含邮件发送队列和用户邮箱。

1. 发送方用户代理使用 SMTP 把邮件推送到发送方邮件服务器。
2. 发送方邮件服务器作为 SMTP 客户，把邮件推送到接收方邮件服务器。
3. 接收方用户代理使用 POP3 或 IMAP4 从接收方邮件服务器读取邮件。

![[6.6-电子邮件系统组成.png|900]]

- 图中邮件发送的两个阶段使用 SMTP，接收方读取阶段使用 POP3；这些应用层协议均使用 TCP 连接。

### 6.6.2 简单邮件传送协议SMTP

- 简单邮件传送协议（Simple Mail Transfer Protocol，SMTP）采用 TCP 连接的客户—服务器方式，服务器熟知端口为 `25`。
- SMTP 只能传送 `7 bit ASCII` 文本数据，不能直接传送可执行文件或其他二进制对象。

1. SMTP 客户建立 TCP 连接，服务器返回 `220` 就绪应答。
2. 客户发送 `HELO` 表明身份，服务器返回 `250`。
3. 客户用 `MAIL FROM` 指定发件人，再用 `RCPT TO` 指定收件人；服务器分别返回状态码。
4. 客户发送 `DATA`，服务器以 `354` 表示可以接收内容。
5. 客户传送邮件内容，并用单独一行的点号结束；服务器返回 `250`。
6. 客户发送 `QUIT` 请求断开连接。

![[6.6-SMTP通信过程.png|900]]

- 图中 SMTP 命令与三位数字应答交替传送，邮件正文仅在 `DATA` 得到 `354` 应答后发送。

### 6.6.3 邮件格式与MIME

- 电子邮件的信息格式由征求意见稿（Request for Comments，RFC）5322 定义，分为首部和主体两部分。
- 首部包含由邮件系统自动提取的 `From`、`To`、`Cc`、`Subject` 等字段。
- 多用途因特网邮件扩展（Multipurpose Internet Mail Extensions，MIME）增加邮件首部字段以说明邮件类型，并定义多媒体内容的编码方法。
- MIME 把非 ASCII 数据编码为 ASCII 数据后交给 SMTP 传送，接收方再解码还原。

### 6.6.4 POP3与IMAP4

| 对比维度 | POP3 | IMAP4 |
| --- | --- | --- |
| 名称 | 邮局协议第 3 版 | 因特网邮件访问协议第 4 版 |
| 工作方式 | 下载并删除或下载并保留 | 联机操作服务器上的邮箱 |
| 服务器端管理 | 不允许用户在服务器上组织邮件 | 可在服务器上创建文件夹并分类管理 |
| TCP 熟知端口 | `110` | `143` |

![[6.6-POP3与IMAP4.png|900]]

- POP3 功能简单，IMAP4 能像操作本地邮箱一样操作服务器上的邮件，因此必须保持联机。

### 6.6.5 基于万维网的电子邮件

- 用户只需浏览器即可通过邮件服务网站撰写、收发和管理邮件，无需安装专用用户代理。
- 浏览器与邮件服务器网站之间使用 HTTP，邮件服务器之间仍使用 SMTP。
