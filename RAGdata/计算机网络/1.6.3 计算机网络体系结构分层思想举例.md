- 分层通信中，发送端自上而下逐层封装数据，接收端自下而上逐层解封装数据。
- 浏览器进程与 Web 服务器进程在应用层进行逻辑通信，实际数据需要经过主机、网络和路由器传送。
- 路由器处理物理层、数据链路层和网络层功能，不处理端系统的应用层和运输层功能。

==发送端逐层添加控制信息，接收端按相反顺序逐层去除控制信息，中间路由器依据网络层信息转发分组。==

### 1.6.3.1 发送端逐层封装

- 参与者：浏览器进程、发送主机各层、路由器、接收主机各层和 Web 服务器进程。
- 触发条件：浏览器请求 Web 服务器执行相应操作。

1. 应用层构造 HTTP 请求报文。
2. 运输层为 HTTP 报文添加 TCP 控制信息，形成 TCP 报文段，用于区分应用进程并实现可靠传输。
3. 网络层添加 IP 控制信息，形成 IP 数据报，用于跨网络传输和路由选择。
4. 数据链路层为 IP 数据报添加本层控制信息，封装成适合当前链路传输的帧。
5. 物理层把帧的比特流转换成相应信号并送入传输介质。

### 1.6.3.2 路由器转发与接收端解封装

1. 路由器从物理层接收信号并恢复比特流。
2. 路由器在数据链路层解封装帧，把 IP 数据报交给网络层。
3. 路由器根据 IP 数据报中的信息选择下一跳，再为下一段链路重新封装数据链路层帧。
4. Web 服务器主机收到数据后，自下而上逐层解封装。
5. HTTP 请求报文最终交给 Web 服务器进程处理。
6. Web 服务器构造 HTTP 响应报文，沿相反方向完成封装、传输和解封装。

![[1.6.3-逐层封装与解封装.png|900]]

- 端系统完成端到端的逐层封装与最终解封装；路由器逐跳解封装入站链路层帧、处理下三层信息，并为出站链路重新封装帧。

### 1.6.3.3 数据传输效率

- 应用层数据传输效率等于应用层数据量与实际传输总量之比：
  - $\eta=\dfrac{L_{\text{应用数据}}}{L_{\text{应用数据}}+L_{\text{额外开销}}}\times100\%$
- 例：OSI 参考模型的应用层发送 `400 B` 数据且不拆分，除物理层和应用层外，其余五层封装协议数据单元时各加入 `20 B` 额外开销：
  - $L_{\text{总计}}=400+5\times20=500\,\mathrm{B}$
  - $\eta=\dfrac{400}{500}\times100\%=80\%$
- 适用条件：应用数据不拆分，各层只加入题目给定的固定开销。

==各层控制信息会产生额外开销，应用数据在总传输量中所占比例就是传输效率。==
