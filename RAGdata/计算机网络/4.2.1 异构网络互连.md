- 异构网络互连是指使用路由器连接拓扑、性能和所用协议各不相同的网络。
- 网络层使用统一的[[4.2 网际协议IP|网际协议（Internet Protocol，IP）]]屏蔽各网络接口的差异，使主机能够按统一方式进行跨网络通信。
- 每个实际网络仍按自己的数据链路层和物理层协议传输，路由器在网络边界完成逐跳转发。

==互连后的 IP 网是逻辑上的统一网络，底层各异构网络的差异并未消失，而是被 IP 层向上屏蔽。==

### 4.2.1.1 IP 网的形成

1. 各异构网络内部使用各自的网络接口协议传送帧。
2. 路由器连接不同网络，并接收发往其他网络的 IP 数据报。
3. 路由器根据目的 IP 地址选择下一跳，再用输出网络的帧格式重新封装数据报。
4. 对上层协议和用户而言，互连结构表现为一个统一的 IP 网。

![[4.2.1-异构网络互连形成IP网.png|900]]

- 图中不同拓扑和协议的网络经路由器互连后，在网络层共同表现为一个统一的 IP 网。
