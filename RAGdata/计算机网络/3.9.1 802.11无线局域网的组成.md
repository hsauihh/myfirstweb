- IEEE 802.11 无线局域网（Wireless Local Area Network，WLAN）通常称为无线保真（Wireless Fidelity，Wi-Fi），用于在局部范围内通过无线信道连接站点。
- 按照是否具有固定基础设施，802.11 无线局域网分为有固定基础设施的网络和无固定基础设施的自组织网络。
- 有固定基础设施的网络通过接入点组成基本服务集，并可借助分配系统扩展覆盖范围；自组织网络则不依赖接入点。

==有固定基础设施的 802.11 无线局域网以接入点为中心组织通信；自组织网络只规定站点间的单跳通信，不包含多跳路由功能。==

### 3.9.1.1 基本服务集与扩展服务集

- 802.11 标准于 1997 年首次制定，后来得到广泛应用。
- 接入点（Access Point，AP）是有固定基础设施无线局域网的中心站。
- 一个 AP 及其关联的移动站构成基本服务集（Basic Service Set，BSS）。
- BSS 的覆盖范围称为基本服务区（Basic Service Area，BSA），其直径小于 `100 m`。
- BSS 内各站点之间以及站点与 BSS 外部的通信都必须经过 AP 转发。
- 网络管理员需要为 AP 分配以下参数：
  - 最大 `32 B` 的服务集标识符（Service Set Identifier，SSID）。
  - 一个无线通信信道。
- SSID 是使用该 AP 的 802.11 无线局域网的名称。
- 多个 BSS 可以通过分配系统（Distribution System，DS）连接成扩展服务集（Extended Service Set，ESS）。
- DS 可以使用以太网，ESS 还可以通过路由器接入因特网。
- 相邻 BSS 的覆盖范围可以重叠，使移动站能够在 ESS 内漫游。

![[3.9.1-BSS与ESS.png|900]]

- 两个 BSS 通过 DS 组成 ESS，移动站从 BSS1 移向 BSS2 时可以改为关联 AP2。

### 3.9.1.2 关联、重关联与分离

- 802.11 标准没有规定漫游的具体实现方法，只定义了关联、重关联和分离等基本服务。
- 关联（Association）服务使移动站与 AP 建立关联，可以采用两种扫描方式：
  - 被动扫描：移动站监听 AP 周期性发送的信标帧。
  - 主动扫描：移动站发送探测请求帧，并等待 AP 返回探测响应帧。
- 信标帧包含 SSID、AP 的 MAC 地址、所支持的速率、加密算法和安全配置等参数。
- 移动站从一个 AP 的覆盖范围移入另一个 AP 的覆盖范围时，可以使用重关联（Reassociation）服务。
- 移动站需要结束与 AP 的关联时，可以使用分离（Disassociation）服务。

![[3.9.1-关联服务.png|900]]

- 被动扫描通过 AP 广播的信标帧发现网络，移动站依据其中的网络参数选择 AP。

### 3.9.1.3 自组织网络

- 无固定基础设施的 802.11 无线局域网称为自组织网络（ad hoc Network）。
- 自组织网络不设置 AP，处于彼此通信范围内的站点可以直接通信。
- 802.11 的 ad hoc 模式只支持站点间的单跳通信，标准本身没有包含多跳路由功能。
- 需要经过多个转发站通信时，必须使用特定的路由选择协议。
- 自组织网络组网方便、不需要基站并具有较好的生存性，可用于军用和民用场景，但 802.11 ad hoc 模式的应用较少。

![[3.9.1-自组织网络.png|900]]

- A 与 E 之间若要经过 B、C、D 多跳转发，需要另行使用路由选择协议；802.11 标准本身只规定通信范围内的单跳传输。
