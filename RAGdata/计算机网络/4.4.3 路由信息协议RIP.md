- 路由信息协议（Routing Information Protocol，RIP）是一种基于距离向量的内部网关协议。
- RIP 使用跳数作为距离度量，选择到达目的网络所经过路由器数量最少的路由。
- RIP 实现简单、开销较低，但只适合规模较小的网络。
- **由UDP协议进行封装，属于应用层**
==RIP 规定最大有效距离为 `15` 跳，距离 `16` 表示目的网络不可达；它选择跳数最少的路径，而不比较链路带宽。==

### 4.4.3.1 距离度量

- 路由器到直连网络的 RIP 距离为 `1`。
- 每经过一台路由器，距离增加 `1`。
- 多条路由到达同一目的网络时，RIP 选择距离最小的路由。
- 只按跳数选择可能使 RIP 选中链路速率较低的路径。

![[4.4.3-RIP距离与不可达.png|900]]

- 图中 `15` 跳仍可达，增加一跳成为 `16` 后即按不可达处理。

### 4.4.3.2 路由信息交换

- RIP 路由器只与相邻路由器交换信息。
- 路由器周期性地把自己的完整 RIP 路由表发送给邻居，典型更新周期为约 `30 s`。
- RIP 报文封装在 UDP 用户数据报中，使用端口号 `520`。

### 4.4.3.3 距离向量更新

- 收到相邻路由器 $X$ 的路由表后，先把其中每条路由的距离加 `1`，并把下一跳改为 $X$。

1. 本地路由表没有该目的网络时，加入新路由。
2. 原路由的下一跳就是 $X$ 时，使用来自 $X$ 的最新信息更新该路由。
3. 原路由来自其他下一跳，但新路由距离更小时，替换原路由。
4. 原路由来自其他下一跳且新路由不更优时，保留原路由。

![[4.4.3-RIP路由更新.png|900]]

- 图中相邻路由器通告的距离先统一加 `1`，再按目的网络逐项更新本地路由表。

### 4.4.3.4 坏消息传播慢

- 网络可达的好消息能够逐跳传播，故障导致的不可达消息却可能在相邻路由器之间反复被错误路由覆盖。
- 路由器可能把邻居从自己学到的旧路由再次当作新路径，形成路由环路。
- 距离会在循环更新中逐步增加，直到达到 `16` 才确认目的网络不可达，这就是计数到无穷问题。

![[4.4.3-RIP坏消息传播慢.png|900]]

- 图中失效网络仍被相邻路由器交替通告，距离逐次增加，因此 RIP 收敛较慢。

### 4.4.3.5 特点

| 优点           | 局限                    |
| ------------ | --------------------- |
| 实现简单，路由计算开销小 | 最大距离只有 `15`，限制网络规模    |
| 路由器只需与邻居交换信息 | 周期性交换==完整路由表==会产生通信开销 |
| 适合小型自治系统     | 只按跳数选路，不能反映链路性能       |
| 配置和维护较容易     | 路由环路和坏消息传播慢会延长收敛时间    |

 