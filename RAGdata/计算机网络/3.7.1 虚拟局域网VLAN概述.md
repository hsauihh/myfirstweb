- 虚拟局域网（Virtual Local Area Network，VLAN）将局域网中的站点划分为与物理位置无关的逻辑组，每个逻辑组就是一个 VLAN。
- VLAN 用于把一个巨大的广播域划分为多个较小的广播域，减少无关广播并便于管理。
- 同一 VLAN 中的站点可以直接通信，不同 VLAN 中的站点不能直接通信；具体实现依赖[[3.7.2 虚拟局域网VLAN的实现机制|VLAN 标记与交换机端口规则]]。

==VLAN 按逻辑关系而非物理位置划分广播域，广播帧只在所属 VLAN 内传播。==

### 3.7.1.1 VLAN 的诞生背景

- 多个分散的局域网通过交换机互连后形成交换式以太网，所有站点可能处于同一个巨大的广播域。
- TCP/IP 体系中的地址解析协议（Address Resolution Protocol，ARP）需要使用广播，因此局域网中的广播无法完全避免。
- 巨大的广播域会带来以下问题：
  - 广播风暴会浪费网络资源和各主机的 CPU 资源。
  - 广播域越大，管理和维护越困难。
  - 大量站点处于同一广播域会带来潜在的安全问题。

![[3.7.1-巨大广播域的问题.png|900]]

- ARP 等广播帧会被交换机扩散到整个广播域；广播数量过多时，网络和所有站点都要承担无关的处理开销。

- 路由器可以隔离广播域，使广播帧不能跨路由器传播，但使用路由器分割大量广播域的成本较高。

![[3.7.1-路由器隔离广播域.png|900]]

- 图中路由器把交换式以太网分成两个广播域，任一侧的广播帧都不会被转发到另一侧。

### 3.7.1.2 VLAN 的逻辑分组

- 一个 VLAN 中的站点具有某些共同的应用需求。
- VLAN 的划分与站点的物理位置无关：
  - 位于不同楼层、不同网段或连接不同交换机的站点可以属于同一 VLAN。
  - 连接同一台交换机的站点可以属于不同 VLAN。
- 同一 VLAN 中的站点可以直接通信，其广播帧只会被同一 VLAN 中的站点接收。
- 不同 VLAN 中的站点不能直接通信。
- VLAN 不是一种新型网络，而是局域网提供的一种服务。

![[3.7.1-VLAN逻辑分组.png|900]]

- 图中的 VLAN1 和 VLAN2 都跨越了物理楼层和交换机；同一逻辑组内可以接收彼此的广播，不同逻辑组之间相互隔离。

### 3.7.1.3 VLAN 的划分方式

- 按照划分依据，VLAN 可分为：

| 划分依据 | 工作原理 | 优点 | 局限 | 适用场景 |
| --- | --- | --- | --- | --- |
| 端口 | 管理员把交换机的端口静态分配到各 VLAN，端口与其所属 VLAN 绑定 | 配置简单直观，易于实现和排查 | 主机更换接入端口后需要管理员重新配置 | 规模不大、位置较固定的网络 |
| MAC 地址 | 交换机根据帧的源 MAC 地址确定所属 VLAN | 主机移动位置后 VLAN 关系自动保持，无需重新配置 | MAC 地址数量多，逐台登记的管理开销大 | 小型网络或对移动性要求高的场合 |
| IP 子网 | 根据帧携带的主机 IP 地址或子网号划分 VLAN | 可按网段组织逻辑分组，主机更换接入端口后归属不变 | 需要解析网络层地址，处理开销较大 | 按网段组织的大型网络 |
| 协议 | 根据帧携带的协议类型（如 IP、IPX）划分 VLAN | 可以对不同协议类型的流量分别管理 | 依赖上层协议类型，应用面较窄 | 多协议并存的网络 |

- 按是否由管理员手工指定，划分方式还可以分为：
  - 静态划分：基于端口，由管理员手工配置端口与 VLAN 的对应关系。
  - 动态划分：基于 MAC 地址、IP 子网或协议，由交换机自动识别主机信息确定所属 VLAN。

==基于端口是最常用的静态划分方式；基于 MAC 地址和 IP 子网的动态划分便于主机移动，但实现开销较大。==

- 无论采用哪种划分方式，最终都由交换机通过 [[3.7.2 虚拟局域网VLAN的实现机制|802.1Q 帧的 VLAN 标记与端口规则]]完成帧的转发限制。
