- 万维网（World Wide Web，WWW）是一个大规模、联机式的信息储藏所，并不是某种特殊的计算机网络。
- 万维网以链接把分布在不同网站的网页连接成信息网，使用统一资源定位符定位资源，使用 HTTP 传送文档。

==浏览器依据 URL 定位资源，通过 HTTP 向万维网服务器请求文档；HTTP 本身无状态，Cookie 和 Web 缓存分别用于记录用户状态和减少重复传输。==

### 6.7.1 万维网的组成

- 浏览器的核心部分是渲染引擎，也称浏览器内核，负责解析和显示网页内容。
- 万维网使用客户—服务器方式，浏览器是万维网客户，网站中的服务器程序是万维网服务器。
- 浏览器向服务器发送请求，服务器返回所请求的万维网文档。

### 6.7.2 统一资源定位符URL

- 统一资源定位符（Uniform Resource Locator，URL）指出因特网上某种资源的位置。
- URL 的一般格式为：`<协议>://<主机>:<端口>/<路径>`。
- 协议指出访问资源使用的协议，主机可使用域名或 IP 地址，端口标识服务器进程，路径标识主机内的资源位置。

![[6.7-URL组成.png|900]]

- 图中的 URL 由协议、主机、端口和路径共同确定资源的位置；使用协议默认端口时可以省略端口字段。

### 6.7.3 万维网文档

- 超文本标记语言（HyperText Markup Language，HTML）使用标签描述网页的结构和内容。
- 层叠样式表（Cascading Style Sheets，CSS）描述网页的样式。
- JavaScript 是控制网页行为的脚本语言，与 Java 没有关系。
- HTML 文档还可引用其他文件，并通过超链接指向其他资源。

![[6.7-万维网文档组成.png|900]]

- HTML、CSS 和 JavaScript 分别承担网页结构与内容、样式和行为的描述。

### 6.7.4 超文本传送协议HTTP

- 超文本传送协议（HyperText Transfer Protocol，HTTP）规定浏览器怎样向服务器请求文档，以及服务器怎样把文档传送给浏览器。
- HTTP 使用 TCP 连接，HTTP 服务器的默认端口为 `80`。
- HTTP/1.0 使用非持续连接，每请求一个对象都建立一条 TCP 连接，收到响应后关闭。
- 非持续连接获取一个文档需要 `2RTT` 加文档传输时延：一个 RTT 用于建立 TCP 连接，一个 RTT 用于发送请求并收到响应的开始部分。
- 浏览器可并行建立多条 TCP 连接请求多个对象以减少时延，但会大量占用服务器资源。
- HTTP/1.1 默认使用持续连接，可在同一条 TCP 连接上传送多个请求和响应。
- 非流水线方式收到上一个响应后才能发出下一请求；流水线方式可以连续发送请求，提高传输效率。

![[6.7-HTTP连接方式.png|900]]

![[6.7-HTTP持续连接与流水线.png|900]]

### 6.7.5 HTTP报文

- HTTP 是面向文本的协议，报文各字段为 ASCII 码串，字段长度不固定。
- 请求报文由请求行、若干首部行、空行和可选的实体主体组成。
- 请求行包含方法、URL 和 HTTP 版本，常用方法包括 `GET`。
- 响应报文由状态行、若干首部行、空行和可选的实体主体组成。
- 状态行包含 HTTP 版本、状态码和短语，例如 `202 Accepted` 与 `400 Bad Request`。

![[6.7-HTTP响应报文格式.png|900]]

- 请求行与状态行决定报文的基本语义，首部行携带附加控制信息，空行标志首部结束。

### 6.7.6 Cookie

- HTTP 是无状态协议，服务器不会仅凭连续请求记住用户状态。
- Cookie 让服务器记录用户信息，实现登录状态和个性化服务等功能。

1. 浏览器首次请求网站资源。
2. 服务器生成唯一 Cookie 识别码，并以该识别码为索引在后端数据库中记录用户信息。
3. 服务器通过响应首部 `Set-Cookie` 把识别码发送给浏览器。
4. 浏览器把 Cookie 存入文件，后续请求携带该 Cookie，使服务器能够关联用户记录。

![[6.7-Cookie记录用户信息.png|900]]

- Cookie 是让无状态 HTTP 承载有状态会话的一种技术，实际用户信息可保存在服务器数据库中。

### 6.7.7 万维网缓存与代理服务器

- 万维网缓存又称 Web 缓存（Web Cache），可位于客户机或中间系统。
- 位于中间系统的 Web 缓存又称代理服务器（Proxy Server）。
- Web 缓存把最近的请求和响应暂存在本地磁盘中。
- 新请求与已缓存请求相同时，缓存可以直接返回响应，不必再次访问原始服务器。
- 缓存可使用 `Last-Modified`、`Expires` 和条件请求 `If-Modified-Since` 检查资源是否仍有效。
- 原始服务器返回 `304 Not Modified` 时，缓存继续使用已有副本，无需重新传送实体主体。

![[6.7-万维网缓存.png|900]]

- 代理服务器先检查本地副本，必要时向原始服务器发送条件请求，从而减少广域网传输并缩短响应时间。


### 课程小结
![[6.7-万维网小结.png|900]]