- 运行在不同主机上的应用进程可以通过网络交换数据。
- 运输层为应用进程提供端到端的逻辑通信服务，并使用端口号标识通信端点。
- 运输层的逻辑通信不等于物理链路上的直接连接，实际数据仍由网际层、链路层和物理层逐段转发。

==运输层让应用进程看起来像在直接通信，同时屏蔽路由器、链路和底层网络接口的细节。==

### 5.1.1.1 进程与通信端点

- 一台主机可以同时运行多个应用进程，例如主机 A 上的 AP1、AP2，以及主机 B 上的 AP3、AP4。
- 应用进程之间的通信由运输层承载，运输层通过端口把数据交给指定的应用进程。
- 运输层向应用层提供逻辑通信服务；应用进程不需要直接处理网络拓扑和路由选择协议等网际层细节。
![[5.1.1-网络层与运输层作用范围.png]]

![[5.1.1-进程间基于网络的通信.png|900]]

- 图中虚线表示 AP1/AP2 与 AP3/AP4 之间的应用进程通信，蓝色箭头表示运输层提供的逻辑通信服务；下方实际路径经过局域网（Local Area Network，LAN）、路由器 R1、广域网（Wide Area Network，WAN）、路由器 R2 和 LAN。

### 5.1.1.2 运输层与下层的关系

- 网际层提供主机到主机的数据报传送，运输层在此基础上区分同一主机内的不同应用进程。
- 运输层把应用数据交给网际层，网际层再通过不同网络接口和路由器完成逐段转发。
- 运输层协议依据应用需求提供不同服务，[[5.1.2 TCP-IP体系结构运输层中的两个重要协议|TCP]]提供面向连接的可靠服务，[[5.1.2 TCP-IP体系结构运输层中的两个重要协议|UDP]]提供无连接的不可靠服务。
![[5.1.1-运输层逻辑通信.png]]