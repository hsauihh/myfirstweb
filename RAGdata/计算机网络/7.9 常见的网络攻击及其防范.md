- 网络扫描用于收集攻击目标信息，网络监听用于截获链路中的通信内容，拒绝服务攻击通过漏洞或资源消耗使合法用户无法获得服务。
- 防护需要减少信息暴露、限制攻击面、加密并鉴别通信，以及持续检测异常流量。

==扫描和监听通常为后续攻击收集信息，拒绝服务攻击则直接破坏可用性；针对这些攻击不存在单一、绝对有效的防范手段。==

### 7.9.1 网络扫描

- 网络扫描是获取攻击目标信息的重要技术。
- 目标信息包括主机 IP 地址、操作系统、运行程序及存在的漏洞。
- 掌握的信息越全面具体，攻击者越容易制定攻击策略并提高成功率。

| 扫描类型 | 目的 | 典型方法 |
| --- | --- | --- |
| 主机发现 | 确定正在运行的目标主机及其 IP 地址 | 向一段地址发送 ICMP 探测报文 |
| 端口扫描 | 获取端口工作状态并判断开放的网络服务 | 尝试建立 TCP 连接或向 UDP 端口发送用户数据报 |
| 操作系统检测 | 判断目标操作系统类型 | 分析协议实现细节和响应特征 |
| 漏洞扫描 | 查找可利用的安全漏洞 | 综合端口状态和系统特征匹配漏洞库 |

![[7.9-网络扫描方法.png|900]]

- 端口扫描通过端口状态推断目标主机开放的网络服务，不同应用通常对应不同的运输层端口号。

#### 防范

- 关闭不需要的端口，关闭闲置和危险端口。
- 限制因特网中的主机主动访问内部网络中的主机。
- 配置防火墙过滤不必要的 ICMP 报文。
- 使用 IDS 发现扫描行为并对攻击者地址告警。
- 通过随机、低速或分布式方式实施的扫描较难被发现和阻断。

### 7.9.2 网络监听

- 网络中的数据大部分是明文形式，攻击者截获后可以直接分析其中的用户数据和控制信息。
- 分组嗅探器（Packet Sniffer）将主机网络适配器设置为混杂方式，接收并分析经过链路的所有分组。
- 在共享式以太网中，混杂方式的适配器可以收到发给其他主机的帧。
- 交换式以太网通常只把单播帧转发到目的端口，但[[7.1 网络安全概述#交换机中毒|交换机毒化攻击]]可以破坏其转发表，使其像集线器一样转发帧。
- 地址解析协议（Address Resolution Protocol，ARP）欺骗通过伪造 IP 地址与 MAC 地址的映射，使攻击者成为通信双方之间的中间人。

![[7.9-ARP欺骗.png|900]]

- 防范措施包括使用交换机替代集线器、禁用交换机自学习并静态绑定 IP 地址、MAC 地址与交换机端口、使用静态 ARP 表、划分 VLAN，以及对数据进行[[7.2.1 密码学相关基本概念#7.2.1.1 加密与解密|加密]]和[[7.4 实体鉴别|实体鉴别]]。
- ==加密和实体鉴别是防范网络监听最有效的办法；即使攻击者截获报文，也难以获得明文或伪造合法通信方。==

### 7.9.3 拒绝服务攻击

- 拒绝服务（Denial of Service，DoS）攻击使系统无法向合法用户提供正常服务。

#### 基于漏洞的 DoS

- 攻击者向目标发送特殊分组，使目标系统出现异常甚至崩溃。
- “死亡之 ping”通过超长 ICMP 回送请求触发存在漏洞的系统异常。
- 修复操作系统或协议实现漏洞可以防范对应攻击。

#### 基于资源消耗的 DoS

![[7.9-TCP-SYN洪泛攻击.png|900]]

- TCP SYN 洪泛攻击伪造大量源 IP 地址发送连接请求，服务器为半连接分配缓存等资源并等待不会到来的确认；半连接达到上限后，服务器无法接受合法连接。
- 反射攻击伪造受害者的源 IP 地址向反射节点发送请求，使反射节点把响应发送给受害者并耗尽其网络资源。

#### 分布式 DoS

- 分布式拒绝服务（Distributed Denial of Service，DDoS）攻击通过攻击命令驱使大量被控制的僵尸主机同时攻击目标。
- DDoS 的流量来源分散且规模巨大，目标很难仅靠屏蔽少量源地址抵御。

#### 防范

- 利用防火墙过滤异常分组，例如过滤畸形 ICMP 报文。
- TCP SYN 洪泛的源地址通常被伪造，单纯根据源地址过滤效果有限。
- 过滤反射攻击需要过滤外部进入网络且源地址属于本网络的分组。
- 限制分组的最大响应长度可以减弱基于响应放大的攻击。
- [[7.8 防火墙访问控制与入侵检测系统|防火墙和 IDS]] 可共同检测并缓解 DoS，但 DoS 是较易实施又较难彻底防范的攻击。
