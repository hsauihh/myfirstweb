- 计算机通过网络适配器接入以太网，网络适配器实现数据链路层和物理层功能。
- 媒体接入控制地址用于标识网络接口，使共享信道上的网卡能够判断帧是否发给自己。
- MAC 地址可以是单播地址、多播地址或广播地址，网卡按照目的地址决定接收或丢弃帧。

==MAC 地址标识的是网络接口而不是整台计算机；一台设备有多少个网络接口，就可能有多少个 MAC 地址。==

### 3.4.1.1 网络适配器

- 网络适配器（Adapter）通常称为网卡。
- 网卡与 CPU、存储器之间通过主板 I/O 总线进行[[2.3 传输方式#2.3.1 串行传输与并行传输|并行传输]]。
- 网卡与局域网之间通过传输媒体进行串行传输。
- 网卡实现串行传输与并行传输之间的转换，并使用缓存协调计算机与网络的速率差异。
- 计算机必须安装网卡驱动程序，才能正常使用网卡。

| 部件 | 作用 |
| --- | --- |
| EEPROM | 存储 MAC 地址和网卡相关信息 |
| BootROM 插槽 | 安装供无盘工作站启动使用的 BootROM 芯片 |
| 核心芯片 | 实现以太网的数据链路层和物理层功能 |
| 网络隔离变压器 | 隔离核心芯片与 RJ45 接口，增强抗干扰能力并保护核心芯片 |
| RJ45 网络接口 | 连接双绞线电缆 |

![[3.4.1-网络适配器结构.png|900]]

- EEPROM 保存接口的 MAC 地址，核心芯片完成协议处理，隔离变压器位于核心芯片与 RJ45 接口之间。

![[3.4.1-网络适配器功能.png|900]]

- 网卡在计算机内部处理并行数据，在网络侧处理串行数据，并完成数据链路层与物理层处理。

### 3.4.1.2 MAC 地址的结构

- 媒体接入控制地址（Media Access Control Address，MAC 地址）是数据链路层使用的地址。
- MAC 地址常称为物理地址或硬件地址，但它不是物理层地址。
- IEEE 802 局域网 MAC 地址长 `48 bit`，即 `6 B`。

| 字段 | 长度 | 分配方式 |
| --- | --- | --- |
| 组织唯一标识符（Organizationally Unique Identifier，OUI） | `3 B` | 由 IEEE 注册管理机构分配给厂商 |
| 网络接口标识符 | `3 B` | 由获得 OUI 的厂商自行分配 |

- MAC 地址通常写成六组十六进制数。
- 第一个字节的最低位 `b0` 是 I/G（Individual/Group）位：`0` 表示单播地址，`1` 表示多播地址。
- 第一个字节的次低位 `b1` 是 G/L（Global/Local）位：`0` 表示全球管理，`1` 表示本地管理。

| G/L 位 | I/G 位 | MAC 地址类型 |
| --- | --- | --- |
| `0` | `0` | 全球单播地址 |
| `0` | `1` | 全球多播地址 |
| `1` | `0` | 本地单播地址 |
| `1` | `1` | 本地多播地址 |

![[3.4.1-MAC地址分类位.png|900]]

- 全球单播地址通常由设备厂商写入网卡的 EEPROM。
- 全部 `48 bit` 都为 `1` 的地址 `FF-FF-FF-FF-FF-FF` 是广播地址。
- MAC 地址按第 1 字节到第 6 字节的顺序发送，每个字节内部按 `b0` 到 `b7` 的顺序发送。

![[3.4.1-MAC地址发送顺序.png|900]]

- 图中的发送顺序同时说明了 OUI、网络接口标识符和每个字节内最低位先发送的规则。

### 3.4.1.3 MAC 帧的接收规则

- 网卡收到无误码的帧后，先检查帧首部的目的 MAC 地址。

1. 目的地址为广播地址时，接收该帧。
2. 目的地址是与本接口相同的全球单播地址或本地单播地址时，接收该帧。
3. 目的地址是本接口已经加入的多播组地址时，接收该帧。
4. 其他情况下丢弃该帧。

![[3.4.1-单播帧接收.png|900]]

- 单播帧到达共享信道上的所有网卡，但只有目的 MAC 地址匹配的网卡接收。

![[3.4.1-广播帧接收.png|900]]

- 广播帧以 `FF-FF-FF-FF-FF-FF` 为目的地址，共享信道上的所有网卡都接收。

![[3.4.1-多播帧接收.png|900]]

- 多播帧只由已经加入相应多播组的接口接收；图中的网桥协议数据单元使用多播地址 `01-80-C2-00-00-00`。

![[3.4.1-MAC帧接收规则.png|900]]

- 工作在混杂方式（Promiscuous Mode）的网卡不检查目的 MAC 地址，而把收到的所有帧交给上层处理。
- 混杂方式可以用于网络管理、故障排查和分组分析，也可能被用于窃听网络通信。
