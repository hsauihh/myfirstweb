- 介质访问控制（Medium Access Control，MAC）用于协调多个站点对同一广播信道的使用。
- 它解决共享信道的使用权如何分配，以及发生竞争时如何处理碰撞的问题。
- 介质访问控制可以采用信道划分、随机访问或受控访问。

==随机访问允许站点竞争信道，可能发生碰撞；受控访问按规则授予发送权，正常工作时不会发生碰撞。==

### 3.10.1 介质访问控制的分类

| 分配方式 | 获得信道使用权的方式 | 碰撞 | 主要特点 | 典型方法 |
| --- | --- | --- | --- | --- |
| 信道划分 | 预先划分频率、时间、波长或码片资源 | 不发生 | 负载稳定时便于管理，突发通信时可能浪费已分配资源 | [[2.6 信道复用技术#2.6.2 频分复用\|频分复用]]、[[2.6 信道复用技术#2.6.3 时分复用\|时分复用]]、[[2.6 信道复用技术#2.6.4 波分复用\|波分复用]]、[[2.6 信道复用技术#2.6.5 码分复用\|码分复用]] |
| 随机访问 | 站点按协议自主竞争信道 | 可能发生 | 适合突发通信，需要确认、退避、碰撞检测或碰撞避免机制 | ALOHA、CSMA、[[3.4.2 CSMA-CD协议\|CSMA/CD]]、[[3.9.3 CSMA-CA协议\|CSMA/CA]] |
| 受控访问 | 轮询或令牌指定当前可发送的站点 | 正常工作时不发生 | 访问机会可控，但存在控制开销和控制机制故障风险 | 轮询、令牌传递 |

### 3.10.2 ALOHA 协议

- ALOHA 协议是一种随机访问协议，站点发送前不监听信道，而是通过确认、超时和随机退避处理碰撞。
- 纯 ALOHA 允许站点在任意时刻发送；时隙 ALOHA 把时间划分为等长时隙，并要求站点只能在时隙起点发送。

1. 站点有帧待发送时，纯 ALOHA 立即发送，时隙 ALOHA 等到下一个时隙起点发送。
2. 接收方正确收到帧后返回确认帧。
3. 发送方在规定时间内没有收到确认时，认为本次发送发生碰撞。
4. 纯 ALOHA 随机退避一段时间后重传；时隙 ALOHA 随机退避若干个完整时隙后，在时隙起点重传。

| 对比维度 | 纯 ALOHA | 时隙 ALOHA |
| --- | --- | --- |
| 发送时刻 | 任意时刻 | 时隙起点 |
| 时间同步 | 不需要 | 需要全网时隙同步 |
| 易受碰撞的时间 | $2T_0$ | $T_0$ |
| 吞吐量 | $S=Ge^{-2G}$ | $S=Ge^{-G}$ |
| 最大吞吐量 | $S_{\max}=\dfrac{1}{2e}\approx18.4\%$，此时 $G=\dfrac{1}{2}$ | $S_{\max}=\dfrac{1}{e}\approx36.8\%$，此时 $G=1$ |
| 主要代价 | 碰撞概率较高，信道利用率低 | 需要同步，但碰撞时间范围更小 |

- 变量：
  - $T_0$：发送一个定长帧所需的时间，单位为 `s`。
  - $G$：一个帧发送时间内产生的平均发送尝试次数，包括新帧和重传帧，无单位。
  - $S$：一个帧发送时间内成功发送的平均帧数，无单位。
- 适用条件：发送尝试相互独立并服从泊松分布，所有帧等长，任意重叠都会使相关帧碰撞失效。
- 纯 ALOHA 中，一个帧开始发送前后各 $T_0$ 内都不能有其他帧开始发送，因此易受碰撞的时间为 $2T_0$。
- 时隙 ALOHA 把发送起点限制在时隙边界，只有同一时隙内的发送会互相碰撞，因此易受碰撞的时间缩短为 $T_0$。

==时隙 ALOHA 以全网时间同步为代价，把易受碰撞的时间从 $2T_0$ 缩短到 $T_0$，最大吞吐量由约 $18.4\%$ 提高到约 $36.8\%$。==

### 3.10.3 CSMA 的 1-坚持、非坚持和 p-坚持策略

- 载波监听多址接入（Carrier Sense Multiple Access，CSMA）要求站点在发送前监听信道，以减少无谓碰撞。
- 传播时延可能使多个站点同时判断信道空闲，因此载波监听只能降低碰撞概率，不能消除碰撞。
- 三种策略的本质区别是：站点检测到信道忙或空闲后，何时以及以多大概率发送。

| 策略 | 检测到信道空闲 | 检测到信道忙 | 适用信道 | 主要取舍 |
| --- | --- | --- | --- | --- |
| 1-坚持 CSMA | 以概率 `1` 立即发送 | 持续监听，转为空闲后立即争用 | 连续时间或时隙信道 | 空闲时间少，但多个等待站可能同时发送并碰撞 |
| 非坚持 CSMA | 立即发送 | 随机等待一段时间后重新监听 | 连续时间信道 | 碰撞概率较低，但可能浪费空闲时间并增大发送时延 |
| p-坚持 CSMA | 在时隙起点以概率 $p$ 发送，以概率 $1-p$ 推迟一个时隙；信道仍空闲时重复判断 | 持续监听到信道空闲，再按概率 $p$ 决定是否发送 | 时隙信道 | 通过 $p$ 折中碰撞概率和信道利用率，但需要时隙同步 |

- 1-坚持中的“坚持”是指站点发现信道忙时不离开，而是持续监听；“1”表示信道空闲时的发送概率为 `1`。
- 非坚持站点发现信道忙后不持续监听，因此多个等待站同时在信道转为空闲时发送的概率较低。
- p-坚持中，$p$ 越大，站点越积极发送，空闲时间越少，但多个站点同时发送的概率越高。
- p-坚持中，$p=1$ 时退化为时隙信道上的 1-坚持 CSMA。
- 共享式以太网的[[3.4.2 CSMA-CD协议#3.4.2.1 CSMA/CD 的基本原理|CSMA/CD]]采用 1-坚持思想，并增加发送过程中的碰撞检测；[[3.9.3 CSMA-CA协议|CSMA/CA]]则通过碰撞避免机制降低无线信道中的碰撞概率。

==1-坚持在信道空闲时一定发送，非坚持在信道忙时随机等待后再监听，p-坚持在信道空闲时以概率 $p$ 发送。==

### 3.10.4 令牌环网络

- 经典令牌环局域网由 IEEE 802.5 标准定义，各站点在逻辑上组成一个环，帧沿固定方向逐站转发。
- 令牌是沿环循环的特殊控制帧，只有获得令牌的站点才能发送数据帧。
- 令牌传递属于受控访问，不依靠站点竞争信道，因此正常工作时不会发生碰撞。

1. 没有站点发送数据时，空闲令牌沿逻辑环依次经过各站点。
2. 有数据待发送的站点捕获令牌，在规定的令牌持有时间内发送数据帧。
3. 各中间站转发该帧；目的站识别目的地址后复制数据，并在帧中标记接收状态。
4. 数据帧绕环返回源站后，源站检查接收状态并从环中删除该帧。
5. 源站释放新的空闲令牌，使下一个站点获得发送机会。

- 目的站只复制数据帧而不将其从环中删除，==发送该帧的源站负责删除返回的数据帧==。
- 每个站点的令牌持有时间受到限制且环正常工作时，站点等待令牌的时间有上界，因而访问机会较公平。
- 活动监控站负责维持环的正常运行，例如检测令牌丢失、重复令牌和无法被删除而持续循环的帧。
- 令牌丢失时需要重新生成令牌；出现重复令牌时需要删除多余令牌。
- 站点或链路故障可能破坏逻辑环，需要旁路或监控机制恢复环路。
- **无数据发送的站点立即释放令牌**

| 对比维度 | 令牌传递 | CSMA 随机访问 |
| --- | --- | --- |
| 信道使用权 | 由唯一令牌授予 | 各站监听后竞争 |
| 碰撞 | 正常工作时无碰撞 | 传播时延可能导致碰撞 |
| 等待时间 | 令牌持有时间受限时有上界 | 退避次数不确定，等待时间通常无确定上界 |
| 低负载表现 | 仍需等待令牌循环，控制开销相对明显 | 站点可较快发送 |
| 高负载表现 | 无碰撞，访问较公平 | 碰撞和退避增多 |
| 维护要求 | 需要处理令牌和环路故障 | 需要处理碰撞和退避 |

==令牌环以令牌的循环开销和故障维护为代价，换取无碰撞、公平且等待时间可控的共享信道访问。==
