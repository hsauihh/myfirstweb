- 传输方式描述发送端如何把比特流送往接收端。
- 按照一次传输的比特数、字符或比特流的时序关系以及允许的数据流向，可以得到三组彼此独立的分类。
- 不同传输方式共同规定[[2.1 物理层概述|物理层]]如何在通信设备之间传送比特流。

==网络链路通常采用串行传输；同步传输连续发送比特，异步传输按字节独立同步；通信方向还可分为单工、半双工和全双工。==

### 2.3.1 串行传输与并行传输

| 对比维度 | 串行传输 | 并行传输 |
| --- | --- | --- |
| 一次传输的比特数 | 1 个 | 多个 |
| 数据传输线路 | 1 条 | 多条 |
| 相同单线速率下的传输效率 | 每个传输时刻发送 1 个比特 | 使用 $n$ 条线路时，传输效率是串行传输的 $n$ 倍 |
| 典型位置 | 计算机网络中的通信链路 | 计算机内部中央处理器、存储器与网卡之间 |

- 网卡在发送端把计算机内部的并行传输转换为网络链路上的串行传输，在接收端再完成相反转换。

![[2.3-串行传输与并行传输.png]]

- 串行传输在一条线路上依次传送各比特，并行传输使用多条线路同时传送多个比特。

### 2.3.2 同步传输与异步传输

| 对比维度 | 同步传输 | 异步传输 |
| --- | --- | --- |
| 传送方式 | 连续传送比特流 | 以字节为单位独立传送 |
| 字节间隔 | 字节之间连续 | 字节之间的时间间隔不固定 |
| 位同步 | 整个连续比特流都依靠同步时钟采样 | 每个字节内部各比特的持续时间相同 |
| 同步方法 | 外同步或内同步 | 每个字节使用起始位和结束位标明边界 |

#### 同步传输

- 接收端在每个信号的中间时刻对连续比特流进行采样。
- 收发双方的时钟频率若存在误差，采样时刻会逐渐偏移，因此必须保持时钟同步。
- 外同步是在收发双方之间增加一条时钟信号线。
- 内同步是把时钟同步信号编码进数据，与数据一起发送。

![[2.3-同步传输与时钟同步.png]]

- 连续比特流在采样时刻保持统一；外同步增加时钟信号线，内同步把时钟信息编码进数据。

#### 异步传输

- 每个字节独立完成同步，字节之间可以间隔不同的时间。
- 同一字节内部的各比特仍然保持相同的持续时间。
- 每个字节使用起始位和结束位，使接收端能够识别字节边界。

- 字节之间可以存在不固定的间隔；若没有边界标志，接收端不能直接判断每个字节的起止位置。

![[2.3-异步传输.png]]

- 起始位和结束位分别标明每个字节的边界，使各字节能够独立完成同步。

### 2.3.3 单工、半双工和全双工

| 对比维度 | 单向通信（单工） | 双向交替通信（半双工） | 双向同时通信（全双工） |
| --- | --- | --- | --- |
| 数据流向 | 只能由固定发送端流向固定接收端 | 两端都能发送和接收 | 两端都能发送和接收 |
| 能否同时双向传输 | 不能反向传输 | 不能，两个方向交替传输 | 能够同时双向传输 |
| 通信端角色 | 一端始终发送，另一端始终接收 | 每端在发送端和接收端角色之间切换 | 每端同时作为发送端和接收端 |
| 典型例子 | 无线电广播 | 对讲机 | 手机 |

![[2.3-三种通信方向.png]]

- 判断通信方向时，应同时检查数据能否双向传输以及两个方向能否在同一时刻传输。
