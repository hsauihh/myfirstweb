- 以太网 V2 MAC 帧是数据链路层的帧，用于封装上层协议数据单元并在以太网链路上传输。
- DIX Ethernet V2 与 IEEE 802.3 的帧格式只有“类型”字段存在差别。
- MAC 帧首部、尾部和数据载荷共同决定帧长，物理层还会在帧前添加前导码。

==以太网 V2 MAC 帧长度为 `64~1518 B`，其中数据载荷为 `46~1500 B`；物理层前导码不计入 MAC 帧长度。==

### 3.6.1 MAC 帧的字段

| 字段                              | 长度          | 作用                                     |
| ------------------------------- | ----------- | -------------------------------------- |
| 目的地址                            | `6 B`       | 指明接收该帧的网络接口                            |
| 源地址                             | `6 B`       | 指明发送该帧的网络接口                            |
| 类型                              | `2 B`       | 指明数据载荷由上层的哪一个协议封装                      |
| 数据载荷                            | `46~1500 B` | 承载上层协议数据单元                             |
| 帧检验序列（Frame Check Sequence，FCS） | `4 B`       | 使用[[3.2.2 差错检测\|循环冗余校验]]生成，用于检测传输中的误码  |

![[3.6-MAC帧字段与类型.png|900]]

- 类型字段取 `0x0800` 时，数据载荷是 TCP/IP 网络层 IP 协议封装的协议数据单元（Protocol Data Unit，PDU）。
- 接收方根据 FCS 的值检查帧在传输过程中是否产生了误码。

![[3.6-FCS差错检测.png|900]]

- FCS 是帧尾的差错检测字段，不属于上层协议数据载荷。

### 3.6.2 MAC 帧的长度限制

- MAC 帧长度由五个字段组成：
  - 公式：$L_{frame}=6\,\mathrm{B}+6\,\mathrm{B}+2\,\mathrm{B}+L_{payload}+4\,\mathrm{B}$
  - `L_frame`：MAC 帧长度，单位为 `B`。
  - `L_payload`：数据载荷长度，单位为 `B`。
  - 适用条件：计算以太网 V2 MAC 帧本身的长度，不包括物理层前导码。
  - 常见变形：$L_{payload}=L_{frame}-18\,\mathrm{B}$。
- 为满足[[3.4.2 CSMA-CD协议#3.4.2.3 最小帧长与最大帧长|以太网最小帧长]]要求，数据载荷至少为 `46 B`：`6+6+2+46+4=64 B`。

![[3.6-最小帧长.png|900]]

- 数据载荷小于 `46 B` 时，需要插入相应数量的填充字节，使 MAC 帧达到 `64 B`。

![[3.6-最大帧长.png|900]]

- 数据载荷最长为 `1500 B`，因此以太网 V2 MAC 帧最长为 `1518 B`。
- 适用范围：上述 `64~1518 B` 只计算 MAC 帧本身，不包括物理层前导码。

### 3.6.3 物理层前导码

- 物理层在发送 MAC 帧前添加 `8 B` 前导码，其中包括 `7 B` 前同步码和 `1 B` 帧开始定界符。
- 前同步码用于接收方进行比特同步，帧开始定界符标志 MAC 帧即将开始。

![[3.6-物理层前导码.png|900]]

- 图中前导码位于 MAC 帧之前，==不计入以太网 V2 MAC 帧的 `64~1518 B` 长度。==

### 3.6.4 无效 MAC 帧

- 接收方可能收到以下无效 MAC 帧：
  1. MAC 帧长度不是整数个字节。
  2. 通过 MAC 帧 FCS 字段的值检测出有误码。
  3. MAC 帧长度不在 `64~1518 B` 之间。
- 接收方将无效 MAC 帧直接丢弃，以太网数据链路层没有重传机制。

![[3.6-无效MAC帧.png|900]]

- 图中的三类无效帧都会被接收方直接丢弃，不会由以太网数据链路层请求重传。
