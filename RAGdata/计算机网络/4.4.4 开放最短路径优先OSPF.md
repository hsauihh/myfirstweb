- 开放最短路径优先（Open Shortest Path First，OSPF）是一种基于链路状态的内部网关协议。
- 每台路由器建立全网一致的链路状态数据库，并据此计算以自己为根的最短路径树。
- OSPF 使用链路代价作为度量，能够按网络规模划分区域。
- **由IP进行封装，属于网络层**

==OSPF 交换的是链路状态并让各路由器独立计算最短路径，不是直接把完整路由表发送给邻居。==

### 4.4.4.1 邻居关系与链路状态

- OSPF 链路状态包括路由器与哪些网络或路由器相邻，以及相应链路代价。
- OSPF 路由器周期性发送问候（Hello）分组发现和维护邻居关系。
- Hello 分组使用多播地址 `224.0.0.5`，典型发送周期为 `10 s`。
- 若连续 `40 s` 未收到邻居的 Hello 分组，则认为邻居不可达。
- OSPF 报文直接封装在 IP 数据报中，IP 首部协议字段值为 `89`。

### 4.4.4.2 链路状态数据库与最短路径

1. 路由器生成描述本地链路状态的链路状态通告（Link State Advertisement，LSA）。
2. LSA 通过==可靠的洪泛==发送给自治系统或区域中的其他路由器。
	- 洪泛法指路由器==向自己的所有邻居路由器发送链路状态更新分组==，收到该分组的各个路由器又将该分组转发给自己所有的邻居路由器（上游除外）。
	- 可靠是指链路状态更新分组后要发送确认，收到重复的更新分组无需再次转发，但需要发送一次确认。
3. 各路由器把收到的 LSA 写入链路状态数据库（Link State Database，LSDB）。
4. 同一区域中的路由器最终形成一致的 LSDB。
5. 每台路由器以自己为根，使用 Dijkstra 算法计算最短路径树并生成路由表。

![[4.4.4-OSPF最短路径计算.png]]

- 图中各路由器根据一致的 LSDB 分别计算以自己为根的最短路径树。

### 4.4.4.3 OSPF 的五种分组

| 分组类型 | 作用 |
| --- | --- |
| 问候分组 Hello | 发现并维护邻居关系 |
| 数据库描述分组 | 向邻居摘要说明本地 LSDB 中的链路状态项目 |
| 链路状态请求分组 | 请求缺少或需要更新的链路状态信息 |
| 链路状态更新分组 | 洪泛发送一个或多个 LSA |
| 链路状态确认分组 | 对收到的链路状态更新进行确认 |

![[4.4.4-OSPF五种分组.png]]

- 五种分组分别完成邻居发现、数据库同步、状态请求、状态传播与可靠确认。

### 4.4.4.4 OSPF 的基本工作过程

- 参与者：相邻的 OSPF 路由器。
- 触发条件：路由器启动、邻居关系发生变化、链路状态发生变化，或到达链路状态的周期更新时间。

1. 相邻路由器周期性交换 Hello 分组，建立并维护邻居关系。
2. 邻居关系建立后，路由器交换数据库描述分组，通告各自 LSDB 中链路状态项目的摘要信息。
3. 路由器发现本地缺少或需要更新的链路状态项目时，发送链路状态请求分组。
4. 邻居使用链路状态更新分组返回所请求的链路状态信息。
5. 路由器收到链路状态更新分组后，发送链路状态确认分组。
6. 各路由器重复请求、更新和确认，直至链路状态数据库达到一致。
7. 此后每隔 `30 min` 或链路状态发生变化时，路由器发送链路状态更新分组并接收确认，使链路状态数据库在新情况下重新达到一致。

![[4.4.4-OSPF基本工作过程.png|900]]

- 图中上半部分表示邻居建立与初始数据库同步，下半部分表示周期更新或链路状态变化后的重新同步。
- ==OSPF 先用 Hello 分组建立邻居关系，再通过数据库描述、链路状态请求、链路状态更新和链路状态确认分组使相邻路由器的 LSDB 达到一致。==

### 4.4.4.5 多路访问网络中的指定路由器

- 多台路由器连接在同一个多路访问网络时，两两建立邻接关系会产生大量状态交换。
- OSPF 选举指定路由器（Designated Router，DR）和备用指定路由器（Backup Designated Router，BDR）。
- 其他路由器主要与 DR 和 BDR 建立邻接关系，以减少邻接数量和链路状态交换量。
- DR 失效时，BDR 可以接替其职责。

![[4.4.4-OSPF指定路由器.png|900]]

- 通过 DR 和 BDR 汇集多路访问网络中的状态交换，可以避免所有路由器之间完全互连。
  
### 4.4.4.6 OSPF划分区域

- OSPF 把较大的自治系统划分为多个区域，每个区域使用一个 `32 bit` 区域标识符。
- 主干区域的标识符为 `0.0.0.0`，其他区域必须与主干区域连通。
- 区域内部保存本区域的详细链路状态，跨区域只交换必要的汇总信息。
- 区域划分可以减小 LSDB、最短路径计算量和链路状态更新流量。

![[4.4.4-OSPF区域.png|900]]

- 图中主干区域连接其他区域，区域内路由器、区域边界路由器和自治系统边界路由器承担不同范围的转发与信息交换任务。
