- 主机发送[[4.2 网际协议IP|网际协议（Internet Protocol，IP）]]数据报前，先判断目的主机是否与自己处于同一网络，以决定直接交付还是交给默认网关间接交付。
- 路由器收到 IP 数据报后，根据目的 IPv4 地址查询路由表，选择输出接口和下一跳。
- 数据报在路由器之间逐跳转发，直到到达目的网络并直接交付给目的主机。

==主机以自己的子网掩码判断目的地址是否在本地网络；不在本地网络的数据报必须先发送给默认网关。==

### 4.2.6.1 主机发送数据报

- 本地网络地址：$N_s=IP_s\mathbin{\&}M_s$。
- 目的地址对应的网络：$N_d=IP_d\mathbin{\&}M_s$。
- 变量：
  - $IP_s$：源接口的 IPv4 地址，长度为 `32 bit`。
  - $IP_d$：目的接口的 IPv4 地址，长度为 `32 bit`。
  - $M_s$：源接口配置的子网掩码，长度为 `32 bit`。
- 适用条件：源主机使用自己所在网络的子网掩码判断目的地址是否属于本地网络。

1. 若 $N_s=N_d$，源主机直接交付数据报，并用 ARP 获得目的主机的 MAC 地址。
2. 若 $N_s\ne N_d$，源主机把数据报交给默认网关，并用 ARP 获得默认网关接口的 MAC 地址。
3. **无论采用哪种交付方式，IP 数据报首部的目的 IPv4 地址始终是最终目的主机的地址。**

![[4.2.6-同网段判断.png|900]]

- 图中源主机把目的 IPv4 地址与本地子网掩码相与，再与自己的网络地址比较，从而选择直接交付或间接交付。

### 4.2.6.2 路由器转发数据报

- 路由表项目通常包含==目的网络、地址掩码、下一跳和输出接口==。
- 直连路由表示目的网络连接在路由器的某个接口上，不需要再经过其他路由器。
- 间接路由给出下一跳路由器，当前路由器只负责把数据报送到该下一跳。

1. 路由器从输入帧中取出 IP 数据报并检查首部。
2. 路由器把目的 IPv4 地址与各路由项目的掩码相与，寻找匹配的目的网络。
3. **多条路由同时匹配时，选择网络前缀最长的路由（最长前缀匹配）**。
4. 路由器确定下一跳和输出接口，并把生存时间 TTL 减 `1`。
5. 路由器使用输出链路的帧格式和下一跳 MAC 地址重新封装数据报。
6. 若没有匹配路由且没有默认路由，路由器丢弃数据报。

![[4.2.6-IP数据报间接交付.png|900]]

- 图中路由器根据目的网络选择下一跳；跨越路由器时帧首部改变，IP 数据报仍指向最终目的主机。

### 4.2.6.3 广播域边界

- 路由器隔离广播域，不转发普通链路层广播帧。
- 目的地址是某网络广播地址的 IP 数据报不会按普通单播方式跨路由器扩散。
- 隔离广播可以防止一个网络中的广播流量影响其他网络。
