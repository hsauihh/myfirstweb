- 每个 IPv6 地址占 `128 bit`，地址空间大小为 $2^{128}$。
- IPv6 地址通常使用冒号十六进制记法，并可通过省略左侧零和压缩连续零缩短表示。
- IPv6 数据报的目的地址有单播、多播和任播三种基本类型，不使用广播术语。

==IPv6 地址的完整二进制长度始终是 `128 bit`；文本压缩只改变写法，不改变地址值。==

### 4.9.4.1 地址空间

- IPv6 地址总数为：$2^{128}>3.4\times10^{38}$。

![[4.9.4-IPv6地址空间.png]]

- IPv6 的 `128 bit` 地址空间远大于 IPv4 的 `32 bit` 地址空间。

### 4.9.4.2 地址表示方法

- 冒号十六进制记法把 `128 bit` 地址分成 `8` 组，每组 `16 bit`，并把每组写成十六进制数，组间使用冒号分隔。
- 每组十六进制数最左侧连续的 `0` 可以省略，称为==左侧零省略==。
- 一串连续的全零组可以用一对冒号 `::` 取代，称为==连续零压缩==。
- ==一个 IPv6 地址中只能使用一次连续零压缩==，否则无法确定各段应补回多少个零。
- 冒号十六进制记法可以结合点分十进制后缀，便于 IPv4 向 IPv6 过渡。
- [[4.2.2 IPv4地址及其编址方法#4.2.2.4 无分类编址 CIDR|无分类域间路由选择（CIDR）斜线记法]]在 IPv6 中仍然可用。
![[4.9.4-IPv6冒号十六进制记法.png]]
![[4.9.4-IPv6地址压缩.png|900]]

- 图中不同位置的连续零都可以压缩，但每个地址只能选择其中一段使用 `::`。

### 4.9.4.3 三种目的地址基本类型

| 类型 | 交付范围 |
| --- | --- |
| 单播（Unicast） | 传统的点对点通信，数据报交付给一个接口 |
| 多播（Multicast） | 一点对多点通信，数据报交付给一组计算机中的每一个 |
| 任播（Anycast） | 终点是一组计算机，但数据报只交付给按路由算法得到的距离最近者 |

- IPv6 不采用广播术语，而把广播看作多播的一个特例。

![[4.9.4-IPv6三种地址基本类型.png|900]]

- 单播、多播和任播的区别在于同一数据报最终交付的接口数量和选择方式。

### 4.9.4.4 常用地址分类

| 地址 | 前缀或写法 | 作用 |
| --- | --- | --- |
| 未指明地址 | `::` | `128 bit` 全为 `0` 的地址 |
| 环回地址 | `::1` | 最低位为 `1`、其余 `127 bit` 为 `0`，用于本机环回 |
| 多播地址 | `FF00::/8` | 最高 `8 bit` 全为 `1`，用于多播通信 |
| 本地链路单播地址 | `FE80::/10` | 用于同一链路上的主机通信，不经过路由器转发 |
| 全球单播地址 | 三级结构 | 用于因特网中的单播通信 |

- IPv6 全球单播地址由 `48 bit` 全球路由选择前缀、`16 bit` 子网标识符和 `64 bit` 接口标识符组成。
- 全球路由选择前缀用于因特网路由选择，子网标识符用于组织内部划分子网，接口标识符用于标识单个网络接口。
- `64 bit` 接口标识符可以直接依据接口硬件地址编码，因此 IPv6 不使用 [[4.2.5 地址解析协议ARP|地址解析协议（ARP）]]；相关功能由 [[4.9.6 网际控制报文协议ICMPv6|网际控制报文协议（ICMPv6）]]承担。

![[4.9.4-IPv6常用地址分类.png]]
![[4.9.4-全球单播地址结构.png]]
- 全球单播地址的三级结构兼顾因特网路由、组织内部子网划分和接口标识。
