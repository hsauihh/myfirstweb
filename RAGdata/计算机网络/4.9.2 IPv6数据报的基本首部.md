- IPv6 数据报由固定 `40 B` 的基本首部和有效载荷组成。
- 基本首部只保留转发所需的八个字段，有效载荷由扩展首部和数据部分组成，最大长度为 `65535 B`。
- IPv6 基本首部与 [[4.2.7 IPv4数据报的首部格式|IPv4 数据报首部]]不兼容。

==IPv6 用固定长度、字段更少的基本首部降低路由器处理开销，把可选功能移入扩展首部。==

### 4.9.2.1 基本首部的简化

- IPv6 取消首部长度字段，因为基本首部长度固定为 `40 B`。
- IPv6 取消标识、标志和片偏移字段，把分片功能放入分片扩展首部。
- IPv6 取消首部检验和字段，减少路由器逐跳处理工作。
- IPv6 取消选项字段，改用 [[4.9.3 IPv6数据报的扩展首部|扩展首部]]实现选项功能。
![[4.9.2-IPv6数据报组成.png]]

![[4.9.2-IPv4与IPv6首部对比.png]]

- IPv4 首部长度可变且直接携带选项，IPv6 基本首部长度固定，选项被移到有效载荷中的扩展首部。

### 4.9.2.2 基本首部字段

| 字段 | 长度 | 作用 |
| --- | --- | --- |
| 版本 | `4 bit` | IPv6 的值为 `6` |
| 通信量类 | `8 bit` | 区分不同 IPv6 数据报的类别或优先级 |
| 流标号 | `20 bit` | 标识从特定源点到特定终点的一系列 IPv6 数据报 |
| 有效载荷长度 | `16 bit` | 以字节为单位表示基本首部之后的扩展首部和数据部分长度，最大为 `65535 B` |
| 下一个首部 | `8 bit` | 指明第一个扩展首部的类型；没有扩展首部时指明上层协议 |
| 跳数限制 | `8 bit` | 源点设置初值，每台路由器转发前减 `1`，减到 `0` 时丢弃数据报 |
| 源地址 | `128 bit` | 标识发送端的 IPv6 地址 |
| 目的地址 | `128 bit` | 标识接收端的 IPv6 地址 |

- “流”是从特定源点到特定终点的一系列 IPv6 数据报，同一个流中的数据报具有相同的流标号。
- 流标号用于支持资源预分配；传统非实时数据不需要流标号时，可以把该字段置为 `0`。
- 跳数限制与 IPv4 生存时间（Time To Live，TTL）字段作用相同，但名称更直接地反映其含义。

![[4.9.2-IPv6基本首部格式.png]]
![[4.9.2-IPv6有效载荷长度与跳数限制.png]]
![[4.9.2-IPv6下一个首部字段.png]]
![[4.9.2-IPv6基本首部与扩展首部.png]]

- 图中基本首部每行宽 `32 bit`，共 `10` 行，因此固定长度为 `40 B`；源地址和目的地址各占 `128 bit`。
