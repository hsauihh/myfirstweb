- 在因特网上进行 IP 多播需要网际组管理协议和多播路由选择协议共同工作。
- 网际组管理协议（Internet Group Management Protocol，IGMP）让本地多播路由器了解直连网络是否存在某个组的成员。
- 多播路由选择协议在路由器之间建立连接多播源与组成员的多播转发树。

==IGMP 只管理主机与本地多播路由器之间的成员关系；跨网络建立和维护转发树必须依靠多播路由选择协议。==

### 4.7.4.1 IGMP与多播路由选择协议

| 协议 | 工作范围 | 作用 |
| --- | --- | --- |
| IGMP | 主机与本地多播路由器之间 | 让路由器知道直连局域网上是否有主机加入或退出某个多播组 |
| 多播路由选择协议 | 多播路由器之间 | 为多播组建立连接源与成员所在路由器的多播转发树 |

- IGMP 只在本地网络有效，不能得知多播组的成员数量，也不能得知成员分布在哪些网络。
- 仅使用 IGMP 不能在因特网上完成 IP 多播，还需要多播路由器协同转发。

![[4.7.4-IGMP与多播路由协议.png|900]]

- IGMP 提供局域网成员状态，多播路由选择协议利用这些状态建立以较小代价覆盖成员的转发路径。

![[4.7.4-多播转发树.png|900]]

- 多播数据报只沿转发树跨路由器洪泛传送，到达成员所在局域网后再使用硬件多播交付。
- 针对**不同的多播组需要维护不同的多播转发树**，而且必须**动态地适应多播组成员的变化**，但此时网络拓扑并不一定发生变化，因此**多播路由选择协议要比单播路由选择协议（例如 RIP、OSPF 等）复杂得多**。
- 即使某个**主机不是任何多播组的成员**，它也可以向任何多播组发送多播数据报。
- 为了覆盖多播组的所有成员，**多播转发树可能要经过一些没有多播组成员的路由器**（例如 R2）。
  ![[4.7.4-因特网IP多播的两种协议.png]]
