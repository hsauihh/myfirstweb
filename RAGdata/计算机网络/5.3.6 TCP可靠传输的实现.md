  - TCP 基于以字节为单位的滑动窗口实现可靠传输。
- 发送方根据接收方通告的接收窗口 `rwnd` 构造发送窗口，并记录哪些字节已发送、已确认或仍可发送。
- 接收方对按序收到的最高字节序号进行累计确认，发送方对超时未确认的数据进行重传。

==TCP 的确认号表示期望收到的下一个字节序号，因而确认号之前的全部字节都已正确收到。==
![[5.3.6-确认号含义.png]]
### 5.3.6.1 发送窗口与接收窗口

- TCP 的滑动窗口以字节为单位。
- 发送方发送窗口的后沿把序号空间分为“已发送且已确认”和“已发送但未确认”。
- 发送方发送窗口的前沿把序号空间分为“允许发送”和“不允许发送”。
- 发送窗口后沿只能不动或前移；前沿通常前移或不动，也可能因窗口收缩而后移，但 TCP 强烈不赞成窗口收缩。
- 接收方接收窗口内的序号是当前允许接收的数据，窗口外的数据不允许接收。
![[5.3.6-发送窗口前后沿.png]]


| 指针 | 所指位置 | 区间含义 |
| --- | --- | --- |
| $P_1$ | 最早未确认字节 | 小于 $P_1$ 的字节均已发送并确认 |
| $P_2$ | 下一个待发送字节 | $P_1$ 到 $P_2-1$ 已发送但未确认 |
| $P_3$ | 发送窗口前沿后一位置 | $P_2$ 到 $P_3-1$ 允许发送但尚未发送 |

- 发送窗口大小为 $P_3-P_1$。
- 可用窗口大小为 $P_3-P_2$。

### 5.3.6.2 累计确认与窗口滑动

1. 接收方把确认号设置为期望收到的下一个字节序号。
2. 确认号之前的所有字节被累计确认。
3. 发送方收到确认后，将窗口后沿前移并删除已经确认的数据。
4. 接收方收到新的按序数据后，将接收窗口向前滑动，并通告新的确认号与 `rwnd`。
5. 发送方据此调整发送窗口前沿。

- ACK 报文段中的 `ack` 是确认号，与 TCP 首部的 ACK 标志位不是同一概念。
- 发送窗口中某个已用序号的数据迟迟未被确认时，发送方超时重传该数据。
- 已用序号若被新的累计确认覆盖，则不再触发重传。
![[5.3.6-TCP滑动窗口.png]]
![[5.3.6-累计确认与窗口滑动.png]]
![[5.3.6-发送窗口用尽.png]]

### 5.3.6.3 实现约束

- 同一时刻发送方的发送窗口与接收方的接收窗口不一定相同：接收窗口通告存在网络时延，接收窗口本身也会动态变化。
- 发送方还会根据网络拥塞程度缩小拥塞窗口，实际发送窗口为 $swnd=\min(cwnd,rwnd)$。
- TCP 通信是全双工通信，通信双方都分别维护自己的发送窗口和接收窗口。
- TCP 接收方只能按序向上层交付数据；对失序数据如何处理由实现决定。
- TCP 接收方采用累计确认，确认不必逐个报文段发送。
- 延迟确认的时间不得超过 `0.5 s`，以免造成发送方不必要的超时重传。
- 连续收到最大长度的报文段时，接收方至少每隔一个报文段发送一次确认。
- 接收方可以在自己发送数据时捎带确认，但多数应用进程很少同时在两个方向发送数据，因此捎带确认实际并不常见。

![[5.3.6-TCP可靠传输实现说明.png|900]]
 
