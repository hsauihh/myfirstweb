- 可靠传输服务通过确认、超时和重传等机制，恢复传输过程中出错或丢失的数据。
- 停止-等待协议每发送一个分组就等待确认，回退 N 帧协议和选择重传协议则使用滑动窗口实现流水线传输。
- 可靠传输不限于数据链路层，也可以由其他层次根据通信需求实现。

==停止-等待逐个确认，GBN 累计确认并成批回退，SR 逐帧确认并只重传缺失分组。==

### 3.2.3.1 可靠传输的基本概念

- [[3.2.2 差错检测#3.2.2.3 循环冗余校验|帧检验序列]]（Frame Check Sequence，FCS）中的检错码使接收方能够检测帧是否出现比特差错。
- 数据链路层可以提供两类传输服务：

| 服务类型 | 对出错帧的处理 | 服务目标 |
| --- | --- | --- |
| 不可靠传输服务 | 仅丢弃检测出误码的帧，不采取其他措施 | 不保证丢失的数据能够恢复 |
| 可靠传输服务 | 通过确认、超时和重传等机制恢复传输差错 | 发送方发送什么，接收方最终就能正确收到什么 |

- 有线链路的误码率通常较低，为减小开销，数据链路层一般不要求提供可靠传输服务。
- 无线链路容易受到干扰，误码率较高，数据链路层需要提供可靠传输服务。

![[3.2.3-可靠传输的实现层次.png|900]]

- TCP 和 802.11 无线局域网提供可靠传输，UDP、IP 和以太网提供不可靠传输，说明可靠性可以由不同层次实现。
- 传输差错包括分组重复、分组失序、分组丢失和误码。
- 分组重复、分组失序和分组丢失出现在数据链路层的上层；误码出现在数据链路层及其下层。

![[3.2.3-传输差错类型.png|900]]

- 可靠传输机制需要根据所在层次可能出现的差错类型采取相应处理。

### 3.2.3.2 停止-等待协议

- 停止-等待协议每发送一个数据分组就停止发送，等待接收方确认后再发送下一个分组。
- 数据分组、确认分组和否认分组都可能在链路中丢失或出错。

1. 发送方发送一个数据分组并启动重传计时器。
2. 接收方检测分组无误后接收该分组，并返回确认分组（Acknowledgement，ACK）。
3. 接收方检测分组有误后丢弃该分组，并返回否认分组（Negative Acknowledgement，NAK）。
4. 发送方收到 NAK 后重传前一个数据分组，收到 ACK 后发送下一个数据分组。
5. 数据分组或 ACK 丢失时，计时器超时，发送方重传尚未确认的数据分组。

![[3.2.3-停止等待差错重传.png|900]]

- NAK 可以使发送方在超时之前尽快重传；只使用超时重传也能实现可靠传输。
- ACK 丢失会使发送方重传已经正确到达的数据分组。
- 接收方需要给数据分组编号，以识别并丢弃重复分组，同时重新发送 ACK。
- 停止-等待协议只需交替使用序号 `0` 和 `1`，因此一个比特的序号字段即可满足要求。

![[3.2.3-停止等待确认丢失.png|900]]

- ACK 丢失后，接收方根据序号识别重传分组，不把同一数据重复交付给上层。
- 重传超时时间（Retransmission Timeout，RTO）一般应略大于收发双方的平均往返时间（Round-Trip Time，RTT）。
- 停止-等待协议属于自动重传请求（Automatic Repeat reQuest，ARQ）协议，重传由发送方自动进行。

#### 信道利用率

- 公式：$U\approx\dfrac{T_D}{T_D+RTT+T_A}$
- 变量：
  - $U$：信道利用率，无单位。
  - $T_D$：数据分组的发送时延，单位为 `s`。
  - $T_A$：确认分组的发送时延，单位为 `s`。
  - $RTT$：收发双方的往返时间，单位为 `s`。
- 适用条件：忽略处理时延，并且一次发送和确认过程没有发生重传。
- 当 $T_A\ll T_D$ 时，可近似为 $U\approx\dfrac{T_D}{T_D+RTT}$。

![[3.2.3-停止等待信道利用率.png|900]]

- 当 $RTT\gg T_D$ 时，大部分时间耗费在等待确认，停止-等待协议的信道利用率很低。

### 3.2.3.3 回退 N 帧协议

- 回退 N 帧（Go-Back-N，GBN）协议采用流水线传输，在收到确认前可以连续发送多个数据分组。
- 使用 $n$ 个比特给分组编号时，序号范围为 $0$ 到 $2^n-1$，并循环使用。
- GBN 的窗口满足 $1<W_T\leq2^n-1$、$W_R=1$：
  - $W_T$：发送窗口大小。
  - $W_R$：接收窗口大小。
  - $W_T=1$ 时退化为停止-等待协议。

![[3.2.3-回退N帧发送与接收窗口.png|900]]

1. 发送方连续发送当前发送窗口内的多个数据分组。
2. 接收方只接收无误且序号等于当前期望值的分组，并把接收窗口向前滑动一个位置。
3. 接收方返回累计确认；$ACK_n$ 表示序号 $n$ 及其之前按序到达的分组均已正确接收。
4. 发送方收到确认后，把发送窗口向前滑动到相应位置。
5. 接收方遇到失序分组时丢弃该分组，并可重复确认此前最后一个按序到达的分组。
6. 某个已发送分组超时后，发送方重传该分组及其后所有已发送但尚未确认的分组。

![[3.2.3-回退N帧超时重传.png|900]]

- 某个分组出错会使其后的正确分组也被丢弃，发送方从最早未确认分组开始“回退”并连续重传。
- 接收方可以对连续收到的多个分组发送一次累计确认，也可以在自己有数据发送时捎带确认。
- 发送窗口不能覆盖全部 $2^n$ 个序号，否则接收方可能无法区分旧分组和下一轮的新分组。

![[3.2.3-回退N帧窗口上限.png|900]]

- 使用三比特序号且 $W_T=8$ 时，所有 ACK 丢失后重传的旧分组 `0` 会与新一轮分组 `0` 混淆，因此必须满足 $W_T\leq2^n-1$。

![[3.2.3-回退N帧规则.png|900]]

- ==GBN 用累计确认和批量回退重传换取较简单的接收端；误码率较高时，大量无差错分组也会被重复传输。==

### 3.2.3.4 选择重传协议

- 选择重传（Selective Repeat，SR）协议允许接收方缓存接收窗口内失序但无误的数据分组，并对每个正确分组逐一确认。
- 发送方只重传超时且尚未确认的分组，不重传已经正确到达的后续分组。
- 使用 $n>1$ 个比特给分组编号时，窗口关系为：
  - $1<W_R\leq W_T$
  - $W_T+W_R\leq2^n$
  - 因而 $1<W_R\leq2^{n-1}$；当 $W_R=2^{n-1}$ 时，$W_T$ 的最大值也为 $2^{n-1}$。

![[3.2.3-选择重传规则.png|900]]

1. 发送方连续发送发送窗口内的多个分组，并分别记录其确认和超时状态。
2. 接收方接收序号落在接收窗口内且无误的分组，即使失序也先缓存，并逐一返回 ACK。
3. 发送方收到失序 ACK 时记录该确认；只有发送窗口下界分组及其连续后继均已确认时，窗口才能向前滑动。
4. 接收方只有在接收窗口下界分组到达后，才能把该分组及已连续缓存的后继分组按序交付，并向前滑动窗口。
5. 某个分组超时后，发送方只重传该分组。

![[3.2.3-选择重传失序缓存.png|900]]

- 序号 `0` 尚未到达时，接收方可以缓存并确认序号 `1`、`2`、`3`，但接收窗口不能越过缺失的序号 `0`。
- $W_T+W_R\leq2^n$ 保证接收窗口滑动后使用的新序号不会与上一轮仍可能出现的旧序号重叠。

### 3.2.3.5 三种协议的比较

![[3.2.3-协议窗口对比.png|900]]

- 三种协议的窗口关系决定了可同时在途的分组数量以及接收方是否能够缓存失序分组。

| 对比维度 | 停止-等待协议 | GBN | SR |
| --- | --- | --- | --- |
| 发送窗口 | $W_T=1$ | $1<W_T\leq2^n-1$ | $1<W_R\leq W_T$ 且 $W_T+W_R\leq2^n$ |
| 接收窗口 | $W_R=1$ | $W_R=1$ | $W_R>1$ |
| 确认方式 | 每次发送后等待确认 | 累计确认 | 逐帧确认 |
| 失序分组 | 不会同时接收多个在途分组 | 丢弃 | 在接收窗口内缓存 |
| 超时重传 | 重传当前分组 | 重传超时分组及其后所有未确认分组 | 只重传超时分组 |

- ==GBN 的本质是“接收方只收按序分组、发送方成批回退”，SR 的本质是“接收方缓存失序分组、发送方只重传缺失分组”。==
