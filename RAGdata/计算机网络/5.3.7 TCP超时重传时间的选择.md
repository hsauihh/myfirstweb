- TCP 超时重传时间（Retransmission Timeout，RTO）的选择是 TCP 最复杂的问题之一。
- RTO 过短会造成不必要的重传，增大网络负荷；RTO 过长会延迟丢失数据的恢复，降低传输效率。
- TCP 根据往返时间（Round-Trip Time，RTT）样本持续计算平滑 RTT 与 RTT 偏差，并据此动态更新 RTO。

==RTO 必须随路径时延变化动态调整，不能直接等于一次测量得到的 RTT，也不能采用固定值。==

### 5.3.7.1 RTO 的基本要求

- RTO 应略大于报文段的 RTT。
- TCP 报文段可能经过速率不同、负载不同且路由动态变化的网络，因此 RTT 会不断变化。
- 使用多个 RTT 样本计算加权平均值，可以得到较平滑的 RTT 估计。
![[5.3.7-RTO与RTT关系.png]]
### 5.3.7.2 加权平均 RTT 与偏差

- 首次测量时：$RTT_{S1}=RTT_1$。
- 后续测量时：$RTT_{S,\text{new}}=(1-\alpha)RTT_{S,\text{old}}+\alpha RTT_{\text{sample}}$。
- $RTT_S$：加权平均往返时间，单位为 `s`。
- $RTT_{\text{sample}}$：新的 RTT 样本，单位为 `s`。
- $0\leq\alpha<1$；RFC 6298 推荐 $\alpha=\frac18=0.125$。
- $\alpha$ 越接近 `0`，新样本影响越小；越接近 `1`，新样本影响越大。

- 首次测量时：$RTT_{D1}=\frac{RTT_1}{2}$。
- 后续测量时：$RTT_{D,\text{new}}=(1-\beta)RTT_{D,\text{old}}+\beta\left|RTT_{S,\text{old}}-RTT_{\text{sample}}\right|$。
- $RTT_D$：RTT 偏差的加权平均值，单位为 `s`。
- $0\leq\beta<1$；RFC 6298 推荐 $\beta=\frac14=0.25$。

- 超时重传时间：$RTO=RTT_S+4RTT_D$。
- 适用条件：按 RFC 6298 的 RTT 平滑估计计算当前 RTO。
- 获取新的 RTT 样本时，先使用更新前的 $RTT_S$ 计算新的 $RTT_D$，再计算新的 $RTT_S$ 和 RTO。
![[5.3.7-加权平均RTT.png]]
![[5.3.7-RTO计算公式.png]]
### 5.3.7.3 重传报文段的 RTT 样本歧义

- 报文段超时重传后收到确认，发送方无法判断该确认对应原报文段还是重传报文段。
- 把确认误认为原报文段的确认会得到偏大的 RTT 样本。
- 把确认误认为重传报文段的确认会得到偏小的 RTT 样本。
- 错误的 RTT 样本会使后续 RTO 计算失真。

![[5.3.7-RTO估计歧义.png]]

![[5.3.7-Karn算法.png]]

### 5.3.7.4 Karn 算法（不考）

1. 报文段发生重传后，不采用该报文段的 RTT 样本更新 $RTT_S$。
2. 每发生一次超时重传，把 RTO 增大为原来的 `2` 倍。
3. 后续收到未重传报文段的有效确认后，再使用其 RTT 样本更新估计值。

#### 计算例题

- 已知：前四次 RTT 样本依次为 $40\ \mathrm{ms}$、$8\ \mathrm{ms}$、$20\ \mathrm{ms}$ 和 $50\ \mathrm{ms}$，第五次测量时发生超时重传；$\alpha=0.125$，$\beta=0.25$。
- 求：每次测量后使用的超时重传时间 RTO。
- 计算过程：
  1. 首次测量：$RTT_{S1}=40\ \mathrm{ms}$，$RTT_{D1}=20\ \mathrm{ms}$，因此 $RTO_1=40+4\times20=120\ \mathrm{ms}$。
  2. 第二次测量：$RTT_{D2}=0.75\times20+0.25\times|40-8|=23\ \mathrm{ms}$，$RTT_{S2}=0.875\times40+0.125\times8=36\ \mathrm{ms}$，因此 $RTO_2=36+4\times23=128\ \mathrm{ms}$。
  3. 第三次测量：$RTT_{D3}=0.75\times23+0.25\times|36-20|=21.25\ \mathrm{ms}$，$RTT_{S3}=0.875\times36+0.125\times20=34\ \mathrm{ms}$，因此 $RTO_3=34+4\times21.25=119\ \mathrm{ms}$。
  4. 第四次测量：$RTT_{D4}=0.75\times21.25+0.25\times|34-50|=19.9375\ \mathrm{ms}$，$RTT_{S4}=0.875\times34+0.125\times50=36\ \mathrm{ms}$，因此 $RTO_4=36+4\times19.9375=115.75\ \mathrm{ms}$。
  5. 第五次发生超时重传，不计算 $RTT_{S5}$ 和 $RTT_{D5}$；按 Karn 算法进行超时退避，$RTO_5=2RTO_4=231.5\ \mathrm{ms}$。
- 结论：$RTO_1=120\ \mathrm{ms}$，$RTO_2=128\ \mathrm{ms}$，$RTO_3=119\ \mathrm{ms}$，$RTO_4=115.75\ \mathrm{ms}$，$RTO_5=231.5\ \mathrm{ms}$。

![[5.3.7-RTO计算例题.png|900]]

- 图中前四个有效 RTT 样本用于更新估计值，第五次发生超时重传后不再采样 RTT，而是把 RTO 加倍。
- Karn 算法跳过具有歧义的 RTT 样本，并通过超时退避适应路径时延突然增大的情况。
