- TCP 报文段由首部和数据载荷组成，首部承载端口、序号、确认、流量控制、差错检测和连接控制等信息。
- TCP 首部的固定部分为 `20 B`，扩展首部最多为 `40 B`，因此首部总长度为 `20~60 B`。
- TCP 依靠首部字段在面向字节流的基础上实现[[5.2 UDP和TCP的对比#5.2.3 数据传输可靠性|可靠传输]]，并支持连接管理和流量控制。

==TCP 的序号按字节编号，确认号指出期望收到的下一个字节；确认号之前的所有字节均被累计确认。==

### 5.3.1.1 TCP 报文段的组成

- TCP 从发送缓存中取出一段字节流，为其添加首部后形成 TCP 报文段。
- TCP 的全部功能都依靠首部中的相应字段实现。

![[5.3.1-TCP字节流与首部作用.png|900]]

- 图中应用进程产生的连续字节流被 TCP 分段传送，每个报文段都带有 TCP 首部。

| 字段 | 长度 | 作用 |
| --- | --- | --- |
| 源端口 | `16 bit` | 标识发送方应用进程使用的端口 |
| 目的端口 | `16 bit` | 标识接收方应用进程使用的端口 |
| 序号 | `32 bit` | 指出本报文段数据载荷第一个字节的序号 |
| 确认号 | `32 bit` | 指出期望收到对方下一个报文段数据载荷第一个字节的序号 |
| 数据偏移 | `4 bit` | 以 `4 B` 为单位指出数据载荷相对报文段起点的偏移，即 TCP 首部长度 |
| 保留 | `6 bit` | 保留供今后使用，置为 `0` |
| URG、ACK、PSH、RST、SYN、FIN | 各 `1 bit` | 控制报文段的确认、连接和数据处理方式 |
| 窗口 | `16 bit` | 以字节为单位通告发送方的接收窗口大小 |
| 检验和 | `16 bit` | 检验伪首部、TCP 首部和数据载荷 |
| 紧急指针 | `16 bit` | 在 `URG=1` 时指出紧急数据末尾相对本报文段序号的偏移量 |
| 选项 | `0~40 B` | 承载最大报文段长度、窗口扩大、时间戳和选择确认等参数 |
| 填充 | 可变 | 用比特 `0` 把首部总长度补齐为 `4 B` 的整数倍 |

![[5.3.1-TCP首部结构.png|900]]

- 图中首部按每行 `32 bit` 排列，固定部分共 `5` 行，即 `20 B`；选项和填充共同构成扩展首部。
- 源端口和目的端口的含义见[[5.1.3 运输层端口号、复用与分用的概念#5.1.3.2 发送方复用与接收方分用|运输层复用与分用]]。

### 5.3.1.2 序号与确认号

- 序号和确认号的取值范围均为 $0\sim 2^{32}-1$；达到最大值后的下一个值回到 `0`（序号绕回）。
- 序号字段只对本报文段的数据载荷编号，TCP 首部不占用这些字节序号。
- 若报文段第一个数据字节的序号为 $seq$，数据载荷长度为 $L$，则最后一个数据字节序号为 $seq+L-1$，后续连续报文段的序号为 $seq+L$。
- 确认号 $ack$ 表示对方期望接收的下一个字节序号，也表示序号小于 $ack$ 的字节已经正确收到。
- 只有 `ACK=1` 时确认号字段才有效；TCP 连接建立后传送的所有报文段都必须把 ACK 置为 `1`。

![[5.3.1-序号与确认号定义.png|900]]

- 图中序号和确认号均占 `32 bit`，ACK 标志位决定确认号字段是否有效。

#### 双向序号的独立性

- TCP 是全双工通信，两个方向分别使用各自的字节序号空间。
- 一方收到 `seq=1913`、数据长度为 `100 B` 的报文段后，向对方发送的确认号应为 $1913+100=2013$。
- 同一报文段中的 `ack=2046` 表示对方已经确认本方序号 `2046` 之前的所有字节，因此本方下一段数据可以从序号 `2046` 开始。

![[5.3.1-序号确认号实例.png|900]]

- 图中两个方向的序号分别由对端报文段的确认号推进，不能把同一报文段的序号与确认号直接相加。

#### 累计确认

- 三个连续报文段的数据长度依次为 `300 B`、`400 B` 和 `500 B`，第三个报文段序号为 `900` 时，前两个报文段的序号分别为 `200` 和 `500`。
- 接收方只正确收到第一、第三个报文段而第二个报文段丢失时，确认号仍为 `500`。
- 确认号不能越过尚未收到的字节，因此失序收到第三个报文段不会把累计确认推进到 `1400`。

![[5.3.1-累计确认实例.png|900]]

- 图中虚线确认指向最后一个按序到达的报文段之后，缺失的第二个报文段使确认号停留在 `500`。

### 5.3.1.3 数据偏移与窗口

#### 数据偏移

- 数据偏移字段占 `4 bit`，取值以 `4 B` 为单位。
- TCP 首部长度为：$H=DataOffset\times4\ \mathrm{B}$。
- 数据偏移的最小值为 `5`，对应固定首部长度 $5\times4\ \mathrm{B}=20\ \mathrm{B}$。
- 数据偏移的最大值为 `15`，对应最大首部长度 $15\times4\ \mathrm{B}=60\ \mathrm{B}$。
- 例如数据偏移为 $(0101)_2=5$ 时，数据载荷从报文段起点之后 `20 B` 处开始。

![[5.3.1-数据偏移.png]]

- 图中数据偏移值 `0101` 乘以单位 `4 B` 得到 `20 B`，也就是该报文段的首部长度。

#### 窗口

- 窗口字段占 `16 bit`，取值以字节为单位。
- 发送方在窗口字段中通告自己的接收窗口大小，即接收缓存的可用空间。
- 对方依据该值控制发送量，使发送速率适应接收方的接收能力，这就是[[5.3.3 TCP的流量控制|TCP 流量控制]]。

![[5.3.1-窗口字段.png|900]]

- 图中窗口值由报文段发送方填写，用来限制接收该报文段一方随后可以发送的数据量。

### 5.3.1.4 检验和

- TCP 检验和不仅覆盖 TCP 首部，还覆盖数据载荷以及计算时临时添加的 `12 B` IPv4 伪首部。
- IPv4 伪首部由源 IPv4 地址、目的 IPv4 地址、全 `0` 保留字节、值为 `6` 的协议字段和 TCP 报文段长度组成。
- 伪首部只参与检验和计算，不作为 TCP 报文段的一部分传送。
- TCP 与[[4.2.7 IPv4数据报的首部格式#4.2.7.4 首部检验和|IPv4 首部检验和]]使用相同的反码算术；区别是 IPv4 只检验自身首部，TCP 检验整个 TCP 报文段及伪首部。
![[5.3.1-首部检验和.png]]

发送方计算检验和：

1. 把 TCP 首部中的检验和字段置为 `0`。
2. 在 TCP 报文段前添加 IPv4 伪首部。
3. 把伪首部、TCP 首部和数据载荷划分为若干个 `16 bit` 字；总字节数为奇数时，在末尾补一个全 `0` 字节。
4. 对所有 `16 bit` 字进行反码算术求和，再将求和结果逐位取反。
5. 把结果写入 TCP 首部的检验和字段。
![[5.3.1-发送方检验和计算.png]]
接收方检验报文段：

1. 在收到的 TCP 报文段前添加12字节的   伪首部。
2. 把伪首部、TCP 首部和数据载荷划分为若干个 `16 bit` 字。
3. 对所有字进行反码算术求和，再将结果逐位取反。
4. 结果为 `0` 时认为未检测出误码，否则认为报文段存在误码。

![[5.3.1-接收方检验和检验.png]]

- 图中伪首部把 IPv4 源地址、目的地址、上层协议值和 TCP 长度纳入检验，接收方以最终结果是否为 `0` 判断是否检出误码。

### 5.3.1.5 控制位与紧急指针

| 控制位 | 置 `1` 时的含义 |
| --- | --- |
| ACK | 确认号字段有效；连接建立后的所有报文段都必须置 `1` |
| SYN | 用于建立连接；`SYN=1, ACK=0` 表示连接请求，`SYN=1, ACK=1` 表示连接响应 |
| FIN | 发送方数据已经发送完毕，并请求释放 TCP 连接 |
| RST | 复位 TCP 连接；用于严重差错后释放并重建连接，也用于拒绝非法报文段或拒绝打开连接 |
| PSH | 发送方立即封装并发送现有数据，接收方尽快把数据交付应用进程 |
| URG | 紧急指针字段有效 |

![[5.3.1-SYN控制位.png|900]]

- SYN 为 `1` 的报文段用于连接请求或连接响应，是否同时置 ACK 区分这两种用途。

![[5.3.1-FIN控制位.png|900]]

- FIN 用于正常释放连接，表示发送方的数据已经发送完毕并请求释放连接。

![[5.3.1-RST控制位.png|900]]

- RST 用于复位出现严重差错的连接，也可拒绝非法报文段或拒绝打开连接；它与用于正常释放连接的 FIN 含义不同。

![[5.3.1-PSH控制位.png|900]]

- PSH 改变数据发送和提交应用进程的时机，使交互式通信的一端可以更快收到响应。

![[5.3.1-URG与紧急指针.png|900]]

- URG 使紧急指针有效；紧急指针以字节为单位指出紧急数据末尾相对本报文段序号的偏移量。
- 发送方有紧急数据时，可以把紧急数据插入发送缓存最前面并立即封装发送。
- 接收方收到 `URG=1` 的报文段后，根据紧急指针取出紧急数据并直接交付应用进程，不必在接收缓存中排队。

### 5.3.1.6 选项与填充

- 选项字段长度可变，最长为 `40 B`。
- 最大报文段长度（Maximum Segment Size，MSS）选项指出 TCP 报文段数据载荷部分的最大长度，不是整个 TCP 报文段的长度。
- 窗口扩大选项用于扩大窗口，提高吞吐率。
- 时间戳选项用于计算往返时间（Round-Trip Time，RTT），也用于处理序号超范围后的绕回问题，即防止序号绕回（Protection Against Wrapped Sequence numbers，PAWS）。
- [[5.3.8 TCP的选择确认#5.3.8.1 SACK 选项|选择确认 SACK 选项]]用于实现选择确认功能。
- 当固定首部与选项的总长度不能被 `4 B` 整除时，填充字段补入相应数量的比特 `0`，使首部长度成为 `4 B` 的整数倍。
![[5.3.1-选项字段.png]]
![[5.3.1-选项与填充.png|900]]

- 图中选项使首部在 `20~60 B` 之间变化，数据偏移按 `4 B` 计量，因此填充字段必须把首部补齐到 `4 B` 的整数倍。
