- 实体鉴别是通信双方的一方验证另一方身份的技术，简称鉴别。
- 被鉴别的实体可以是人、客户进程或服务器进程。
- 通信实体的鉴别通常在传输实际数据或进行[[7.6 访问控制|访问控制]]之前完成，用于确认通信对方不是伪装者。

==实体鉴别不仅要验证对方能够证明自己的身份，还要保证证明过程具有新鲜性；不重数可以使接收方区分新的鉴别请求与被重放的旧请求。==

### 7.4.1 实体鉴别的作用

- 实体鉴别验证通信对方确实是预期实体，而不是其他伪装者。
- 实体鉴别是许多安全协议的重要组成部分。
- 双向鉴别要求通信双方相互验证身份。

### 7.4.2 用户名和口令

- 使用用户名和口令是最简单的实体鉴别方法。
- 直接传送明文用户名和口令时，攻击者截获报文后即可获得鉴别凭据。
- 加密用户名和口令可以防止攻击者直接读出凭据，但固定的加密报文仍可能被截获并在以后重放。

![[7.4-用户名口令截获风险.png|900]]

- 图中攻击者可以截获 A 发往 B 的加密用户名和口令；若该报文每次都相同，攻击者即使不能解密，也可能重放它来冒充 A。

### 7.4.3 不重数与对称密钥双向鉴别

- 不重数（nonce）是只使用一次的大随机数。
- 每次会话使用不同的不重数，可以把新的实体鉴别请求与重复的旧请求区分开，从而抵御重放攻击。
- A 和 B 预先共享对称密钥 `K_AB`。

1. A 随机选择不重数 `R_A`，把自己的用户名和 `R_A` 发送给 B。
2. B 随机选择不重数 `R_B`，把 `R_B` 和用 `K_AB` 加密的 `R_A` 发送给 A。
3. A 用 `K_AB` 解密得到 `R_A`，由此确认 B 掌握共享密钥并验证 B 的身份。
4. A 用 `K_AB` 加密 `R_B` 并发送给 B。
5. B 解密得到 `R_B`，由此确认 A 掌握共享密钥并验证 A 的身份。

![[7.4-对称密钥双向鉴别.png|900]]

- 图中双方分别验证对方对本次不重数的正确加密结果；旧会话中截获的应答无法通过新不重数的验证。

### 7.4.4 公钥双向鉴别

- 使用[[7.2.3 公钥密码体制|公钥密码体制]]也可以实现实体鉴别。
- `SK_A` 和 `SK_B` 分别是 A 和 B 的私钥，`PK_A` 和 `PK_B` 分别是对应的公钥。

1. A 随机选择不重数 `R_A`，把自己的用户名和 `R_A` 发送给 B。
2. B 随机选择不重数 `R_B`，使用私钥 `SK_B` 对 `R_A` 签名，并把 `R_B` 和签名发送给 A。
3. A 使用 B 的公钥 `PK_B` 核验签名中的 `R_A`，从而验证 B 的身份。
4. A 使用私钥 `SK_A` 对 `R_B` 签名并发送给 B。
5. B 使用 A 的公钥 `PK_A` 核验签名中的 `R_B`，从而验证 A 的身份。

![[7.4-公钥双向鉴别.png|900]]

- 图中私钥用于生成[[7.3.2 数字签名|数字签名]]以证明签名者的身份，公钥用于核验签名；双向鉴别成立的前提是双方取得的公钥确实属于所声称的实体。

### 7.4.5 公钥替换与中间人攻击

- 仅能核验签名与某个公钥匹配，并不能自动证明该公钥属于所声称的实体。
- 攻击者 C 位于 A 与 B 之间时，可以向 B 冒充 A，并把自己的公钥 `PK_C` 当作 A 的公钥发送给 B。
- B 使用 `PK_C` 验证 C 生成的签名后，会错误地把 C 识别为 A。
- B 随后使用 `PK_C` 加密发给 A 的数据，C 可以用自己的私钥解密，再使用 A 的真实公钥 `PK_A` 加密并转发给 A。

![[7.4-公钥替换中间人攻击.png|900]]

- 图中 C 通过替换公钥并分别与 A、B 交互，实现居中冒充和转发；防止这种攻击必须通过[[7.5 密钥分发#7.5.3 公钥的分发|可信的公钥分发机制]]验证公钥与实体身份之间的绑定关系。
