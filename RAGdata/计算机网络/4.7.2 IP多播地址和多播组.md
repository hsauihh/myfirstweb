- IPv4 多播地址用于标识一组接收者，不标识某一台具体主机。
- 每个多播地址对应一个动态多播组，主机可以随时加入或离开。
- IP 多播数据报采用尽最大努力交付，不保证交付给组内所有成员。

==IPv4 多播地址只能作为目的地址；它标识的是动态变化的接收者集合，而不是固定网络接口。==

### 4.7.2.1 IPv4多播地址与多播组

- IPv4 使用 D 类地址作为多播地址，前 `4 bit` 固定为 `1110`，地址范围为 `224.0.0.0~239.255.255.255`。
- 多播地址只能用作目的地址，不能用作源地址。
- 每个 IPv4 多播地址标识一个多播组；接收发送到同一多播地址的数据报的主机构成该组。
- 多播组成员可以随时加入或离开，一台主机也可以同时属于多个多播组。
- 多播组成员的数量和地理位置不受限制。
- 非多播组成员也可以向该组发送 IP 多播数据报。
- IP 多播数据报采用尽最大努力交付，不保证交付给组内所有成员。

![[4.7.2-IPv4多播地址与多播组.png|900]]

- 多播地址标识动态变化的一组接收进程，而不是一个固定网络接口。

### 4.7.2.2 IPv4多播地址的作用域

| 地址范围或地址 | 用途 |
| --- | --- |
| `224.0.0.0` | 基地址，保留 |
| `224.0.0.1` | 本子网上所有参加多播的主机和路由器 |
| `224.0.0.2` | 本子网上所有参加多播的路由器 |
| `224.0.0.3` | 未指派 |
| `224.0.0.4` | 距离向量多播路由选择协议（DVMRP）路由器 |
| `224.0.0.5` | 开放最短路径优先（OSPF）路由器 |
| `224.0.1.0~238.255.255.255` | 全球范围内可使用的多播地址 |
| `239.0.0.0~239.255.255.255` | 本地管理的多播地址，仅在特定本地范围内有效 |

![[4.7.2-IPv4多播地址作用域.png|900]]

- 永久多播地址用于预先规定的多播对象，其余地址按全球或本地管理范围使用。
