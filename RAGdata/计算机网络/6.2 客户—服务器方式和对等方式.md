- 网络应用中的应用进程按照组织和通信方式，可以采用客户—服务器（Client/Server，C/S）方式或对等（Peer-to-Peer，P2P）方式。
- C/S 方式描述服务请求者与服务提供者之间的关系，P2P 方式则让对等方兼具这两种角色。

==C/S 方式具有固定的服务请求方和提供方；P2P 方式没有固定角色，每个对等方既请求服务又提供服务。==

### 6.2.1 客户—服务器方式

- 客户和服务器是通信中涉及的两个应用进程。
- 客户是服务请求方，服务器是服务提供方。 
- **服务器始终处于运行状态并等待客户请求**，通常具有固定端口号，运行服务器的主机也具有固定 IP 地址。
- 万维网、电子邮件和文件传输等应用采用 C/S 方式。
- 基于 C/S 方式的应用是服务集中型的，服务器故障会影响大量客户，客户数量过多也会使服务器过载。
- 可以使用服务器集群构建强大的虚拟服务器，以提高服务能力。

![[6.2-客户服务器方式.png|900]]

- 客户进程主动发出服务请求，持续运行的服务器进程接收请求并提供服务。

### 6.2.2 对等方式

- P2P 方式中不存在固定的服务请求者和服务提供者。
- **对等方相互之间直接通信**，每个对等方既是服务请求者，又是服务提供者。
- P2P 文件共享、即时通信、P2P 流媒体和分布式存储属于典型应用。
- P2P 应用是服务分散型的，服务分布在大量对等方中。
- 随着对等方增加，系统服务能力也随之增强，因此具有较好的可扩展性。
- 系统性能不会因某个对等方故障而整体失效，但同时在线的对等方越多，占用的网络资源也越多。
- P2P 方式仍可使用服务器进行对等方查找等辅助工作。

![[6.2-对等方式.png|900]]

- 图中多个边缘主机直接通信，各对等方同时承担请求和提供服务的角色。

### 6.2.3 两种方式的对比
![[6.2-客户服务器与对等方式对比.png]]

| 对比维度 | 客户—服务器方式 | 对等方式 |
| --- | --- | --- |
| 进程角色 | 客户请求服务，服务器提供服务 | 每个对等方既请求服务又提供服务 |
| 服务分布 | 集中在少数服务器 | 分散在大量对等方 |
| 可扩展性 | 受服务器处理能力限制 | 对等方增加时服务能力通常增强 |
| 故障影响 | 服务器故障可能影响大量客户 | 单个对等方故障通常不使系统整体失效 |
| 资源占用 | 服务主要占用服务器资源 | 对等方越多，占用的网络资源越多 |
