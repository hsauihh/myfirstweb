- 文件传送协议（File Transfer Protocol，FTP）是因特网上使用最广泛的文件传送协议之一。
- FTP 提供交互式访问，允许客户指定文件类型与格式，并提供访问权限控制。
- FTP 屏蔽不同计算机系统之间的文件存储方式和字符编码差异，使异构主机能够传送文件。

==FTP 在整个会话期间保持一条控制连接，并为每次文件传输另建一条数据连接。==
==FTP端口号为20/21，建立在TCP之上==
### 6.5.1 FTP 的应用

- FTP 采用客户—服务器方式。
- 用户通过 FTP 客户连接 FTP 服务器，浏览目录并上传或下载文件。
- FTP 常用于计算机之间传送文件，尤其适合批量传送。

### 6.5.2 FTP 的双连接

- FTP 使用两条作用不同的 TCP 连接：
	- 控制连接负责会话控制
	- 数据连接负责传送文件。

#### 6.5.2.1 控制连接

- 客户使用随机临时端口，向服务器熟知端口 `21` 发起**控制连接**。
- **控制连接在整个会话期间保持打开**，用于传送 FTP 控制命令和服务器应答。
- 客户通过控制连接协商或通知数据连接所需的端口和连接方式。

#### 6.5.2.2 数据连接

- 需要传送文件时，客户和服务器根据控制连接中的信息建立数据连接。
- **数据连接只在本次文件传输期间保持打开**，传输结束后关闭。
- 下一次文件传输需要重新建立数据连接；控制连接不需要重复建立。

#### 6.5.2.3 主动方式与被动方式

| 对比维度     | 主动方式                            | 被动方式                         |
| -------- | ------------------------------- | ---------------------------- |
| 数据连接的发起方 | 服务器发起                           | 客户发起                         |
| 服务器端口    | 使用熟知端口 `20`建立数据连接               | 使用服务器临时端口                    |
| 客户端端口    | 使用客户指定的临时端口                     | 使用客户临时端口连接服务器临时端口            |
| 建立过程     | 客户通过控制连接告知临时端口，服务器从端口 `20` 连接客户 | 服务器通过控制连接告知临时端口，客户连接服务器该临时端口 |
- 即是否使用TCP 20端口建立数据连接与传输模式有关，主动方式使用TCP 20端口，被动方式由服务器和客户端协商决定。
- 无论采用哪种数据连接方式，控制连接都由客户连接服务器的端口 `21`。
- 控制连接和数据连接都使用[[5.1.2 TCP-IP体系结构运输层中的两个重要协议#5.1.2.1 传输控制协议 TCP|传输控制协议 TCP]]。

![[6.5-FTP双连接.png]]


