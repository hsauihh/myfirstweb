- 数字签名是发送方使用自己的私钥对报文进行的密码运算，用于证明报文来源并支持不可否认性。
- 接收方使用发送方的公钥核验数字签名，确认报文确由相应私钥持有者签发。

==私钥用于生成数字签名，公钥用于核验数字签名；数字签名本身不保证报文保密，保密性需要再使用接收方公钥加密。==

### 7.3.2.1 数字签名的要求

- 数字签名（Digital Signature）用于证明网络中传送报文的真实来源。
- 数字签名必须满足以下条件：
  - 接收方能够核实发送方对报文生成的数字签名。
  - 包括接收方在内的任何人都不能伪造发送方的数字签名。
  - 发送方事后不能否认自己对报文生成过数字签名。

### 7.3.2.2 数字签名的实现

- 发送方 A 的私钥为 `SK_A`，公钥为 `PK_A`。

1. A 使用自己的私钥 `SK_A` 对报文 `m` 进行数字签名，得到 $D_{SK_A}(m)$。
2. A 把带有数字签名的报文发送给 B。
3. B 使用 A 的公钥 `PK_A` 核验签名并恢复报文：$E_{PK_A}(D_{SK_A}(m))=m$。

![[7.3.2-数字签名工作过程.png|900]]

- 只有 A 掌握私钥 `SK_A`，因此其他人无法生成能由 `PK_A` 正确核验的签名。
- A 无法否认签名，因为第三方也可以使用 A 的公钥核验该签名。
- 报文 $D_{SK_A}(m)$ 仍可被任何取得 `PK_A` 的人恢复，因此这种数字签名不提供保密性。

### 7.3.2.3 具有保密性的数字签名

- 发送方 A 使用自己的私钥 `SK_A` 生成数字签名，再使用接收方 B 的公钥 `PK_B` 加密带签名的报文。
- 发送内容为 $E_{PK_B}(D_{SK_A}(m))$。

1. B 使用自己的私钥 `SK_B` 解密，得到带有 A 数字签名的报文 $D_{SK_A}(m)$。
2. B 使用 A 的公钥 `PK_A` 核验数字签名并恢复报文 `m`。

![[7.3.2-具有保密性的数字签名.png|900]]

- 其他人无法取得 B 的私钥 `SK_B`，因此不能解密出带签名的报文。
- ==先用发送方私钥签名，再用接收方公钥加密，可同时提供来源鉴别、不可否认性和保密性。==
