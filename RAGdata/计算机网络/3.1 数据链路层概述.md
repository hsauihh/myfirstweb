- [[1.6.1 常见的三种计算机网络体系结构#1.6.1.3 五层原理体系结构|数据链路层]]位于网络层与物理层之间，负责在相邻节点之间传送帧。
- 一条端到端通信路径可以由多段链路组成，每段链路都需要相应的数据链路层协议控制数据传输。
- 链路是相邻节点之间的物理线路；在链路上加入实现通信协议的硬件和软件后，才构成数据链路。

==帧是数据链路层的协议数据单元，沿端到端通信路径中的各段数据链路逐段传输。==

### 3.1.1 数据链路层在体系结构中的位置

- 端系统实现五层原理体系结构，路由器实现物理层、数据链路层和网络层。
- 从主机 H1 到主机 H2 的通信路径经过路由器 R1、R2，由 H1-R1、R1-R2、R2-H2 三段数据链路组成。
- 数据在端系统中沿协议栈向下或向上处理，在路由器中处理到网络层后再转发到下一段链路。

![[3.1-数据链路层在体系结构中的位置.png]]

- 数据链路层只负责相邻节点之间的一段传输；端到端传输需要多段数据链路依次配合。

### 3.1.2 链路、数据链路和帧

#### 链路

- 链路（Link）是从一个节点到相邻节点的一段物理线路，可以是有线线路或无线线路。
- 链路中间没有任何其他交换节点。
- 跨越多个交换节点的通信路径由多段链路构成，不能把整条通信路径称为一条链路。

![[3.1-链路.png]]

- 主机 H1 到路由器 R2 的通信路径跨越路由器 R1，因此由两段链路组成。

#### 数据链路

- 数据链路（Data Link）建立在链路之上。
- 在一条链路上传送数据时，除物理线路外，还需要通信协议控制数据传输。
- 把实现这些协议的硬件和软件加入链路，就构成数据链路。
- 计算机中的网络适配器（网卡）及其软件驱动程序实现这些协议；一般的网络适配器包含物理层和数据链路层功能。

![[3.1-数据链路.png]]

- 每段链路两端的网络适配器共同执行该段数据链路所使用的协议。

#### 帧

- 帧（Frame）是数据链路层对等实体之间进行水平逻辑通信的协议数据单元（Protocol Data Unit，PDU）。
- 网络层数据经过数据链路层封装后，以帧为单位在一段数据链路上传输。
- 路由器在不同链路之间转发时，上一段链路的帧传输结束，下一段链路再使用相应的帧继续传输。

![[3.1-帧的逐段传输.png]]

- 帧只在相邻节点之间逐段传送，不是从源主机直接跨越所有链路到达目的主机。
