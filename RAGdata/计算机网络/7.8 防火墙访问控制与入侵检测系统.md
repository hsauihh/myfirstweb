- 防火墙通过[[7.6 访问控制|访问控制]]策略隔离可信内部网络与不可信外部网络，阻止不允许的通信。
- 入侵检测系统用于发现已经发生或正在发生的入侵，并向管理员报告异常。

==防火墙负责依据预先配置的规则控制通信，入侵检测系统负责发现规则未能阻止的入侵；两者互相补充，但都不能保证阻止所有攻击。==

### 7.8.1 防火墙

- 防火墙（Firewall）是具体实现为可编程特殊路由器的一种访问控制技术。
- 防火墙把内部网络与因特网隔离为不同安全区域，允许符合策略的分组通过并丢弃其他分组。

#### 分组过滤路由器

- 分组过滤路由器根据源和目的 IP 地址、协议、源和目的端口及传输方向等字段决定是否转发分组。
- 分组过滤规则通常以访问控制列表（Access Control List，ACL）保存。
- 无状态分组过滤独立处理每个分组；有状态分组过滤还跟踪连接或会话状态。

![[7.8-访问控制列表.png|900]]

- 图中的 ACL 先允许内部主机访问外部 Web 服务器及相应返回流量，再允许指定内部邮件服务器收发邮件，最后用拒绝规则阻止特定外部主机访问内网。

#### 应用网关

- 应用网关又称代理服务器，在应用层实现访问控制，可以根据应用数据和用户身份实施更细粒度的策略。
- 每种应用需要专用的应用网关，不同应用网关可以运行在同一台主机上。

#### 类型与局限

- 网络防火墙保护内部网络，由网络管理员统一配置。
- 个人防火墙只保护一台主机，由用户配置分组过滤规则。
- 防火墙的安全效果高度依赖配置是否正确和完整。
- 防火墙查杀恶意代码的能力有限，提高查杀强度会增加计算开销并降低网络处理速度。
- 防火墙无法阻止利用已允许端口上的系统或协议漏洞实施的攻击。
- 防火墙自身也可能存在不足，例如分组过滤路由器不能防止 IP 地址和端口号欺骗，应用网关也可能存在程序漏洞。

### 7.8.2 入侵检测系统

- 入侵检测系统（Intrusion Detection System，IDS）在入侵造成更大危害之前尽早发现并报告入侵。
- IDS 分析实际通信流量，发现可疑分组时向网络管理员告警并记录相关信息。
- 由于误报率较高，IDS 一般不直接对可疑分组进行自动阻断。

| 类型 | 判断依据 | 优点 | 局限 |
| --- | --- | --- | --- |
| 基于特征 | 已知攻击的标志性特征数据库 | 能明确识别已知攻击 | 无法发现特征库中没有的未知攻击 |
| 基于异常 | 正常网络流量的统计特性和规律 | 能发现偏离正常模式的未知行为 | 正常行为变化也可能触发告警 |

- 攻击特征必须具有良好的区分度，特征库需要持续维护。

![[7.8-入侵检测系统分类.png|900]]

- 调整 IDS 的参数或阈值可以降低漏报率，但通常会同时提高误报率；漏报会造成安全假象，误报过多则会增加分析负担并使告警失去可信度。
