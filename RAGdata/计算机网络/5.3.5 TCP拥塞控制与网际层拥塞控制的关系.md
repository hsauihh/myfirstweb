- TCP 拥塞控制由端系统依据丢包和确认情况调整发送速率，网际层拥塞控制由路由器管理队列并决定何时丢弃 IP 数据报。
- 路由器队列采用先进先出（First In First Out，FIFO）时，队列已满再丢弃新到数据报的策略称为**尾部丢弃**。
- 主动队列管理（Active Queue Management，AQM）在队列满之前主动丢弃数据报，使 TCP 发送方更早降低发送速率。
![[5.3.5-尾部丢弃.png]]

==网际层提前丢弃数据报可向 TCP 发送方提供隐式拥塞信号，从而在队列溢出前抑制发送速率。==

### 5.3.5.1 尾部丢弃与全局同步

1. 多条 TCP 连接共同经过同一路由器时，报文段进入路由器 FIFO 队列。
2. 队列已满后到达的数据报被尾部丢弃。
3. 多个 TCP 发送方在相近时间检测到丢包，同时把拥塞窗口减小。
4. 网络负载骤降后，各发送方又同时增大拥塞窗口。

- 多条 TCP 连接的窗口同时增大和减小会使网络负载周期性大幅波动，这种现象称为全局同步。

![[5.3.5-TCP与网际层拥塞控制关系.png|900]]
![[5.3.5-全局同步.png]]

- 图中多个 TCP 发送方向同一路由器注入数据；路由器队列溢出引起丢包，继而使多个 TCP 拥塞窗口同时减小。

### 5.3.5.2 主动队列管理

- AQM 在队列长度达到最大值之前，按某种规则主动丢弃数据报。
- 提前丢弃使发送方尽早减小发送速率，可降低发生全局同步和队列溢出的概率。
- 随机早期检测（Random Early Detection，RED）也称随机早期丢弃（Random Early Drop 或 Random Early Discard，RED）。
![[5.3.5-主动队列管理.png]]
1. RED 维护路由器队列的平均长度。
2. 平均队列长度小于最小门限时，将新到 IP 数据报排入队列。
3. 平均队列长度处于最小门限与最大门限之间时，按概率随机丢弃新到 IP 数据报。
4. 平均队列长度大于最大门限时，丢弃新到 IP 数据报。

![[5.3.5-主动队列管理RED.png|900]]

- RED 在队列尚未满时随机选择部分连接提供拥塞信号，避免所有 TCP 连接同时减小拥塞窗口。
- 因特网工程任务组（Internet Engineering Task Force，IETF）发布的 RFC 7567 已将推荐使用 RED 的 RFC 2309 列为陈旧文档，不再推荐使用 RED；路由器仍需要主动队列管理，但替代旧 RED 的几种算法尚处于实验阶段，尚未形成 IETF 标准。
