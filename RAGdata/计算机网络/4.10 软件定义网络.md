- 软件定义网络（Software Defined Networking，SDN）是一种将网络的控制层面与数据层面分离的新型网络体系结构。
- 数据层面的分组交换机依据流表转发分组，控制层面的 SDN 控制器掌握网络状态并向交换机下发流表。
- 网络控制应用程序通过开放接口使用控制器提供的网络控制能力，使网络行为可以由软件编程。

==SDN 将分散在各台路由器中的控制功能与分组转发功能分离，由逻辑集中的控制器统一计算规则，数据层面只按流表执行“匹配加动作”。==

### 4.10.1 从传统路由器到 SDN

- 路由器具有两项基本功能：为主机间的通信提供分组转发服务，以及执行路由选择。
- 路由器之间传送的信息包括主机分组和路由信息。
- 传统路由器的控制层面包含路由选择算法和路由表，数据层面包含转发表。
- 每台传统路由器都运行自己的控制软件，并通过交换路由信息独立计算转发表。

![[4.10-传统路由器控制与数据层面.png]]

- 图中每台传统路由器都同时具有控制层面的路由选择功能和数据层面的分组转发功能。
![[4.10-SDN控制与数据层面.png]]
- SDN 把控制功能从分组交换机中移出，由逻辑集中的远程控制器掌握各主机和链路的状态。
- SDN 控制器计算分组转发规则并写入各交换机的流表，交换机之间不再交换路由信息。

### 4.10.2 OpenFlow 与广义转发

- OpenFlow 是控制层面与数据层面之间的通信接口协议，使控制器可以直接访问和控制数据层面的物理设备。
- 开放网络基金会（Open Networking Foundation，ONF）负责制定 OpenFlow 技术规范。
- OpenFlow 是实现 SDN 控制与数据层面通信的一种方式，SDN 并不限定必须使用 OpenFlow。

![[4.10-OpenFlow协议定位.png]]

- 图中控制器通过 OpenFlow 协议管理交换机中的流表，流表决定安全信道以外的数据分组如何处理。

| 对比维度 | 传统转发 | SDN 广义转发 |
| --- | --- | --- |
| 匹配依据 | 主要匹配网络层的目的 IP 地址 | 可匹配数据链路层、网络层和运输层等首部字段 |
| 匹配方式 | 对目的网络前缀执行最长前缀匹配 | 按流表项中的多个字段执行匹配 |
| 动作 | 把分组转发到指定接口 | 可以转发、丢弃、修改首部或交给控制器处理 |

![[4.10-广义匹配与动作.png|900]]

- SDN 先将分组首部与流表项进行匹配，再执行匹配项规定的一个或多个动作。

### 4.10.3 OpenFlow 交换机与流表

- OpenFlow 交换机具有一个或多个流表，并通过安全信道与 SDN 控制器通信。
- SDN 中的“流”是具有相同属性并采用相同处理规则的一组分组。
- 流表由 SDN 控制器管理；控制器通过 OpenFlow 协议添加、更新和删除流表项。
- 分组没有匹配任何流表项时，可以被丢弃或发送给控制器作进一步处理。

![[4.10-OpenFlow交换机与流表.png]]

- 图中控制器维护交换机的流表，进入交换机的分组由流表中的匹配规则和动作共同决定处理结果。

| 流表项部分 | 作用 |
| --- | --- |
| 首部字段 | 与到达分组的对应首部字段进行匹配 |
| 计数器 | 记录已经与该流表项匹配的分组数量 |
| 动作 | 规定匹配成功后对分组执行的处理 |

![[4.10-OpenFlow1.0匹配字段.png]]

- OpenFlow 1.0 可同时匹配入端口以及以太网、网络层和运输层的多个首部字段。
![[4.10-OpenFlow流表计数器.png]]

![[4.10-OpenFlow流表动作.png|900]]

- 流表动作可以把分组转发到指定端口，也可以丢弃分组或修改分组的首部字段。

### 4.10.4 流表应用

#### 简单转发

- 控制器可以为各交换机下发相互配合的流表项，使满足条件的分组沿指定路径逐跳转发。

![[4.10-简单转发流表.png]]

- 图中不同交换机依据各自流表中的入端口、源地址和目的地址匹配结果，把同一条流上的分组导向预定路径。

#### 负载均衡

- 控制器可以为不同流设置不同动作，使业务分布到不同链路或目的设备。

![[4.10-负载均衡.png|900]]

- 图中流表把不同来源的分组导向不同转发路径，从而分担网络负载。

#### 防火墙

- 控制器可以只为允许通过的流设置转发动作，使不满足访问条件的分组无法获得相应的转发规则。

![[4.10-防火墙.png|900]]

- 图中的流表仅允许指定来源的分组进入受保护网络，实现基于流规则的访问控制。

### 4.10.5 SDN 体系结构与控制器

- SDN 体系结构包括数据层面、SDN 控制器和网络控制应用程序。
- 数据层面的分组交换机执行基于流的转发。
- SDN 控制器作为网络操作系统，维护网络范围的状态并向数据层面下发规则。
- 路由选择、负载均衡和接入控制等网络控制应用程序通过北向应用程序编程接口使用控制器能力。
- 控制器通过南向应用程序编程接口与受控分组交换机通信。

![[4.10-SDN体系结构与关键特征.png|900]]

- 图中网络控制应用程序、SDN 控制器和分组交换机构成控制层面与数据层面分离的体系结构。

| 控制器组成 | 作用 |
| --- | --- |
| 面向网络控制应用程序的接口 | 向上层应用提供网络控制能力 |
| 网络范围的状态管理层 | 维护链路状态、主机信息和分组交换机信息等网络状态 |
| 通信层 | 通过南向接口与数据层面的受控设备通信 |

![[4.10-SDN控制器组成.png|900]]

- SDN 控制器在网络状态管理、上层应用接口和设备通信之间提供统一的控制支撑。
- 开放网络操作系统（Open Network Operating System，ONOS）和 OpenDaylight 都是开源 SDN 控制器。
- ONOS 和 OpenDaylight 为实现可编程网络控制提供了开源控制器平台。

![[4.10-SDN核心知识.png|900]]

- 图中汇总了 SDN 的核心知识要点。
