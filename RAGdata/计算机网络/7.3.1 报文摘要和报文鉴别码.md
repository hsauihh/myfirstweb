- 报文摘要是散列函数对任意长度报文生成的固定长度输出，可用于检测报文是否被篡改。
- 报文鉴别码把报文与只有通信双方掌握的密钥关联起来，在检验完整性的同时鉴别报文来源。

==单独发送报文摘要不能阻止攻击者同时修改报文和摘要；只有对摘要加密或把共享鉴别密钥纳入散列计算，才能形成可信的报文鉴别码。==

### 7.3.1.1 报文完整性与报文鉴别

- 报文完整性要求接收方能够发现报文是否被攻击者篡改。
- 报文鉴别用于确认报文的真伪。
- 对报文进行完整性验证，实质上是在进行报文鉴别。
- 加密技术通常可以辅助报文鉴别，因为被篡改的密文解密后通常无法得到可理解的内容。
- 对于不需要保密但需要鉴别的报文，直接加密和解密整个报文会耗费较多计算资源，因此只加密较短的报文摘要更合适。

### 7.3.1.2 报文摘要与报文鉴别码

- 报文摘要算法 `MD` 对可变长度报文 `m` 生成固定长度摘要 $H(m)$。
- 通信双方预先共享密钥 `K`。

1. 发送方计算 $H(m)$，使用密钥 `K` 加密摘要，得到报文鉴别码 $E_K(H(m))$。
2. 发送方把报文 `m` 与加密后的摘要一起发送。
3. 接收方使用 `K` 解密收到的摘要，同时对收到的报文重新计算 $H(m)$。
4. 两个摘要相同则接受报文，否则判定报文已被篡改。

![[7.3.1-报文鉴别码工作过程.png|900]]

- 只加密固定且较短的摘要，比加密整个长报文更简单。
- 密钥只在通信双方之间共享，第三方不能生成由 `K` 加密的合法摘要。

### 7.3.1.3 密码散列函数

- 报文摘要算法也称密码散列函数或散列算法。
- 报文摘要 `H(m)` 的输入可以是任意长度，输出长度固定且较短。
- 不同输入通常得到不同的散列值，散列函数的输入与输出并非一一对应，而是多对一映射。
- 散列函数必须满足以下条件：
  - 已知 $H(x)$ 时，在计算上无法找到满足 $H(y)=H(x)$ 的报文 `y`。
  - 在计算上无法找到两个不同报文 `x` 和 `y`，使 $H(y)=H(x)$。
- 满足上述条件时，攻击者既不能由摘要逆推出报文，也难以构造具有相同摘要的替代报文。
- 散列函数是一种单向函数，无法通过散列函数的逆运算恢复原报文。

### 7.3.1.4 实用的报文摘要算法

- MD5（Message Digest 5）输出 `128` 比特的报文摘要，已被证明不安全。
- 安全散列算法 1（Secure Hash Algorithm 1，SHA-1）输出 `160` 比特的报文摘要，计算速度比 MD5 稍慢。
- SHA-1 虽比 MD5 安全，但实际安全性也没有达到设计目标。
- 采用 SHA-1 的证书已逐步被采用 SHA-2 的证书替代。

### 7.3.1.5 散列报文鉴别码

- 散列报文鉴别码（Hash-based Message Authentication Code，HMAC）利用密码散列函数的性质实现报文鉴别。
- 通信双方共享鉴别密钥 `s`。

1. 发送方把鉴别密钥 `s` 与报文 `m` 作为密码散列函数的输入，计算 HMAC。
2. 发送方把报文与 HMAC 一起发送。
3. 接收方使用同一密钥 `s` 和收到的报文重新计算 HMAC。
4. 两个 HMAC 相同则接受报文，否则判定报文已被篡改。

![[7.3.1-HMAC工作过程.png|900]]

- 攻击者既不知道鉴别密钥 `s`，也不能从截获的 HMAC 中计算出 `s`，因此无法为伪造报文生成合法 HMAC。
