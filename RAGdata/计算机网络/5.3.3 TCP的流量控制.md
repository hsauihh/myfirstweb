- TCP 流量控制（Flow Control）使发送方的发送速率不超过接收方的接收能力，避免接收缓存溢出。
- 接收方通过 TCP 报文段首部中的窗口字段通告接收窗口 `rwnd`，发送方据此调整发送窗口。
- 接收方通告零窗口后，发送方使用持续计时器和零窗口探测报文段确认窗口是否重新开放。

==TCP 发送方的发送窗口不能超过接收方通告的 `rwnd`；实际发送窗口还要同时受拥塞窗口 `cwnd` 限制，即 $swnd=\min(cwnd,rwnd)$。==

### 5.3.3.1 流量控制的基本概念

- TCP 接收方的应用进程不一定能立即读取已经到达的数据，未读取数据需要暂存在接收缓存中。
- 接收方根据接收缓存的可用空间确定 `rwnd`，并通过确认报文段把它通告给发送方。
- 发送方把发送窗口限制在接收方能够接收的范围内，从而控制向接收方发送数据的速率。
- 流量控制作用于一对发送方和接收方之间，不负责解决整个网络中的拥塞。

![[5.3.3-TCP流量控制基本概念.png|900]]

- 图中接收方依据接收缓存可用空间向发送方反馈窗口大小，发送方据此限制在 TCP 连接中注入的字节数。

### 5.3.3.2 接收窗口与发送窗口

1. 接收方在确认报文段中用确认号累计确认已经按序收到的数据，并在窗口字段中通告新的 `rwnd`。
2. 发送方收到确认后向前滑动发送窗口，并把发送窗口大小调整为不超过通告的 `rwnd`。
3. 接收缓存可用空间减小时，接收方通告较小的 `rwnd`，发送方随之缩小发送窗口。
4. 接收缓存没有可用空间时，接收方通告 `rwnd=0`，发送方暂停发送普通数据。

- 确认号表示接收方希望收到的下一个字节序号，与窗口字段分别说明“已经收到哪里”和“还能接收多少”。
- 发送窗口中的数据可以连续发送，不必在每发送一个报文段后等待确认。
- 发送方还需要进行[[5.3.4 TCP的拥塞控制|拥塞控制]]，所以实际发送窗口为 $swnd=\min(cwnd,rwnd)$。
![[5.3.3-流量控制窗口缩小.png]]
![[5.3.3-接收窗口控制发送窗口.png]]

- 图中接收方先通告 `rwnd=300`，随后通告 `rwnd=100`，最后通告 `rwnd=0`；发送方的发送窗口随接收能力逐步缩小并暂停发送。

### 5.3.3.3 零窗口与持续计时器

- 接收方把零窗口更新为非零窗口的通知可能在网络中丢失。
- 若发送方一直等待非零窗口通知，而接收方一直等待新数据，就会形成死锁局面。
![[5.3.3-零窗口死锁.png]]

1. 发送方收到零窗口通知后启动持续计时器。
2. 持续计时器超时后，发送方发送携带 `1 Byte` 数据的零窗口探测报文段。
3. 接收方确认探测报文段，并在确认中通告当前接收窗口。
4. 若 `rwnd` 仍为 `0`，发送方重新启动持续计时器；若 `rwnd` 大于 `0`，发送方恢复发送。

- TCP 规定接收方即使通告了零窗口，也必须接收零窗口探测报文段、确认报文段以及携带紧急数据的报文段。
![[5.3.3-零窗口探测.png]]
![[5.3.3-零窗口与持续计时器.png|900]]
![[5.3.3-零窗口探测重传.png]]
- 图中非零窗口通知丢失后，发送方通过周期性的零窗口探测取得新的窗口通告，从而打破双方互相等待的死锁局面。

### 5.3.3.4 发送窗口计算例题

- 已知：最大报文段长度 $MSS=1000\ \mathrm{B}$，拥塞窗口 $cwnd=4000\ \mathrm{B}$，接收窗口 $rwnd=2000\ \mathrm{B}$；发送方连续发送两个最大报文段后，只收到对第一个报文段的确认。
- 求：发送方此时还能发送的最大字节数。
- 使用知识：[[#5.3.3.2 接收窗口与发送窗口|发送窗口约束]]。
- 解题过程：
  1. 实际发送窗口为 $swnd=\min(cwnd,rwnd)=\min(4000,2000)=2000\ \mathrm{B}$。
  2. 第一个报文段已经确认，第二个报文段的 $1000\ \mathrm{B}$ 数据仍未确认，占用发送窗口。
  3. 可用窗口为 $swnd-\text{已发送未确认数据量}=2000\ \mathrm{B}-1000\ \mathrm{B}=1000\ \mathrm{B}$。
  4. 发送方还能发送一个最大报文段，即序号为 `2000~2999` 的 `1000 B` 数据。
- 结论：==发送方还能发送的最大字节数为 `1000 B`，答案为 A。==
- MSS 表示 TCP 报文段数据载荷的最大长度，不是整个 TCP 报文段的长度。

![[5.3.3-流量控制例题.png|900]]

- 图中上方窗口表示拥塞窗口；收到第一个报文段的确认后，发送窗口内仍有一个最大报文段尚未确认，因此只剩 `1000 B` 可用窗口。
