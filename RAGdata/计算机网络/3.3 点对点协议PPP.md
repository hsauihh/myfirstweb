- 点对点协议（Point-to-Point Protocol，PPP）是目前使用最广泛的点对点数据链路层协议。
- PPP 用于在点对点链路上传送网络层[[1.6.4 计算机网络体系结构中的专用术语#1.6.4.4 PDU 与 SDU|协议数据单元（PDU）]]，典型应用包括用户接入因特网和路由器之间的点对点链路。
- PPP 由网络层协议数据单元的封装方法、链路控制协议和一组网络控制协议组成。

==PPP 使用 LCP 建立并维护数据链路，使用相应的 NCP 配置网络层协议，并依据链路类型采用字节填充或比特填充实现透明传输。==

### 3.3.1 PPP 的应用与组成

- PPP 由因特网工程任务组（Internet Engineering Task Force，IETF）在 1992 年制定。
- PPP 的主要规范包括 RFC 1661 和 RFC 1662。
- 用户主机可以通过 PPP 接入因特网服务提供者（Internet Service Provider，ISP）。
- PPP 也可以用于路由器之间的点对点链路。

![[3.3-PPP典型应用.png|900]]

- 图中的因特网用户通过 PPP 接入 ISP，ISP 再把用户接入因特网。
- PPP 可以承载多种网络层协议，而不只承载 IP。

| 组成部分 | 作用 |
| --- | --- |
| 网络层协议数据单元的封装方法 | 把网络层 PDU 封装到串行链路上传输 |
| 链路控制协议（Link Control Protocol，LCP） | 建立、配置、测试和终止数据链路 |
| 网络控制协议（Network Control Protocol，NCP） | 为不同网络层协议配置参数；每种网络层协议使用相应的 NCP |

![[3.3-PPP协议组成.png|900]]

- PPP 位于物理层和网络层之间，通过 LCP 管理链路，并通过不同 NCP 支持 IP、IPX 和 AppleTalk 等网络层协议。
- ==LCP 面向数据链路本身，NCP 面向 PPP 所承载的网络层协议。==

### 3.3.2 PPP 的帧格式与差错检测

| 字段 | 长度 | 取值或作用 |
| --- | --- | --- |
| 标志 F | `1 B` | 固定为 `0x7E`，标识帧的边界 |
| 地址 A | `1 B` | 固定为 `0xFF`，目前没有实际作用 |
| 控制 C | `1 B` | 固定为 `0x03`，目前没有实际作用 |
| 协议 P | `2 B` | 指明信息字段承载哪一种协议的数据单元 |
| 信息 | 不超过 `1500 B` | 承载网络层 PDU 或 LCP、NCP 分组 |
| 帧检验序列 FCS | `2 B` | 由循环冗余校验计算得到，用于检测传输差错 |

- `1500 B` 是 PPP 信息字段的最大传送单元（Maximum Transmission Unit，MTU）。
- 协议字段取 `0x0021` 时，信息字段承载 IP 数据报。
- 协议字段取 `0xC021` 时，信息字段承载 LCP 分组。
- 协议字段取 `0x8021` 时，信息字段承载 IP 控制协议（IP Control Protocol，IPCP）分组；IPCP 是用于配置 IP 的 NCP。

![[3.3-PPP帧格式.png|900]]

- 帧首和帧尾都使用 `0x7E` 作为标志，接收方依据标志确定帧边界。
- 协议字段把同一条 PPP 链路上的不同上层协议数据区分开来。

### 3.3.3 PPP 的透明传输

#### 异步链路的字节填充

- 面向字节的异步链路按照 RFC 1662 使用字节填充。
- 发送方按以下规则处理信息字段：

1. 遇到 `0x7E`，把它转换为 `0x7D 0x5E`。
2. 遇到 `0x7D`，把它转换为 `0x7D 0x5D`。
3. 遇到数值小于 `0x20` 的 ASCII 控制字符，在其前面插入 `0x7D`，并把该字符与 `0x20` 异或；例如 `0x03` 转换为 `0x7D 0x23`。
4. 接收方执行相反变换，恢复原始信息字段。

![[3.3-PPP字节填充.png|900]]

- `0x7D` 是转义字符，后续字节通过与 `0x20` 异或恢复原值。
- 字节填充使信息字段中的数据不会被误判为帧边界。

#### 同步链路的比特填充

- 面向比特的同步链路使用零比特填充。

1. 发送方扫描信息字段的比特流。
2. 每遇到五个连续的 `1`，立即插入一个 `0`。
3. 接收方每遇到五个连续的 `1`，删除其后的一个 `0`。

![[3.3-PPP比特填充.png|900]]

- 标志字段的比特模式为 `01111110`。
- ==在五个连续的 `1` 后插入 `0`，可以避免信息字段中出现与标志字段相同的比特模式。==

### 3.3.4 PPP 的工作状态

- PPP 链路依次经历物理连接建立、LCP 配置、可选鉴别、NCP 配置和数据传输。

1. 线路处于“静止”状态时，检测到载波并建立物理层连接，进入“建立”状态。
2. 双方在“建立”状态使用 LCP 协商链路参数；协商成功进入“鉴别”状态，协商失败返回“静止”状态。
3. 不需要鉴别或鉴别成功时进入“网络”状态；鉴别失败时进入“终止”状态。
4. 双方在“网络”状态使用相应 NCP 配置网络层协议，配置完成后进入“打开”状态。
5. 链路在“打开”状态传送网络层数据；出现故障或收到终止请求时进入“终止”状态。
6. 载波停止后，链路由“终止”状态返回“静止”状态。

![[3.3-PPP工作状态.png|900]]

- “打开”状态表示 PPP 链路已经完成 LCP、鉴别和 NCP 阶段，可以传送网络层数据。
- LCP 协商失败、鉴别失败和链路故障分别在不同阶段终止连接。
