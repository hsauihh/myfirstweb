- 已部署的 IPv4 路由器数量巨大，不能在短时间内全部升级为 IPv6。
- IPv4 向 IPv6 的过渡必须逐步进行，并要求新的 IPv6 系统能够处理 IPv4 数据报。
- 双协议栈和隧道技术是两种过渡方法。

==双协议栈在 IPv4 与 IPv6 数据报之间转换，隧道技术则把完整 IPv6 数据报封装在 IPv4 数据报中穿越 IPv4 网络。==

![[4.9.5-IPv4向IPv6过渡方法.png]]
### 4.9.5.1 双协议栈

- 双协议栈（Dual Stack）要求部分主机或路由器同时装有 IPv6 和 IPv4 两套协议栈。
- 双协议栈节点既可以与 IPv6 系统通信，也可以与 IPv4 系统通信，并具有 IPv6 地址和 IPv4 地址。
- 双协议栈主机与域名系统（Domain Name System，DNS）配合选择协议：DNS 返回 IPv4 地址时使用 IPv4，返回 IPv6 地址时使用 IPv6。

1. IPv6 数据报到达 IPv4 网络入口的双协议栈路由器。
2. 入口路由器把 IPv6 数据报转换成 IPv4 数据报。
3. IPv4 数据报在 IPv4 网络中转发。
4. 出口双协议栈路由器再把 IPv4 数据报转换为 IPv6 数据报。

![[4.9.5-双协议栈.png]]

- IPv6 首部中的流标号等字段不能完整转换到 IPv4 首部，因此出口路由器无法恢复所有原始 IPv6 首部信息。

### 4.9.5.2 隧道技术

1. IPv6 数据报进入 IPv4 网络时，**入口双协议栈路由器把完整 IPv6 数据报作为 IPv4 数据报的数据载荷**。
2. 外层 IPv4 首部的源地址和目的地址分别是隧道入口与出口路由器的 IPv4 地址。
3. 封装后的 IPv4 数据报在 IPv4 网络中按普通 IPv4 数据报转发。
4. 隧道出口路由器移除外层 IPv4 首部，恢复原始 IPv6 数据报并继续转发。
- 外层 IPv4 首部的协议字段必须设置为 `41`，表示其数据载荷是 IPv6 数据报。

![[4.9.5-隧道技术.png]]

- 隧道在 IPv4 网络内部表现为一条传送 IPv6 数据报的逻辑链路，原 IPv6 数据报内容在穿越过程中保持不变。
