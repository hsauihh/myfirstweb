- 网桥工作在数据链路层，能够识别 MAC 帧并按照转发表选择是否转发。
- 透明网桥通过自学习建立 MAC 地址与接口的对应关系，对已知单播帧进行明确转发或过滤，对未知单播帧进行盲目转发。
- 生成树协议在保留物理冗余链路的同时构造无环的逻辑拓扑，避免广播帧永久循环。

==网桥用存储转发分隔碰撞域，STP 再从带环的物理拓扑中选出无环的逻辑转发拓扑。==

### 3.4.5.1 网桥的结构与基本工作原理

- 网桥（Bridge）工作在数据链路层，并包含物理层功能。
- 网桥把收到的帧先存入缓存，处理后再转发，这种方式称为[[1.3 电路交换、分组交换和报文交换#存储转发|存储转发]]。
- 网桥接口在所连接的共享式以太网上发送帧时，仍执行 CSMA/CD。

| 对比维度 | 集线器 | 网桥 |
| --- | --- | --- |
| 工作层次 | 物理层 | 数据链路层，并包含物理层功能 |
| 处理对象 | 比特 | MAC 帧 |
| 转发依据 | 从一个接口收到后向其他接口转发 | 根据目的 MAC 地址和转发表选择接口 |
| 碰撞域 | 所有接口属于同一碰撞域 | 每个接口连接的网段形成独立碰撞域 |
| 同网段帧 | 向其他接口转发比特 | 目的站与入接口同属一个网段时不向其他网段转发 |

1. 网桥从一个接口接收完整的无误码帧并暂存在缓存中。
2. 网桥查找目的 MAC 地址对应的转发接口。
3. 目的接口与入接口不同时，网桥从目的接口转发该帧。
4. 目的接口与入接口相同时，网桥丢弃该帧，不把同网段通信转发到其他网段。

![[3.4.5-网桥跨网段转发.png|900]]

- A 发给 D 的帧从接口 1 进入网桥，网桥查表后从接口 2 转发。

![[3.4.5-网桥同网段过滤.png|900]]

- A 发给 C 的帧已经能在左侧共享网段直接到达 C，网桥不再把它转发到其他网段。

![[3.4.5-网桥结构.png|900]]

- 网桥由接口、缓存、转发表、转发器、网桥协议实体和接口管理软件等部分组成。
- 广播帧从除入接口外的其他接口转发。

### 3.4.5.2 透明网桥的自学习与转发

- 透明网桥（Transparent Bridge）符合 IEEE 802.1D 标准。
- “透明”表示站点不需要知道帧会经过哪些网桥，网桥可以即插即用。

1. 网桥收到无误码帧后，==登记帧的源 MAC 地址和入接口号==。
2. 目的地址为广播地址时，==从除入接口外的其他接口转发==。
3. 目的地址为单播地址时，查找转发表。
4. 查到的目的接口与入接口不同时，进行明确转发。
5. 查到的目的接口与入接口相同时，丢弃该帧。
6. 查不到目的地址时，进行==盲目转发==，把帧从除入接口外的所有接口转发。

![[3.4.5-透明网桥跨网段转发.png|900]]

- 网桥学习到 A 位于接口 1 后，D 发给 A 的帧可以从接口 2 明确转发到接口 1。

![[3.4.5-透明网桥同网段过滤.png|900]]

- 网桥学习到 C 位于接口 1 后，发现 C 发给 A 的帧不需要跨网段，因而将其丢弃。

![[3.4.5-透明网桥转发规则.png|900]]

- 收到有误码的帧时，网桥直接丢弃。
- 转发表中的每条记录都有有效时间，到期后自动删除。
- 转发表老化使网桥能够适应网卡更换或站点移动造成的 MAC 地址与接口对应关系变化。

### 3.4.5.3 生成树协议 STP

- 在两个以太网之间设置多个网桥可以提供冗余链路，提高可靠性。
- 冗余链路会形成环路，广播帧可能在环路中永久兜圈，耗尽网络资源并使站点无法通信。

![[3.4.5-环路中的广播风暴.png|900]]

- 两个网桥不断互相转发广播帧，广播帧沿环路无限循环。
- 生成树协议（Spanning Tree Protocol，STP）用于在保留冗余链路的同时消除逻辑环路。
- 网桥之间交换网桥协议数据单元（Bridge Protocol Data Unit，BPDU）。
- STP 从带环的物理拓扑中找出一个连通且无环的子集，这个子集就是生成树。
- 不属于当前生成树的接口被阻塞，不转发普通数据帧。
- 首次连接网桥或网络拓扑发生变化时，网桥重新构造生成树。
- 当前链路发生故障时，STP 可以启用原先阻塞的冗余接口，以恢复网络连通。

![[3.4.5-STP故障恢复.png|900]]

- 上方链路故障后，网桥交换 BPDU 并开启原先阻塞的接口，使两个以太网继续连通。
- ==STP 使物理拓扑可以保留冗余，但任一时刻用于帧转发的逻辑拓扑必须无环。==
