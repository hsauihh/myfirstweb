- 信道的极限容量是在给定频率带宽和噪声条件下能够达到的传输速率上限。
- 信号失真程度受[[2.4 编码与调制#2.4.1 编码与调制的基本概念|码元]]传输速率、传输距离、噪声干扰和传输媒体质量影响。
- 奈奎斯特准则限制理想低通信道的码元传输速率，香农公式限制带宽受限且有噪声信道的可靠数据传输速率。

==实际信道的数据传输速率必须同时低于奈奎斯特准则和香农公式所确定的上限。==

### 2.5.1 信号失真与信道带宽

| 影响因素 | 变化 | 对失真的影响 |
| --- | --- | --- |
| 码元传输速率 | 越高 | 越严重 |
| 信号传输距离 | 越远 | 越严重 |
| 噪声干扰 | 越大 | 越严重 |
| 传输媒体质量 | 越差 | 越严重 |

- 信道中的数字信号可以由多个不同频率的模拟信号叠加形成。
- 叠加的频率分量越多，形成的波形越接近期望的数字信号波形。
- 如果数字信号中的高频分量不能通过信道，接收波形的前沿和后沿会变得不陡峭。
- 相邻码元的波形边界变得不清晰时，接收端会产生码间串扰。
- 信道的频率带宽越宽，能够通过的信号高频分量越多，码元失真越小。

![[2.5-数字信号的频率分量.png|900]]

- 基波与高次谐波叠加后，模拟波形逐步逼近期望的数字信号。
- ==限制信道带宽会滤除部分高频分量，从而引起波形失真和码间串扰。==

### 2.5.2 奈奎斯特准则

- 理想低通信道的最高码元传输速率为：
  - $R_s=2W\,\mathrm{Baud}$
- 变量：
  - $R_s$：码元传输速率，也称波特率、调制速率或波形速率，单位为 `Baud`，即 `码元/s`。
  - $W$：信道的频率带宽，单位为 `Hz`。
- 适用条件：理想低通信道。
- 该公式给出码元传输速率上限，不直接给出比特传输速率上限。
- 只要码元传输速率不超过该上限，就可以避免码间串扰。

![[2.5-奈奎斯特准则.png|900]]

- 当一个码元只携带一个比特时，码元传输速率与比特传输速率在数值上相等。
- 当一个码元携带多个比特时，比特传输速率为：
  - $R_b=R_s\log_2 M$
- 因此理想低通信道的最高比特传输速率为：
  - $R_{b\max}=2W\log_2 M$
- 变量：
  - $R_b$：比特传输速率，单位为 `b/s`。
  - $M$：可区分的码元状态数，无单位。
- 常见变形：
  - $M=2^{R_b/R_s}$
  - $R_s=\dfrac{R_b}{\log_2 M}$

#### 奈奎斯特准则计算

- 已知：无噪声信道带宽为 $3\,\mathrm{kHz}$，采用 $4$ 种相位和每种相位 $4$ 种振幅的 [[2.4 编码与调制#2.4.4 正交振幅调制|QAM]] 调制。
- 求：最大数据传输速率。
- 计算：
  1. 最高码元传输速率为 $R_s=2\times3\,\mathrm{kHz}=6\,\mathrm{kBaud}$。
  2. 码元状态数为 $M=4\times4=16$。
  3. 每个码元携带 $\log_2 16=4$ 比特。
  4. 最高数据传输速率为 $R_b=6\,\mathrm{kBaud}\times4=24\,\mathrm{kb/s}$。
- 结论：==该信道的最大数据传输速率为 $24\,\mathrm{kb/s}$。==

### 2.5.3 香农公式

- 带宽受限且有高斯白噪声干扰的信道，其极限信息传输速率为：
  - $C=W\log_2\left(1+\dfrac{S}{N}\right)$
- 变量：
  - $C$：信道的极限信息传输速率，单位为 `b/s`。
  - $W$：信道的频率带宽，单位为 `Hz`。
  - $S$：信道内所传信号的平均功率，单位为 `W`。
  - $N$：信道内高斯噪声的平均功率，单位为 `W`。
  - $S/N$：信噪比，是无量纲比值。
- 分贝表示与线性信噪比之间的换算关系：
  - $\left(\dfrac{S}{N}\right)_{\mathrm{dB}}=10\log_{10}\dfrac{S}{N}$
  - $\dfrac{S}{N}=10^{\left(S/N\right)_{\mathrm{dB}}/10}$
- 适用条件：信道带宽受限，并存在高斯白噪声干扰。
- 信道带宽或信噪比越大，极限信息传输速率越高。
- 实际信道不能无限提高带宽和信噪比，实际数据传输速率也必须低于香农极限。

![[2.5-香农公式.png|900]]

- 香农公式同时考虑信道带宽和信噪比，但不规定具体的码元状态数和调制方法。
- 采用更好的编码与调制方法，可以使实际数据传输速率更接近香农极限。

#### 香农公式计算

- 已知：信道带宽为 $8\,\mathrm{kHz}$，信噪比为 $30\,\mathrm{dB}$，实际数据传输速率为理论最大值的 $50\%$。
- 求：实际数据传输速率。
- 计算：
  1. 将分贝换算为线性比值：$S/N=10^{30/10}=1000$。
  2. 代入香农公式：$C=8\,\mathrm{kHz}\times\log_2(1+1000)\approx80\,\mathrm{kb/s}$。
  3. 实际速率为 $80\,\mathrm{kb/s}\times50\%=40\,\mathrm{kb/s}$。
- 结论：==实际数据传输速率约为 $40\,\mathrm{kb/s}$。==

### 2.5.4 奈奎斯特准则与香农公式的关系

| 对比维度 | 奈奎斯特准则 | 香农公式 |
| --- | --- | --- |
| 信道条件 | 理想低通信道 | 带宽受限且有高斯白噪声的信道 |
| 直接限制的量 | 码元传输速率 | 可靠数据传输速率 |
| 主要影响因素 | 信道带宽 | 信道带宽和信噪比 |
| 上限公式 | $R_s=2W$ | $C=W\log_2(1+S/N)$ |
| 与调制的关系 | 结合码元状态数 $M$ 才能换算比特率 | 不规定具体调制方式 |

- 奈奎斯特准则确定带宽允许的码元传输速率，再由每个码元携带的比特数换算数据传输速率。
- 香农公式给出噪声条件下可靠数据传输速率的最终上限。
- 实际可用的数据传输速率不能超过 $2W\log_2 M$ 和 $W\log_2(1+S/N)$ 中的任一上限。
- 信号传播速率影响传播时延，但不出现在这两个数据传输速率上限公式中。
