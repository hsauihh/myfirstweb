- 实体、协议和服务用于描述分层体系结构中通信主体、同层规则和相邻层关系。
- 对等实体按协议进行水平的逻辑通信，每一层通过这种通信向上一层提供服务。
- 协议数据单元描述对等层之间传送的数据，服务数据单元描述同一系统相邻层之间交换的数据。

==协议是水平的，服务是垂直的；实体使用下层服务时不需要知道该服务所依赖的具体协议。==

### 1.6.4.1 实体与对等实体

- 实体是任何可发送或接收信息的硬件或软件进程。
- 对等实体是通信双方相同层次中的实体。
- 同一层的两个对等实体之间进行逻辑通信，实际信息仍需经过下层传送。

### 1.6.4.2 协议及三要素

- 协议是控制两个对等实体在水平方向进行逻辑通信的规则的集合。

| 协议要素 | 定义 |
| --- | --- |
| 语法 | 定义所交换信息的格式 |
| 语义 | 定义通信双方所要完成的操作 |
| 同步 | 定义通信双方的时序关系 |

- HTTP 请求报文和响应报文的先后关系属于同步所定义的内容。

### 1.6.4.3 服务与服务访问点

- 在协议控制下，两个对等实体的水平逻辑通信使本层能够向上一层提供服务。
- 要实现本层协议，还需要使用下一层所提供的服务。
- 协议是水平的，服务是垂直的。
- 下层协议对上层实体是透明的，上层实体只需使用下层提供的服务。
- 相邻层实体通过服务原语交换信息。
- 服务访问点（Service Access Point，SAP）是同一系统中相邻两层实体交换信息的逻辑接口，用于区分不同的服务类型。
- 用于区分上层服务的 SAP 标识可由帧的“类型”字段、IP 数据报的“协议”字段、TCP 报文段或 UDP 用户数据报的“端口号”字段承载。

### 1.6.4.4 PDU 与 SDU

- 对等层次之间传送的数据包称为该层的协议数据单元（Protocol Data Unit，PDU）。
- 同一系统内层与层之间交换的数据包称为服务数据单元（Service Data Unit，SDU）。

| 层次 | 协议数据单元 |
| --- | --- |
| 应用层 | 应用报文（message） |
| 运输层 | TCP 报文段（segment）或 UDP 用户数据报（datagram） |
| 网络层 | IP 数据报或分组（packet） |
| 数据链路层 | 帧（frame） |
| 物理层 | 比特流（bit stream） |

![[1.6.4-各层协议数据单元.png|900]]

- PDU 描述同层实体之间的逻辑传送，SDU 描述相邻层实体之间的服务交付。
