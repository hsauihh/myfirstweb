- 编码与调制把计算机发出的数字基带信号转换成适合信道传输的信号。
- 编码用于数字信道，调制用于模拟信道。
- 码元是代表一个离散数值的基本波形，码元状态数决定一个码元能够携带的比特数。

==数字信道通过编码传输数字信号，模拟信道通过调制传输模拟信号；若有 $M$ 种码元状态，则每个码元可携带 $\log_2 M$ 比特。==

### 2.4.1 编码与调制的基本概念

- 消息可以表现为文字、图片、音频或视频。
- 数据是运送消息的实体。
- 信号是数据的电磁表现。
- 数字基带信号是计算机直接发出的数字信号。
- 码元是使用时间域波形表示数字信号时，代表不同离散数值的基本波形。

| 对比维度 | 编码 | 调制 |
| --- | --- | --- |
| 又称 | 基带调制 | 带通调制 |
| 输入 | 数字基带信号 | 数字基带信号 |
| 输出 | 另一种数字信号 | 模拟信号 |
| 适用信道 | 数字信道 | 模拟信道 |
| 应用示例 | 以太网使用曼彻斯特编码、4B/5B、8B/10B 等 | Wi-Fi 使用补码键控（CCK）、直接序列扩频（DSSS）、正交频分复用（OFDM）等 |

![[2.4-编码与调制关系.png|900]]

- 发送端根据信道类型选择编码或调制，接收端执行相反的信号还原过程。
- ==编码与调制的判断依据是输出信号和所用信道，不是消息本身的形式。==

### 2.4.2 常用编码方式

| 编码方式 | 波形规则 | 时钟信息 | 优点 | 局限 |
| --- | --- | --- | --- | --- |
| 双极性不归零编码 | 正电平和负电平分别表示两种比特，码元期间不回到零电平 | 不携带自同步信息 | 编码效率高 | 连续出现相同比特时难以划分码元边界，需要额外时钟信号 |
| 双极性归零编码 | 每个码元中间回到零电平 | 码元中间的跳变可提供同步 | 能够自同步 | 数据有效波形只占部分码元时间，编码效率低 |
| 曼彻斯特编码 | 每个码元中间必定发生电平跳变，跳变方向同时表示数据 | 中间跳变提供时钟 | 能够自同步 | 所需频带宽度较大 |
| 差分曼彻斯特编码 | 码元中间跳变只表示时钟，码元开始处是否跳变表示数据 | 中间跳变提供时钟 | 能够自同步，且不受信号极性反转影响 | 接收端必须结合前一码元判断数据 |

- 传统 $10\,\mathrm{Mb/s}$ 以太网采用曼彻斯特编码。

- 曼彻斯特编码可以约定正跳变表示 `0`、负跳变表示 `1`，也可以采用相反约定。
- 差分曼彻斯特编码可以约定码元开始处无跳变表示 `1`、有跳变表示 `0`，也可以采用相反约定。

![[2.4-常用线路编码对比.png|900]]

- 不归零编码在长串相同比特期间没有电平变化，接收端容易因收发时钟偏差而失去码元边界。
- 归零、曼彻斯特和差分曼彻斯特编码通过码元内部的电平跳变提供同步信息。

### 2.4.3 基本带通调制方法

| 调制方法 | 改变的载波参数 | 两种码元的区分方式 |
| --- | --- | --- |
| 调幅（Amplitude Modulation，AM） | 振幅 | 使用有载波和无载波，或不同振幅 |
| 调频（Frequency Modulation，FM） | 频率 | 使用不同频率 |
| 调相（Phase Modulation，PM） | 相位 | 使用不同初始相位 |

![[2.4-基本带通调制.png|900]]

- 调幅保持载波频率和相位不变，只改变振幅。
- 调频保持振幅不变，以不同频率表示不同码元。
- 调相保持频率和振幅不变，以不同相位表示不同码元。
- 频率与相位相互关联，通常不把调频和调相直接组合；相位可以与振幅组合形成混合调制。

### 2.4.4 正交振幅调制

- 正交振幅调制（Quadrature Amplitude Modulation，QAM）同时改变载波的相位和振幅。
- 星座图中的每个点代表一种码元波形，点的位置反映该码元的相位和振幅。
- QAM-16 具有 $16$ 种码元状态，每个码元可携带的比特数为：
  - $n=\log_2 16=4\,\mathrm{bit/码元}$
- 一般关系：
  - $n=\log_2 M$
  - $M=2^n$
- 变量：
  - $M$：可区分的码元状态数，无单位。
  - $n$：每个码元携带的比特数，单位为 `bit/码元`。
- 适用条件：每种码元状态必须能够由接收端可靠区分。
- 星座图常采用格雷码分配比特组合，使任意两个相邻码元只有一个比特不同。

![[2.4-QAM16星座图.png|900]]

- QAM-16 的 $16$ 个星座点分别对应一个四比特组合。
- ==在相邻星座点之间只改变一个比特，可以减小码元误判时产生的比特错误数量。==
