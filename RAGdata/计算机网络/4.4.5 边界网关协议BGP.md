- 边界网关协议（Border Gateway Protocol，BGP）是自治系统之间使用的外部网关协议。
- BGP 交换网络可达性及其路径属性，并按自治系统的路由策略选择可接受的路径。
- BGP 不以单一链路代价寻找全网最短路径，因为不同自治系统的度量和管理策略并不统一。
- **由TCP进行封装，属于应用层**
==BGP 的目标是在满足自治系统策略的前提下寻找可达且无环的路径，而不是计算统一度量下的最短路径。==

### 4.4.5.1 BGP 发言人

- 每个自治系统选择至少一台路由器作为 BGP 发言人。
- 相邻 BGP 发言人先建立 TCP 连接，再交换 BGP 报文，TCP 端口号为 `179`。
- BGP 发言人通告可达网络前缀及到达这些前缀所经过的自治系统序列等路径属性。
- 收到路由信息后，BGP 按本自治系统策略构造路由并向其他邻站传播。

![[4.4.5-BGP发言人.png|900]]

- 图中各自治系统通过 BGP 发言人建立邻站关系，并在 TCP 连接上交换可达性信息。

### 4.4.5.2 分层传播与路径选择

- 自治系统之间的 BGP 邻站负责交换外部路由信息。
- 自治系统内部还要把外部可达性传播给需要作出转发决策的其他 BGP 路由器。
- 路径中的自治系统序列可以用于检查环路；若本自治系统编号已出现在路径中，则拒绝该路由。
- 自治系统可以根据政治、经济、安全和服务关系等策略接受或拒绝某条路径。

![[4.4.5-BGP路径向量.png]]

- 图中主干网、地区 ISP 和本地 ISP 分层传播可达性，路由通告携带到达目的网络所经过的自治系统序列。

### 4.4.5.3 BGP-4 的四种报文

| 报文              | 作用                  |
| --------------- | ------------------- |
| 打开 OPEN         | 与邻站建立 BGP 关系并初始化通信  |
| 保活 KEEPALIVE    | 周期性确认邻站关系仍然有效       |
| 更新 UPDATE       | 发布新路由、撤销失效路由并携带路径属性 |
| 通知 NOTIFICATION | 报告检测到的差错并关闭 BGP 连接  |

![[4.4.5-BGP报文.png|900]]

- OPEN 建立关系，UPDATE 传递路由变化，KEEPALIVE 维护关系，NOTIFICATION 处理差错并终止连接。

### 4.4.5.4 RIP、OSPF 与 BGP 的封装

| 协议 | 承载方式 | 标识 |
| --- | --- | --- |
| RIP | UDP | 端口号 `520` |
| OSPF | 直接封装在 IP 数据报中 | IP 协议字段值 `89` |
| BGP | TCP | 端口号 `179` |

