- 物理层扩展可以延长站点与集线器之间的距离，也可以通过级联集线器接入更多站点。
- 光纤调制解调器完成电信号与光信号的转换，使远距离光纤连接成为可能。
- 级联集线器只扩大覆盖范围，不会分隔碰撞域。

==在物理层扩展以太网不会隔离碰撞；多个集线器级联后仍构成一个更大的碰撞域。==

### 3.4.4.1 扩展站点与集线器之间的距离

- 10BASE-T 中每个站点到集线器的距离不能超过 `100 m`，同一集线器上两个站点之间的最大布线距离为 `200 m`。
- 可以在站点与集线器之间使用一对光纤调制解调器，把电信号转换为光信号传输，从而延长连接距离。

![[3.4.4-光纤扩展以太网.png|900]]

- 两端的光纤调制解调器分别完成电/光和光/电转换，使站点可以通过光纤链路连接到集线器。

### 3.4.4.2 扩展网络范围和站点数量

- 一个集线器通常有 `8~32` 个接口。
- 可以级联多个集线器，扩大网络覆盖范围并接入更多站点。
- 多级集线器组成的共享式以太网仍是一个碰撞域。

![[3.4.4-物理层扩展碰撞域.png|900]]

- 各部门原本独立的碰撞域经主干集线器连接后合并为一个更大的碰撞域，接入站点过多会增加碰撞并降低平均吞吐量。
