- 以太网硬件多播把 IPv4 多播地址映射为目的多播 MAC 地址，使同一局域网中的组成员接收同一多播帧。
- IPv4 多播地址到以太网多播 MAC 地址的映射不是一一对应关系。
- 网卡按目的 MAC 地址接收多播帧后，网际层还必须按目的 IPv4 多播地址进行软件过滤。

==IPv4 多播地址只有低 `23 bit` 参与 MAC 地址映射，因此 `32` 个不同的 IPv4 多播地址可能映射为同一个以太网多播 MAC 地址。==

### 4.7.3.1 以太网多播MAC地址

- 以太网硬件多播把 IPv4 多播地址映射为目的多播 [[3.4.1 网络适配器和MAC地址|MAC 地址]]。
- 因特网号码分配机构（Internet Assigned Numbers Authority，IANA）分配的以太网 IPv4 多播 MAC 地址块为 `01-00-5E-00-00-00~01-00-5E-7F-FF-FF`。
- 该地址块的前 `25 bit` 固定，后 `23 bit` 可变。

![[4.7.3-多播MAC地址范围.png|900]]

- 多播 MAC 地址以 `01-00-5E` 开头，第 `4` 个字节的最高位固定为 `0`。

### 4.7.3.2 IPv4多播地址到MAC地址的映射

- IPv4 多播地址的低 `23 bit` 直接映射到多播 MAC 地址的低 `23 bit`。
- IPv4 多播地址中除固定前缀外还有 `5 bit` 不参与映射，因此映射不是一一对应关系。
- $2^5=32$ 个不同的 IPv4 多播地址可能映射为同一个多播 MAC 地址。
- 例如 `226.0.9.26` 和 `226.128.9.26` 都映射为 `01-00-5E-00-09-1A`。

![[4.7.3-IPv4多播地址到MAC地址映射.png|900]]

- 图中两个 IPv4 多播地址的低 `23 bit` 相同，未映射的 `5 bit` 不同，因此得到同一个多播 MAC 地址。

### 4.7.3.3 硬件接收与软件过滤

1. 发送方把 IPv4 多播地址映射为以太网目的多播 MAC 地址。
2. 局域网中的主机按网卡配置在 MAC 层接收或丢弃多播帧。
3. 由于多个 IPv4 多播地址可能映射到同一个 MAC 地址，接收帧的主机还要在网际层检查目的 IPv4 多播地址。
4. 网际层丢弃目的地址不属于本机所加入多播组的数据报。

![[4.7.3-硬件多播与软件过滤.png]]

- 图中属于 `226.0.9.26` 的主机可能因相同的目的 MAC 地址收到发往 `226.128.9.26` 的帧，最终由网际层软件过滤该数据报。
