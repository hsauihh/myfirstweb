- 载波监听多址接入/碰撞避免（Carrier Sense Multiple Access with Collision Avoidance，CSMA/CA）协议用于协调 802.11 无线局域网中多个站点对无线信道的使用。
- 无线站点难以在发送过程中可靠检测碰撞，因此 802.11 保留载波监听，并以碰撞避免、确认和退避机制取代碰撞检测。
- 请求发送和允许发送帧可以预约信道，并借助虚拟载波监听缓解隐藏站引起的碰撞。

==CSMA/CA 的核心是发送前监听并随机退避、接收后立即确认；它只能降低碰撞概率，不能保证碰撞绝不发生。==

### 3.9.3.1 采用 CSMA/CA 的原因

- CSMA/CA 保留了[[3.4.2 CSMA-CD协议#3.4.2.1 CSMA/CD 的基本原理|载波监听]]，但不再执行碰撞检测。
- 无线局域网不采用 CSMA/CD 的主要原因：
  - 无线信号强度的动态范围很大，站点自身的发送信号远强于到达本机的接收信号，难以在发送时检测碰撞。
  - 无线信号的覆盖范围有限，发送站即使能够监听本地信道，也不一定能发现接收站处发生的碰撞。
- 隐蔽站问题（Hidden Station Problem）是指两个站点都能与接收站通信，却因彼此不在对方的信号覆盖范围内而无法互相监听。
- 两个隐蔽站可能同时判断信道空闲并向同一接收站发送数据，使信号在接收站处碰撞。

![[3.9.3-隐藏站问题.png|900]]

- A 与 B 互相听不到对方，却都能到达 AP；二者同时发送时只能在 AP 处形成碰撞。

### 3.9.3.2 基本工作过程与虚拟载波监听

- 802.11 的分布式协调功能（Distributed Coordination Function，DCF）不设置中心控制器，各站点通过 CSMA/CA 争用信道。
- DCF 是 802.11 必须实现的接入方式。
- ==DCF 帧间间隔==（Distributed Interframe Space，DIFS）为 $128\,\mu\mathrm{s}$，用于发送数据帧和管理帧。
- ==短帧间间隔（==Short Interframe Space，SIFS）为 $28\,\mu\mathrm{s}$，是最短的帧间间隔，用于分隔一次对话中的各帧。

1. 源站有数据帧要发送时监听信道。
2. 信道空闲时，源站等待一个 DIFS 后发送数据帧。
3. 目的站正确收到数据帧后等待一个 SIFS，再返回确认帧（Acknowledgement，ACK）。
4. 源站没有收到 ACK 时，执行退避并重传数据帧。

- 无线信道误码率较高，802.11 使用停止-等待确认机制实现[[3.2.3 可靠传输#3.2.3.2 停止-等待协议|可靠传输]]。
- 数据帧首部的持续期字段给出本次通信占用信道的时间，其中包括目的站返回 ACK 所需的时间。
- 其他站收到数据帧后，根据持续期字段设置网络分配向量（Network Allocation Vector，NAV）。
- NAV 未减到零时，即使物理载波监听发现信道空闲，站点仍把信道视为忙碌，这种方式称为虚拟载波监听。

![[3.9.3-CSMA-CA基本过程.png|900]]

- 数据帧与 ACK 之间使用 SIFS，其他站在 NAV 指示的时间内保持静默。

### 3.9.3.3 退避算法

- 多个站点可能在信道刚转为空闲时同时开始发送，因此 CSMA/CA 让需要发送的站点在 DIFS 后随机退避。
- 站点在以下情况下必须执行退避：
  - 发送第一个数据帧前检测到信道忙。
  - 每次重传数据帧。
  - 成功发送一个数据帧后继续发送下一个数据帧。
- 第一个数据帧发送前若检测到信道空闲，可以等待 DIFS 后直接发送。
- 退避时间为：
  - $T_{\mathrm{backoff}}=rT_{\mathrm{slot}}$
- 第 $i$ 次退避时：
  - $r\in\{0,1,\ldots,2^{2+i}-1\}$
- 变量：
  - $T_{\mathrm{backoff}}$：随机退避时间，单位为 `s`。
  - $T_{\mathrm{slot}}$：基本退避时间，即一个时隙的长度，单位为 `s`。
  - $r$：随机选择的时隙编号，无单位。
  - $i$：退避次数，无单位。
- 适用条件：站点按 CSMA/CA 执行第 $i$ 次随机退避；从第 6 次退避开始，随机范围固定为 `0~255`。
- 常见变形：$T_{\mathrm{slot}}=T_{\mathrm{backoff}}/r$，其中 $r\ne0$。
- 当时隙编号上限达到 `255` 时，对应第 6 次退避，随机范围不再扩大。

1. 站点为退避计时器设置一个随机退避时间。
2. 信道保持空闲时，退避计时器持续递减。
3. 退避计时器减到零时，站点开始发送数据。
4. 计时器尚未减到零而信道转为忙时，站点冻结计时器的剩余值。
5. 信道重新空闲并经过 DIFS 后，站点从剩余值继续倒计时。

![[3.9.3-CSMA-CA退避算法.png|900]]

- 随退避次数增加而扩大随机时隙范围，可以降低再次碰撞的概率。

![[3.9.3-退避计时器冻结与恢复.png|900]]

- B、C、D 和 E 在信道忙时冻结剩余退避时间，在信道重新空闲并经过 DIFS 后继续倒计时，计时器最先归零的站点先发送。

### 3.9.3.4 RTS/CTS 信道预约

- 802.11 允许源站使用请求发送帧（Request To Send，RTS）和允许发送帧（Clear To Send，CTS）预约信道。
- RTS 是短控制帧，包含源地址、目的地址和本次通信所需的持续时间；该持续时间包括目的站返回 ACK 所需的时间。
- CTS 是目的站返回的短响应控制帧，也携带从 RTS 复制的本次通信持续时间。

1. 源站完成载波监听和退避后发送 RTS。
2. 目的站正确收到 RTS 后等待 SIFS，再返回 CTS。
3. 源站正确收到 CTS 后等待 SIFS，再发送数据帧。
4. 目的站正确收到数据帧后等待 SIFS，再返回 ACK。
5. 其他站只要收到数据帧、RTS 或 CTS 中的任意一种，就根据持续期字段设置 NAV。

- 源站发送 RTS 后没有收到 CTS，说明 RTS 可能发生碰撞，源站执行退避后重传 RTS。
- RTS 和 CTS 都是短控制帧，若发生碰撞，浪费的信道时间小于长数据帧碰撞造成的浪费。
- 尽管如此，802.11 无线局域网仍然为用户提供以下三种选择：
  1. 使用 RTS 帧和 CTS 帧。
  2. 只有当数据帧的长度超过某个数值时才使用 RTS 和 CTS 帧。
  3. 不使用 RTS 和 CTS 帧。
- RTS 帧和 CTS 帧会携带通信需要持续的时间，因此使用 RTS 帧和 CTS 帧进行信道预约也属于虚拟载波监听机制。
- 隐蔽站只要能监听到 RTS 或 CTS 中的一种，就能获知信道将被占用的时间并设置 NAV，因此减少了隐蔽站带来的碰撞问题。

![[3.9.3-RTS-CTS与NAV.png|900]]

- RTS、CTS、数据帧和 ACK 之间使用 SIFS；其他站依据 RTS 或 CTS 携带的持续期执行虚拟载波监听。
