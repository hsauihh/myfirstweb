- 网际组管理协议（Internet Group Management Protocol，IGMP）用于管理主机与本地多播路由器之间的多播组成员关系。
- 路由器只需确认直连网络中是否至少存在一个组成员，不需要统计成员数量。
- 主机通过成员报告与离开组报文表达状态，路由器通过成员查询持续检查成员关系。

==IGMP 使用随机延迟和成员报告抑制，使每个仍有成员的多播组至少产生一份报告，同时避免同组主机重复响应。==

### 4.7.5.1 报文类型与封装

- IGMPv3 由 RFC 3376 规定。
- IGMP 有成员报告、成员查询和离开组三种报文类型。
- IGMP 报文封装在 IP 数据报中，IPv4 首部协议字段值为 `2`。
- IGMP 报文使用多播目的地址，具体目的地址随报文类型而变化。

![[4.7.5-IGMP报文类型与封装.png]]

- IGMP 直接封装在 IP 数据报中，不经过 TCP 或 UDP。

### 4.7.5.2 加入多播组

1. 主机要加入多播组时，向该组的 IPv4 多播地址发送 IGMP 成员报告。
2. 成员报告所在 IP 数据报封装在目的地址为对应多播 MAC 地址的以太网帧中。
3. 本地多播路由器收到报告后，把该多播组加入自己的多播组列表。
4. 同一组的其他主机收到成员报告后，可以取消自己尚未发送的重复报告。

![[4.7.5-IGMP加入多播组与报告抑制.png]]

- 成员报告抑制减少了重复报文；路由器只需知道局域网上至少存在一个组成员，不需要统计成员数量，**即只关心自己直连网络中有哪些多播组存在**。

### 4.7.5.3 监视成员变化

1. 查询路由器默认每隔 `125 s` 向本网络所有主机发送 IGMP 成员查询。
2. 一般查询的组地址字段为 `0.0.0.0`，IP 目的地址为 `224.0.0.1`（**该 IP 地址为特殊 IP 多播地址，在本网络中所有参加多播的主机和路由器都会接收**），以太网目的地址为 `01-00-5E-00-00-01`。
3. 各组成员设置随机响应延迟，并在计时结束时发送成员报告。
4. 主机若在计时期间收到同组其他成员的报告，就取消自己的响应；不同组的成员仍分别响应。
5. 路由器根据收到的报告更新多播组列表；在查询后没有成员报告的组将被移除。
![[4.7.5-IGMP成员查询与随机响应.png]]

![[4.7.5-IGMP查询响应抑制.png]]

- 随机延迟与报告抑制避免同一组的所有主机同时响应查询，同时保证每个仍有成员的组至少产生一份报告。

### 4.7.5.4 查询路由器的选择

- 同一网络中可以连接多台多播路由器，但只需要一台查询路由器周期性发送成员查询。
- 多播路由器若监听到源 IP 地址小于自身 IP 地址的成员查询，就退出查询路由器选举。
- 最终由 IP 地址最小的多播路由器担任查询路由器，其他路由器被动接收响应并更新各自的多播组列表。

![[4.7.5-IGMP查询路由器选举.png|900]]

- 图中只有被选出的查询路由器主动发送查询，其他路由器仍能通过局域网中的成员报告更新状态。

### 4.7.5.5 退出多播组

- IGMPv2 增加了可选的离开组报文，主机可以主动通告退出而不必等待下一次成员查询。
- 离开组报文中的组地址指出要退出的多播组。
- 离开组报文的 IP 目的地址为 `224.0.0.2`，以太网目的地址为 `01-00-5E-00-00-02`，本网络中的所有多播路由器都能收到。
- 查询路由器收到离开组报文后，通过针对该组的成员查询确认是否仍有其他成员；没有响应时才从列表中移除该组。

![[4.7.5-IGMP离开组.png]]

- 离开组报文不能直接证明局域网上已无该组成员，因此路由器还需查询其他成员。
