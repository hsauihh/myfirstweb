- 交换式以太网使用以太网交换机连接站点或网段，并按照[[3.4.1 网络适配器和MAC地址#3.4.1.2 MAC 地址的结构|MAC 地址]]选择帧的转发接口。
- 以太网交换机本质上是具有多个接口的[[3.4.5 在数据链路层扩展以太网#3.4.5.1 网桥的结构与基本工作原理|网桥]]，通常称为以太网交换机（Switch）或二层交换机。
- 交换式以太网解决了早期网桥接口数量少、难以连接更多网段和站点的问题。

==只使用交换机而不使用集线器的以太网才称为交换式以太网；交换机是工作在数据链路层并包含物理层功能的多接口网桥。==

### 3.5.1 以太网交换机

- 网桥的接口数量通常只有 `2~4` 个，主要用于连接不同的网段。
- 1990 年面世的交换式集线器（Switching Hub）实质上是具有多个接口的网桥，后来通常称为以太网交换机或二层交换机。
- “二层”表示交换机工作在数据链路层，包括物理层功能。

![[3.5-网桥连接不同网段.png|900]]

- 图中网桥把两个网段连接起来；网桥两侧分别构成碰撞域，并扩大了以太网的覆盖范围。

![[3.5-以太网交换机特点.png|900]]

- 交换机内部通过基于主机通信的自学习算法逐步建立转发表。
- 交换机使用生成树协议（Spanning Tree Protocol，STP）构造能够连通全网且不产生环路的通信路径。
- 交换机的接口工作方式、自学习和转发方式见[[3.5.1 以太网交换机|以太网交换机]]。

### 3.5.2 共享式以太网和交换式以太网的对比

- 仅使用交换机、不使用集线器时，网络属于交换式以太网。
- 使用总线、集线器、物理层扩展或网桥扩展的网络属于[[3.4 共享式以太网|共享式以太网]]或其扩展形式。

![[3.5-交换式以太网拓扑.png|900]]

- 图中从小型交换式以太网到通过交换机在数据链路层扩展的拓扑，均由交换机完成帧转发。
- 两类以太网的逐项差异见[[3.5.2 共享式以太网和交换式以太网的对比|共享式以太网和交换式以太网的对比]]。
