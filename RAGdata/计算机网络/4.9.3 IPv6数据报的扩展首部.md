- IPv6 扩展首部位于基本首部与数据部分之间，并计入 IPv6 数据报的有效载荷。
- 扩展首部承载 IPv4 首部选项对应的控制功能，使基本首部保持固定和简洁。
- 基本首部和各扩展首部使用“下一个首部”字段按顺序连接。

==除逐跳选项扩展首部外，中间路由器不处理其他扩展首部，从而避免所有路由器检查与自身无关的选项。==

### 4.9.3.1 扩展首部链

1. IPv6 基本首部的“下一个首部”字段指出第一个扩展首部的类型。
2. 每个扩展首部的第一个字段也是 `8 bit` 的“下一个首部”字段。
3. 该字段继续指出后面扩展首部的类型；最后一个扩展首部再指出数据部分所使用的上层协议。

![[4.9.3-IPv6扩展首部链.png|900]]

- 图中基本首部、扩展首部 1、扩展首部 2 和数据部分通过“下一个首部”字段形成有序链。

### 4.9.3.2 扩展首部的处理

- IPv4 数据报在首部中使用选项字段时，传送路径上的全部路由器都要检查选项，降低了处理速度。
- IPv6 把选项移入扩展首部，通常由路径两端的源主机和目的主机处理。
- 逐跳选项扩展首部是例外，传送路径上的路由器需要对它进行处理。

### 4.9.3.3 六种扩展首部

RFC 2460 定义的扩展首部及规定顺序如下：

| 顺序 | 扩展首部 |
| --- | --- |
| 1 | 逐跳选项 |
| 2 | 路由选择 |
| 3 | 分片 |
| 4 | 鉴别 |
| 5 | 封装安全有效载荷 |
| 6 | 目的站选项 |

- 每种扩展首部由若干字段组成，长度可以不同。
- 使用多个扩展首部时，应按表中的先后顺序出现。


![[4.9.3-IPv6扩展首部类型.png]]
- 六种扩展首部分别提供逐跳控制、路径选择、分片、鉴别、安全封装和目的站选项功能。
