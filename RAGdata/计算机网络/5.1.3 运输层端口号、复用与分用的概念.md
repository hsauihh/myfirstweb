- 运输层端口号用于标识主机中的进程，端口号长度为 `16 bit`，取值范围为 `0–65535`。
- 发送方的运输层可以复用多个应用进程的数据，接收方的运输层可以依据端口号分用并交付给相应进程。
- 端口号只标识主机中运行的应用进程；不同主机上的相同端口号彼此没有关系。

==IP 地址定位主机，端口号定位主机中的应用进程；运输层通过“复用—分用”把多路应用通信承载在 IP 之上。==

### 5.1.3.1 端口号的分类

- 操作系统使用进程标识符（Process Identification，PID）标识本机进程，但不同操作系统使用不同格式的 PID。
- TCP/IP 体系结构使用统一的运输层端口号标识应用进程，使不同操作系统主机上的进程可以进行网络通信。
- 端口号由因特网号码分配机构（Internet Assigned Numbers Authority，IANA）分配或登记，运输层端口号总范围为 `0–65535`。
- TCP 端口号与 UDP 端口号之间没有联系，二者分别用于各自协议的复用和分用。
![[5.1.3-端口号与进程标识.png]]

| 类别 | 端口范围 | 使用方式 |
| --- | --- | --- |
| 熟知端口号 | `0–1023` | IANA 分配给 TCP/IP 体系结构应用层中的重要协议 |
| 登记端口号 | `1024–49151` | 没有熟知端口号的应用程序先向 IANA 登记，避免与其他应用重复 |
| 短暂端口号 | `49152–65535` | 由客户进程运行时动态选择，通信结束后由系统收回 |

| 应用协议 | 端口号 |
| --- | --- |
| 文件传送协议（File Transfer Protocol，FTP） | `21/20` |
| 简单邮件传送协议（Simple Mail Transfer Protocol，SMTP） | `25` |
| 域名系统（Domain Name System，DNS） | `53` |
| 动态主机配置协议（Dynamic Host Configuration Protocol，DHCP） | `67/68` |
| 超文本传送协议（Hypertext Transfer Protocol，HTTP） | `80` |
| 边界网关协议（Border Gateway Protocol，BGP） | `179` |
| 超文本传送安全协议（Hypertext Transfer Protocol Secure，HTTPS） | `443` |
| 路由信息协议（Routing Information Protocol，RIP） | `520` |

![[5.1.3-端口号分类.png]]

- 图中把 `0–65535` 划分为熟知、登记和短暂端口号，并列出 FTP、DNS、HTTP 等常见协议的端口示例。

### 5.1.3.2 发送方复用与接收方分用

- 复用发生在发送方：多个应用层进程把应用报文交给 UDP 或 TCP，运输层分别加上运输层首部，再交给 IP。
- UDP 在 IP 首部的协议字段中使用值 `17`，TCP 使用值 `6`；接收方先依据该字段把 IP 数据报分用给 UDP 或 TCP。
- 分用继续依据运输层首部中的目的端口号，把用户数据报或报文段交给对应的应用进程。

1. 发送方应用进程产生应用报文，并指定使用 UDP 或 TCP。
2. 对应运输层协议添加首部，形成用户数据报或 TCP 报文段。
3. IP 根据协议字段值 `17` 或 `6` 识别上层运输层协议并传送数据报。
4. 接收方先按 IP 协议字段分用到 UDP/TCP，再按目的端口号分用到目标应用进程。

![[5.1.3-复用与分用.png|900]]

- 图中发送方的 UDP 复用和 TCP 复用把多路应用报文汇入 IP；接收方执行相反方向的 UDP/TCP 分用。
- OSPF 不使用 TCP 或 UDP 进行封装，**而是直接使用网际层的 IP 进行封装，协议字段值为 89**。
![[5.1.3-UDP与TCP协议字段.png]]
### 5.1.3.3 端口号应用举例

- 用户 PC 的 IPv4 地址为 `192.168.0.1`，DNS 服务器为 `192.168.0.2`，Web 服务器为 `192.168.0.3`，域名为 `www.porttest.net`。
- 用户 PC 访问域名时，先向 DNS 服务器发送 UDP 查询：源端口为短暂端口 `49152`，目的端口为 DNS 的 `53`。
- DNS 返回域名对应的 Web 服务器地址后，用户 PC 向 Web 服务器发送 HTTP 请求：源端口仍可为 `49152`，目的端口为 HTTP 的 `80`。
- 通信结束后，系统收回客户端使用的短暂端口号。

1. 用户 PC 使用 UDP 源端口 `49152` 向 DNS 服务器的目的端口 `53` 发起域名查询。
2. DNS 服务器返回 `www.porttest.net` 对应的 IPv4 地址 `192.168.0.3`。
3. 用户 PC 使用 TCP 源端口 `49152` 向 Web 服务器的目的端口 `80` 发送 HTTP 请求。
4. Web 服务器按目的端口把请求交给 HTTP 应用进程，通信结束后客户端临时端口被系统回收。
![[5.1.3-DNS查询端口举例.png]]
![[5.1.3-HTTP请求端口举例.png]]

- 图中 HTTP 响应使用 TCP 源端口 `80`、目的端口 `49152` 返回用户 PC；通信结束后系统收回客户端的临时端口号。
