- IP 多播是一种一对多通信方式，源主机只发送一份数据，路由器在转发路径分支处复制数据报。
- IP 多播避免源主机和公共链路为每个接收者分别传送相同副本。
- 因特网多播负责跨网络转发，数据报到达目的局域网后仍需使用硬件多播交付。

==单播为每个接收者建立一对一传送，多播只沿共享路径传送一份数据，并在通往不同成员的分支处复制。==

### 4.7.1.1 单播与多播

- 单播是发送者与接收者之间的一对一通信，多播是发送者与一组接收者之间的一对多通信。
- 使用单播向多个接收者发送同一内容时，源主机和公共链路需要传送多个副本。
- 使用 IP 多播时，源主机只发送一份数据，路由器仅在通往不同成员的路径分支处复制数据。
- 同一局域网中的多个成员通过硬件多播接收同一个多播帧。

![[4.7.1-IP多播与单播比较.png|900]]

- 图中单播需要为每个接收者复制节目，多播则在路由分支处复制，并在局域网上使用硬件多播交付。

- 各多播地址的作用范围见[[4.7.2 IP多播地址和多播组#4.7.2.2 IPv4多播地址的作用域|IPv4 多播地址的作用域]]。

### 4.7.1.2 IP多播的应用范围

- IP 多播可以分为：
  - 只在本局域网上进行的硬件多播。
  - 在因特网上进行的多播。
- 主机通常通过局域网接入因特网，因此因特网多播到达目的局域网后仍需使用硬件多播交付给成员。

![[4.7.1-IP多播分类.png|900]]

- 因特网多播负责跨网络转发，硬件多播负责多播路由器直连局域网内的最终交付。
