- 密码学研究如何把可理解的明文转换成不可理解的密文，并在掌握正确密钥时恢复明文。
- 加密用于保护数据的保密性，也是实现多种网络安全机制的基础。

==现代密码体制的安全性应依赖密钥的保密，而不依赖加密算法和解密算法的保密。==

### 7.2.1.1 加密与解密

- 加密（Encryption）把明文（Plaintext）转换为密文（Ciphertext）。
- 解密（Decryption）把密文恢复为原来的明文。
- 加密算法 `E` 使用加密密钥 `K_A` 对明文 `X` 进行加密：$Y=E_{K_A}(X)$。
- 解密算法 `D` 使用解密密钥 `K_B` 对密文 `Y` 进行解密：$D_{K_B}(Y)=D_{K_B}(E_{K_A}(X))=X$。
- 加密密钥与解密密钥可以相同，也可以不同，但二者必须具有确定的相关性。

![[7.2.1-数据加密一般模型.png|900]]

- 加密密钥和解密密钥是否相同，决定了密码体制属于[[7.2.2 对称密钥密码体制|对称密钥密码体制]]还是[[7.2.3 公钥密码体制|公钥密码体制]]。

### 7.2.1.2 密码体制的安全性

- 无条件安全是指无论攻击者获得多少密文，密文都不包含足以唯一确定对应明文的信息，也称理论上不可破。
- 实用密码体制通常并非无条件安全，而是要求在现实的计算资源和限定时间内无法被破译，即在计算上是安全的。
- 增加密钥长度通常可以提高穷举破解的难度，但也会增加加密方和解密方的计算量。

![[7.2.1-密码体制安全性.png|900]]

- ==评价实用密码体制时，关注的是计算上不可破，而不是理论上绝对不可破。==
