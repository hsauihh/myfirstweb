- TCP 默认使用累计确认，只能确认按序到达的最高字节序号；失序到达但已正确接收的数据块无法通过普通确认号单独说明。
- 选择确认（Selective Acknowledgement，SACK）允许接收方通过 TCP 选项告知发送方已经收到的非连续字节块。
- 发送方可据此只重传缺失数据，而不重传已经正确到达的字节块。

==SACK 只报告已经收到的非连续字节块边界，不改变 TCP 确认号的累计确认含义。==
![[5.3.8-选择确认问题.png]]
### 5.3.8.1 SACK 选项

- 接收方使用两个边界值描述一个已收到的连续字节块：
  - 左边界是该字节块第一个字节的序号。
  - 右边界是该字节块最后一个字节序号加 `1`。
- TCP 连接双方需要在建立连接时通过“允许 SACK”选项协商使用 SACK。
- “允许 SACK”选项占 `2` 字节。
- SACK 选项由 `2` 字节的种类与长度字段，加若干个字节块边界组成。
- 每个字节块使用两个 `32 bit` 边界值，共占 `8` 字节。
- TCP 首部选项最长 `40` 字节，因此一个 SACK 选项最多报告 `4` 个字节块。

![[5.3.8-TCP选择确认.png]]

- 图中接收方用左右边界分别报告两个已收到的非连续字节块，使发送方能够定位两处缺失区间。
