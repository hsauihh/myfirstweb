- [[4.2.2 IPv4地址及其编址方法|网际协议第 4 版（IPv4）地址]]应用规划根据各网络的主机数量，为每个网络分配互不重叠且容量足够的地址块。
- 定长子网掩码（Fixed Length Subnet Mask，FLSM）为所有子网使用相同前缀长度，变长子网掩码（Variable Length Subnet Mask，VLSM）则按不同规模为子网选择不同前缀长度。
- 地址规划必须同时预留每个子网的网络地址和广播地址。

==选择地址块时必须同时满足“容量足够、边界对齐、彼此不重叠”；采用 VLSM 时应优先分配需求最大的子网。==

### 4.2.3.1 定长与变长子网掩码

| 对比维度 | 定长子网掩码 FLSM | 变长子网掩码 VLSM |
| --- | --- | --- |
| 子网掩码 | 所有子网相同 | 不同子网可以不同 |
| 地址块大小 | 所有子网相同 | 按主机数量选择 |
| 规划方式 | 以最大子网需求确定统一块大小 | 由大到小分配不同大小的块 |
| 优点 | 规则简单、便于管理 | 地址利用率高 |
| 局限 | 小网络会浪费大量地址 | 规划与配置较复杂 |

- FLSM 使用同一子网掩码划分所有子网；VLSM 允许各子网使用不同长度的网络前缀。

### 4.2.3.2 地址块容量计算

- 若一个子网需要容纳 $N$ 个接口，应选择最小的主机号位数 $h$，使 $2^h-2\geq N$。
- $N$ 表示所需接口地址数量，单位为“个”；$h$ 表示主机号长度，单位为 `bit`。
- 相应地址块包含 $2^h$ 个地址，单位为“个”，前缀长度为 $32-h$。
- 地址块起始地址必须落在块大小的整数倍边界上。
- 可分配范围从网络地址后的第一个地址开始，到广播地址前的一个地址结束。

### 4.2.3.3 FLSM 规划步骤

1. 找出主机接口需求最大的网络，并计入路由器接口。
2. 选择能满足该网络需求的最小主机号位数。
3. 用同一前缀长度把申请到的地址块等分为多个子网。
4. 依次为各网络分配网络地址、可用主机地址范围和广播地址。
5. 检查子网数量、主机容量与地址范围是否满足要求。


![[4.2.3-定长子网掩码规划.png]]
- 图中所有网络统一使用 `/27` 前缀，每个地址块包含 `32` 个地址并可分配给 `30` 个接口。

### 4.2.3.4 VLSM 规划步骤

1. 按各网络所需接口数量从大到小排序。
2. 为最大网络选择最小可容纳它的地址块，并从可用空间的对齐边界开始分配。
3. 在剩余地址空间中依次为较小网络分配地址块。
4. 为每个子网记录前缀长度、网络地址、可用地址范围和广播地址。
5. 检查所有地址块是否互不重叠且均位于申请到的总地址块内。

![[4.2.3-定长子网地址规划.png]]

- 图中同一总地址块被划分为大小不同的子网地址块，每个块的边界和容量均由其网络前缀确定。
