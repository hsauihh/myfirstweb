- 网络体系结构的不同层次可以分别提供安全服务：物理层保护传输信道，数据链路层保护局域网链路，网络层保护 IP 数据报，运输层保护端到端连接，应用层保护具体应用数据。
- 将安全机制部署在较低层可以对上层透明地保护更多业务，部署在较高层则更容易针对应用需求提供细粒度保护。

==安全协议部署在哪一层，决定了受保护的数据范围、通信端点和可提供的安全服务；没有单一层次能够替代其他各层的安全机制。==

### 7.7.1 物理层和数据链路层

#### 物理层信道加密

- 物理层通过信道加密机保护信息的保密性和完整性。
- 点到点加密在每段链路上分别解密并重新加密，链路中间设备会接触明文。
- 信道加密对上层协议和用户透明，但不能提供端到端保护。

#### IEEE 802.11i

- 无线信号在自由空间传播，接入点覆盖范围内的终端都可能接收到信号，因此无线局域网需要专门的安全机制。
- 早期 IEEE 802.11 使用服务集标识符（Service Set Identifier，SSID）匹配、MAC 地址过滤和有线等效保密（Wired Equivalent Privacy，WEP），这些机制的安全性较弱。
- IEEE 802.11i 的商业名称为 WPA2，提供可扩展的鉴别机制、更强的加密算法和密钥分发机制。
- 无线局域网受保护的接入（Wi-Fi Protected Access，WPA）是从 WEP 向 IEEE 802.11i 过渡的安全机制。

![[7.7-802.1X鉴别流程.png|900]]

- 终端先与接入点发现彼此的安全能力，再通过接入点与鉴别服务器双向鉴别；鉴别过程使用可扩展鉴别协议（Extensible Authentication Protocol，EAP）框架，并生成主密钥。
- 接入点从鉴别服务器取得成对主密钥（Pairwise Master Key，PMK），随后接入点与终端相互鉴别并使用 PMK 生成临时密钥。
- IEEE 802.11i 可使用临时密钥完整性协议（Temporal Key Integrity Protocol，TKIP）或高级加密标准（Advanced Encryption Standard，AES）保护无线帧。

### 7.7.2 网络层：IPsec

- IP 安全（IP Security，IPsec）是一组为网络层提供安全服务的协议族，不限定必须使用某一种加密或鉴别算法。
- IPsec 支持 IPv4 和 IPv6，并包含运输方式和隧道方式。

![[7.7-IPsec两种工作方式.png|900]]

- 运输方式保护原 IP 数据报的数据载荷，保留原 IP 首部，通常用于两台主机之间。
- 隧道方式保护整个原 IP 数据报，并在外部添加新的 IP 首部，常用于路由器之间或主机与路由器之间建立安全隧道。
- 两种方式生成的 IP 安全数据报首部都不加密，以便中间路由器识别并转发。

#### 安全关联

- 安全关联（Security Association，SA）是通信双方建立的单向逻辑连接。
- 每条 SA 由安全参数索引（Security Parameter Index，SPI）和目的 IP 地址唯一标识。
- SA 还记录所使用的安全协议、加密与完整性算法、密钥及密钥生存期。
- 双向安全通信需要分别建立两个方向的 SA。

#### AH 与 ESP

| 协议 | 完整性与源点鉴别 | 保密性 |
| --- | --- | --- |
| 鉴别首部（Authentication Header，AH） | 提供 | 不提供 |
| 封装安全有效载荷（Encapsulating Security Payload，ESP） | 提供 | 提供 |

- ESP 比 AH 提供的安全服务更多，实际使用更广泛。

![[7.7-IPsec-ESP封装.png|900]]

- 隧道方式下，ESP 首部中的 SPI 用于查找对应 SA，序号用于抵御重放攻击；原 IP 数据报和 ESP 尾部被加密，ESP 首部至尾部接受完整性鉴别，外部的新 IP 首部用于转发。
- 安全关联数据库（Security Association Database，SAD）保存实体可使用的 SA。
- 安全策略数据库（Security Policy Database，SPD）规定不同 IP 数据报应执行的 IPsec 处理。
- 因特网密钥交换（Internet Key Exchange，IKE）通过[[7.5 密钥分发|自动密钥管理]]建立 SA。

### 7.7.3 运输层：SSL/TLS

- 安全套接层（Secure Sockets Layer，SSL）与运输层安全（Transport Layer Security，TLS）位于应用层和运输层之间，为基于 TCP 的应用提供安全服务。
- SSL/TLS 可以提供服务器鉴别、可选的客户鉴别和加密会话。
- HTTPS 使用 SSL/TLS 保护 HTTP，默认 TCP 端口为 `443`。

![[7.7-SSL安全会话流程.png|900]]

1. 浏览器发送自己支持的加密算法，服务器选择双方都支持的算法。
2. 服务器发送数字证书，浏览器使用认证中心的公钥验证证书及服务器身份。
3. 浏览器生成随机秘密数，使用服务器公钥加密后发送。
4. 双方根据该秘密数和协商的算法生成共享的对称会话密钥。
5. 后续应用数据使用会话密钥加密传输。

- [[7.2.3 公钥密码体制|公钥密码体制]]用于鉴别和建立共享秘密，[[7.2.2 对称密钥密码体制|对称密钥密码体制]]用于高效保护会话数据。

### 7.7.4 应用层：PGP

- 相比于计算机网络体系结构的其他各层，在应用层实现安全服务较为简单。
- 良好保密（Pretty Good Privacy，PGP）用于保护电子邮件的保密性、完整性、来源鉴别和防抵赖。

![[7.7-PGP收发流程.png|900]]

1. 发送方计算邮件摘要，并用自己的私钥对摘要进行数字签名。
2. 发送方把签名附加到邮件，使用随机产生的一次性会话密钥加密扩展后的邮件。
3. 发送方用接收方公钥加密一次性会话密钥，并与加密邮件一起发送。
4. 接收方用自己的私钥恢复会话密钥，再解密邮件。
5. 接收方使用发送方公钥验证签名，并重新计算邮件摘要进行比较。

- PGP 通过[[7.3.2 数字签名|数字签名]]保护来源与完整性，通过一次性会话密钥保护邮件内容，并使用公钥保护会话密钥。
- PGP 不强制要求使用认证中心签发的证书，也允许使用第三方签署等方式确认公钥归属。
