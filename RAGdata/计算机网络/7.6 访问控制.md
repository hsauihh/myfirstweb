- 访问控制依据预定策略限制主体对系统资源的访问，使通过[[7.4 实体鉴别|身份鉴别]]的用户只能执行被授权的操作。
- 身份鉴别是第一道安全防线，访问控制是第二道安全防线。

==访问控制的核心是依据访问策略判断主体能否以某种方式访问客体；授权应遵循最小特权原则，只授予完成工作所必需的权限。==

### 7.6.1 基本概念

![[7.6-身份鉴别与访问控制.png|900]]

- 身份鉴别把未授权用户隔离在系统之外，访问控制限制合法用户只能访问授权范围内的资源。
- 最小特权是指用户不会获得超出实际工作需要的权限。

| 要素 | 含义 |
| --- | --- |
| 主体 | 访问活动的发起者，可以是用户、进程、服务或设备 |
| 客体 | 被访问的信息、文件、设备或服务等资源 |
| 访问 | 主体对客体实施的创建、读取、修改、删除、执行、发送或接收等操作 |
| 访问策略 | 规定哪些主体可以对哪些客体执行哪些操作 |

- 访问策略可以用三元组 `(S, O, P)` 描述，其中 `S` 表示主体，`O` 表示客体，`P` 表示许可。

![[7.6-访问控制基本要素.png|900]]

### 7.6.2 访问监控器模型

- 主体把访问请求发送给访问监控器。
- 访问监控器向授权服务器查询访问策略，并据此允许或禁止主体访问客体。
- 访问监控器把主体、客体、访问类型、访问时间和决策结果写入审计模块。
- 系统管理员通过审计记录检查被拒绝的访问、策略执行情况，并为事后追查提供证据。

![[7.6-访问监控器模型.png|900]]

- 如果访问规则在配置或执行过程中存在失误，审计记录可以帮助发现并纠正违规访问。

### 7.6.3 三类典型访问控制策略

| 对比维度 | 自主访问控制 DAC | 强制访问控制 MAC | 基于角色的访问控制 RBAC |
| --- | --- | --- | --- |
| 权限决定者 | 客体拥有者 | 系统管理员依据安全级别 | 角色关联许可，用户关联角色 |
| 优点 | 灵活 | 能严格限制信息流 | 降低授权复杂度，便于管理 |
| 局限 | 权限分散，易被恶意程序利用 | 灵活性较低 | 依赖角色和许可设计是否合理 |

#### 自主访问控制 DAC

- 自主访问控制（Discretionary Access Control，DAC）允许客体拥有者自主决定其他主体访问该客体的权限。
- DAC 灵活，但权限管理分散，难以约束已经取得合法主体权限的恶意程序。
- 木马可以诱骗有读取权限的用户运行程序，读取受保护文件后写入攻击者可访问的新文件，再把权限授予攻击者。

![[7.6-DAC木马泄露.png|900]]

- 图中木马借用了合法用户 A 的权限，使 DAC 无法阻止其读取并转存机密信息。

#### 强制访问控制 MAC

- 强制访问控制（Mandatory Access Control，MAC）由系统管理员为主体和客体设置安全级别。
- “下读”允许主体读取低于或等于自身级别的客体，“上写”允许主体写入高于或等于自身级别的客体。
- 这种信息流限制可以阻止木马把高级别信息写入低级别客体。

![[7.6-MAC阻断木马泄露.png|900]]

- 图中攻击者和木马的级别低于文件，因此“下读”规则阻止机密文件向低级别主体泄露。

#### 基于角色的访问控制 RBAC

- 基于角色的访问控制（Role-Based Access Control，RBAC）以角色连接用户和许可。
- 用户与角色之间、角色与许可之间都可以形成多对多关系。
- 角色与许可的关系通常比用户与角色的关系稳定；职责变化时只需调整用户所属角色。

![[7.6-RBAC用户角色许可.png|900]]

- 通过角色对用户与许可进行逻辑隔离，可以降低授权复杂度、提高权限可管理性并减少授权失误。
