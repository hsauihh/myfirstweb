- 网际协议第 6 版（Internet Protocol version 6，IPv6）用于解决 [[4.2.2 IPv4地址及其编址方法|IPv4 地址]]空间不足，并改进地址层次、首部结构和自动配置能力。
- IPv4 的 `32 bit` 地址长度和早期编址方法难以适应因特网的全球普及，网络地址转换只能延缓地址耗尽。
- IPv6 把地址长度扩展到 `128 bit`，同时为后续功能扩展和实时业务提供新的机制。

==IPv6 不只是把地址从 `32 bit` 扩展到 `128 bit`，还重新设计了地址层次、首部、选项、自动配置和资源预分配机制。==

### 4.9.1.1 IPv6 的诞生背景

- IPv4 在 20 世纪 70 年代末设计时，没有预见到因特网会在全球范围广泛使用。
- 分类编址和划分子网等早期编址方法造成了 IPv4 地址资源浪费。
- 2011 年 2 月 3 日，因特网号码分配管理局（Internet Assigned Numbers Authority，IANA）宣布 IPv4 地址已经分配完毕。
- 网络地址转换（Network Address Translation，NAT）延长了 IPv4 的使用时间，但没有从根本上增加地址空间。
- 因特网工程任务组（IETF）从 1992 年开始研究下一代 IP，最初称为 IPng，后来正式命名为 IPv6。
- 从 IPv4 直接转换到 IPv6 不可行，协议升级只能逐步推进。

![[4.9.1-IPv6诞生背景.png|900]]

- IPv4 地址长度和编址方法共同造成地址资源紧张，IPv6 的首要目标是提供更大的地址空间。

### 4.9.1.2 IPv6 引进的主要变化

| 变化 | 作用 |
| --- | --- |
| 更大的地址空间 | 把地址长度从 `32 bit` 扩展到 `128 bit` |
| 扩展的地址层次结构 | 支持更多层次，更灵活地反映网络拓扑并设计寻址和路由层次 |
| 灵活的首部格式 | 使用可选扩展首部增加功能，并减少路由器不必要的首部处理 |
| 改进的选项 | 允许分组携带含选项的控制信息，并可定义新的选项 |
| 允许协议继续扩充 | 为不断出现的新应用保留扩展能力 |
| 支持即插即用 | 主机或路由器能够自动配置 IPv6 地址和其他网络参数，可不依赖动态主机配置协议（Dynamic Host Configuration Protocol，DHCP） |
| 支持资源的预分配 | 为实时音视频等对带宽和时延有要求的应用提供服务质量保证 |

![[4.9.1-IPv6主要变化.png|900]]

- 七项变化分别面向地址容量、路由层次、处理效率、功能扩展、自动配置和实时业务需求；具体首部机制见 [[4.9.2 IPv6数据报的基本首部|IPv6 基本首部]]与 [[4.9.3 IPv6数据报的扩展首部|IPv6 扩展首部]]。
