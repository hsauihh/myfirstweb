- 密钥分发是把密钥安全地交付给通信实体的过程，是密码系统安全运行的基础。
- [[7.2.2 对称密钥密码体制|对称密钥]]必须由通信双方共享并保密；[[7.2.3 公钥密码体制|公钥]]可以公开，但私钥仍必须保密。

==秘密密钥在网络上传送时必须受到其他密钥保护；公钥不要求保密，但必须可靠验证其与合法实体之间的绑定关系。==

### 7.5.1 密钥分发方式

- 密钥可以在网络之外通过可信信使分发，也可以在网络内自动分发。
- 网外分发需要定期更换密钥，难以适应用户数量不断增长的网络。
- 网内自动分发通常借助密钥分发中心或证书系统建立所需密钥。

![[7.5-密钥分发方式.png|900]]

- 图中对称密钥和私钥需要保密，公钥可以公开；两类密码体制都必须解决密钥与合法实体之间的安全绑定问题。

### 7.5.2 对称密钥的分发

- 密钥分发中心（Key Distribution Center，KDC）为登记用户保存用户私有的主密钥。
- 用户 A 和 B 分别预先拥有与 KDC 通信的主密钥 `K_A` 和 `K_B`。
- `K_AB` 是 KDC 为 A 与 B 的一次会话生成的会话密钥。

1. A 向 KDC 请求与 B 通信。
2. KDC 生成 `K_AB`，用 `K_A` 加密 `K_AB`，并生成用 `K_B` 加密的票据，其中包含 A、B 和 `K_AB`。
3. A 使用 `K_A` 解密得到 `K_AB`，再把票据转交给 B。
4. B 使用 `K_B` 解密票据，得到 `K_AB`。
5. A 与 B 使用 `K_AB` 保护本次会话。

![[7.5-KDC分发会话密钥.png|900]]

- 网络上传送的 `K_AB` 都处于主密钥保护之下，解密用的主密钥不在网络上传送。
- KDC 可以在报文中加入时间戳以抵御重放攻击。
- 用户主密钥应定期更换，以减少密钥被破译后的影响。

### 7.5.3 公钥的分发

- 随意发布的公钥可能被攻击者替换，因此必须可靠证明公钥属于所声称的实体。
- 认证中心（Certification Authority，CA）通过签发数字证书，把用户身份与公钥绑定。

1. CA 核实用户的真实身份。
2. CA 为用户生成公钥与私钥，并生成包含身份和公钥的证书。
3. CA 使用自己的私钥对证书进行数字签名。
4. 证书可以通过网络发送或存放在服务器中供其他用户取得。
5. 验证者使用从可信渠道获得的 CA 公钥核验签名，确认公钥与证书主体的绑定。

![[7.5-CA签发与验证证书.png|900]]

- CA 生成的私钥必须由证书持有者自行秘密保存。
- 验证证书之前，必须先可信地取得签发该证书的 CA 公钥。

### 7.5.4 公钥基础结构

- 单个 CA 面临负载过重和单点失效问题，因此实际系统使用层次化 CA。
- 公钥基础结构（Public Key Infrastructure，PKI）在全球范围提供证书签发和认证服务。
- 下级 CA 的证书由上级 CA 签发和认证。
- 验证不受信任 CA 签发的证书时，应沿证书链逐级向上验证，直至到达受信任的 CA，最终可能到达根 CA。

![[7.5-PKI层次结构.png|900]]

- 用户直接信任根 CA；中间 CA 是否可信，则由从该 CA 到可信根的证书链决定。
