- 载波监听多址接入/碰撞检测协议用于协调共享式以太网上多个站点对同一信道的使用。
- 站点发送前监听信道，发送过程中检测碰撞，发生碰撞后停止发送并随机退避。
- 传播时延决定争用期和最小帧长，并影响共享信道的利用率。

==CSMA/CD 只能检测并处理碰撞，不能避免传播时延导致的碰撞；站点必须边发送边检测。==

### 3.4.2.1 CSMA/CD 的基本原理

- 载波监听多址接入/碰撞检测（Carrier Sense Multiple Access with Collision Detection，CSMA/CD）由三个部分组成：
  - 多址接入（Multiple Access，MA）：多个站点连接在一条总线上并竞争使用总线。
  - 载波监听（Carrier Sense，CS）：站点在发送前检测信道是否空闲。
  - 碰撞检测（Collision Detection，CD）：站点在发送过程中继续检测是否发生碰撞。

1. 站点有帧待发送时，先监听信道。
2. 信道空闲时，等待 ==`96 bit` 时间的帧间最小间隔==后开始发送。
3. 信道忙时持续监听；信道转为空闲后仍等待帧间最小间隔，再开始发送。这种“忙则持续监听、空闲后立即争用”的方式采用[[3.10 介质访问控制#3.10.3 CSMA 的 1-坚持、非坚持和 p-坚持策略|1-坚持 CSMA]]思想。
4. 站点一边发送一边检测碰撞。
5. 检测到碰撞后立即停止发送当前帧，并继续发送 `32 bit` 或 `48 bit` 的人为干扰信号。
6. 站点退避一个随机时间后再次尝试发送。

![[3.4.2-CSMA-CD碰撞处理.png|900]]

- 载波监听可以减少碰撞概率，但传播时延使多个站点仍可能同时判断信道空闲并开始发送。
- CSMA/CD 只能在发送时检测碰撞，因此共享式以太网只能进行[[2.3 传输方式#2.3.3 单工、半双工和全双工|半双工通信]]。

### 3.4.2.2 争用期

- 设端到端的单程传播时延为 $\tau$。
- 一个站点从开始发送起，最多经过 $2\tau$ 就能检测到本次发送是否发生碰撞。
- $2\tau$ 称为==争用期==或==碰撞窗口==。
- 如果站点在争用期内没有检测到碰撞，本次发送就不会再发生碰撞。

![[3.4.2-争用期时序.png|900]]

- 两个最远站点几乎同时发送时，碰撞信号最迟在一个往返传播时延后返回先发送的站点。
- 争用期随网络端到端距离增大而增大，因此共享式以太网必须限制网络覆盖范围和站点数量。

### 3.4.2.3 最小帧长与最大帧长

- `10 Mb/s` 共享式以太网把争用期规定为 `512 bit` 时间，即 $51.2\,\mu\mathrm{s}$。
- 最小帧长必须保证发送方在完成整个帧之前仍能检测到最远处产生的碰撞：
  - $L_{\min}=R\times2\tau$
- 变量：
  - $L_{\min}$：最小帧长，单位为 `bit` 或 `B`。
  - $R$：数据传输速率，单位为 `bit/s`。
  - $\tau$：端到端单程传播时延，单位为 `s`。
- 对传统 `10 Mb/s` 共享式以太网,最小帧长为：
  - $L_{\min}=10\,\mathrm{Mb/s}\times51.2\,\mu\mathrm{s}=512\,\mathrm{bit}=64\,\mathrm{B}$
- 适用条件：站点必须在发送帧期间持续执行碰撞检测。
- 常见变形：$2\tau=L_{\min}/R$。

![[3.4.2-最小帧长.png|900]]

- 帧长小于 `64 B` 时，发送方可能在碰撞信号返回之前已经发送完毕，从而无法发现碰撞。
- 接收方收到长度小于 `64 B` 的帧时，可以判定它是碰撞造成的无效帧并将其丢弃。

| 以太网 V2 MAC 帧字段 | 长度 |
| --- | --- |
| 目的地址 | `6 B` |
| 源地址 | `6 B` |
| 类型 | `2 B` |
| 数据载荷 | `46~1500 B` |
| FCS | `4 B` |

- 以太网 V2 MAC 帧长度为 `64~1518 B`。
- 数据载荷设置最小长度可以满足最小帧长，设置最大长度可以避免一个站点长时间独占共享信道。

![[3.4.2-以太网V2帧格式.png|900]]

- 帧首部和尾部共 `18 B`；数据载荷最少为 `46 B` 时，整个 MAC 帧恰好为 `64 B`。

### 3.4.2.4 截断二进制指数退避

- 站点检测到碰撞并发送人为干扰信号后，使用截断二进制指数退避算法选择重传时间。
- 退避时间：$T_{\mathrm{backoff}}=r\times2\tau$
- 随机数 $r$ 从集合 $\{0,1,\ldots,2^k-1\}$ 中选取。
- $k=\min(N,10)$，其中 $N$ 是本帧已经发生的重传次数。
- 第 1 次重传时，$r$ 从 $\{0,1\}$ 中选择；第 2 次重传时，从 $\{0,1,2,3\}$ 中选择。
- 从第 10 次重传开始，$k$ 保持为 `10`，随机数范围不再扩大。
- 连续 `16` 次重传仍不成功时，站点丢弃该帧并向高层报告错误。

![[3.4.2-截断二进制指数退避.png|900]]

- 随重传次数增加而扩大随机数范围可以降低再次碰撞的概率；把 $k$ 截断为 `10` 可以限制最大退避范围。

### 3.4.2.5 信道利用率

- 在总线一空闲就有站点发送、各站点都不发生碰撞的理想条件下：
  - $S_{\max}=\dfrac{T_0}{T_0+\tau}=\dfrac{1}{1+a}$
  - $a=\dfrac{\tau}{T_0}$
- 变量：
  - $S_{\max}$：极限信道利用率，无单位。
  - $T_0$：一帧本身的发送时间，单位为 `s`。
  - $\tau$：端到端单程传播时延，单位为 `s`。
  - $a$：传播时延与帧发送时间之比，无单位。
- 一帧占用总线的时间为 $T_0+\tau$，其中只有 $T_0$ 用于实际发送该帧。

![[3.4.2-信道利用率.png|900]]

- $a$ 越小，极限信道利用率越高；缩短网络端到端距离或增加帧的发送时间都可以减小 $a$。
