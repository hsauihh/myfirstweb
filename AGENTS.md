# AGENTS.md

## 环境

* 使用 bash，不使用 PowerShell / CMD。
* Python 包管理统一使用 `uv`，禁止使用 `pip`。
* 使用 `bash` 命令 `date` 获取当前时间。
* 优先使用项目现有的工具链、包管理器和运行方式，不擅自更换技术栈。

## 语言

* 注释、Docstring、设计文档、提交信息一律使用中文，专业术语除外。
* 代码中的变量名、函数名、类名等按照项目现有命名规范执行。
* 面向用户的界面文案遵循项目现有语言规范，不因个人偏好擅自修改。

## 核心原则

### 1. KISS

* 优先选择简单、直接、可维护的方案。
* 能用简单结构解决的问题，不引入复杂架构。
* 不为了体现设计模式、抽象能力或技术先进性而增加不必要的层次。

### 2. 解决根因

* 优先解决问题的根因，而不是持续堆叠补丁。
* 局部问题采用最小必要修改。
* 发现明确的结构性问题时进行必要重构。
* 不为了“看起来更优雅”而进行与当前任务无关的重构。

### 3. 长期维护

* 项目面向长期维护，新增代码应降低未来开发成本。
* 优先选择稳定、成熟、社区活跃的方案。
* 避免短期主义、特殊分支和一次性补丁。
* 除非有明确需求，不主动增加兼容层、历史包袱或过时 API。

### 4. 第三方依赖

* 优先使用成熟稳定的第三方库解决通用问题，不重复造轮子。
* 新增依赖必须具有明确收益。
* 简单功能不得为了避免少量代码而引入重量级依赖。
* 引入新依赖前检查项目已有依赖，避免功能重复。

### 5. 控制修改范围

* 只修改完成当前任务所必需的文件和代码。
* 不主动处理与当前任务无关的问题。
* 不主动重构无关代码、升级无关依赖或调整无关配置。
* 发现潜在问题但与当前任务无关时，记录并告知用户，不擅自处理。

### 6. 不做形式主义

* 规则的目的在于降低复杂度、提高可维护性，而不是机械满足指标。
* 不得为了满足代码行数、参数数量或抽象层级要求而进行无意义拆分。
* 任何重构都应使代码更容易理解、测试或维护。

### 7. 需求判断

* 需求不明确且会影响实现方案、数据结构、接口行为或架构设计时，先向用户确认。
* 对低风险、局部、可逆的细节，可依据现有代码、项目约定和上下文合理判断后直接执行。
* 严禁擅自扩大需求范围。
* 不猜测业务规则；实现细节可以合理推断，但业务行为必须有依据。

### 8. 文档唯一信息源

* 文档保持精简，只负责索引、约定和必要信息。
* 不重复描述代码中已经明确表达的实现细节。
* 同一信息只保留一个权威来源，避免多处维护导致不一致。

### 9. 错误沉淀

* 对具有复现价值、容易再次发生或会影响后续开发的错误，将原因和解决方案记录到 README 或对应开发文档中。
* 一次性的普通编码错误无需记录。
* 错误记录应关注可复用经验，而不是记录完整开发过程。

---

# 行为规范

## 开发前

### 1. 检查项目状态

* 首先检查当前 Git 工作区状态。
* 不得覆盖、撤销或删除用户已有的未提交修改。
* 发现与当前任务无关的工作区变更时，默认保留原状。
* 不擅自执行 `git reset --hard`、批量删除或其他可能破坏用户修改的操作。

### 2. 阅读项目

首次进入项目时：

1. 查看 `README.md`。
2. 查看项目目录结构。
3. 查看与当前任务相关的配置文件。
4. 查看与当前任务直接相关的代码。
5. 判断项目已有的编码规范和技术约定。

不要为了“了解项目”而无目的读取大量无关文件。

### 3. 明确任务边界

开始修改前，明确：

* 当前任务要解决什么问题。
* 哪些行为需要改变。
* 哪些行为必须保持不变。
* 哪些文件可能需要修改。

需求已经明确时，不重复询问用户已经提供的信息。

### 4. 制定最小实现方案

优先选择：

* 最小修改范围。
* 最少新增依赖。
* 最少新增抽象。
* 最少新增配置。
* 最容易验证的实现方式。

对于简单任务，不需要输出冗长计划。

### 5. 中间文件

* 计划文件、临时分析文件和其他开发中间文件统一保存在 `pifile/`。
* 不将临时文件散落在项目根目录或业务代码目录。
* 可随时删除的临时文件不要进入正式代码结构。

---

## 开发中

### 1. 保持代码简单

* 避免不必要的设计模式、抽象层和包装层。
* 优先复用已有模块和项目内已有能力。
* 不因为“未来可能需要”而提前实现未经需求验证的功能。

### 2. 类型完整

* 函数签名、类属性、数据类字段必须带完整类型标注。
* 新增公共接口必须明确输入和输出类型。
* 不通过滥用 `Any`、空类型或弱类型规避类型设计。

### 3. 函数复杂度

* 单个函数体原则上不超过 50 行。
* 超过 50 行时，应优先判断是否存在独立职责；确有必要再拆分。
* 不得为了凑行数进行机械拆函数。
* 控制流嵌套原则上不超过 3 层。
* 优先使用提前返回、提取函数等方式降低嵌套。

### 4. 文件复杂度

* 单文件原则上不超过 300 行。
* 超过 300 行时，优先判断是否存在清晰的模块边界。
* 拆分模块后应降低认知复杂度，而不是单纯降低文件行数。

### 5. 参数设计

* 函数位置参数原则上不超过 3 个。
* 参数较多且具有明确语义关系时，优先使用对象、数据类或配置结构传递。
* 不为了满足参数数量限制而创建没有实际意义的包装对象。

### 6. 魔法数字

* 使用命名常量代替具有业务含义的魔法数字。
* `0`、`1`、`-1` 等具有公认语义的值可以直接使用。
* 常量命名应能表达实际业务含义，而不是简单替换成 `VALUE_1`。

### 7. 修改现有代码

* 优先延续项目已有风格。
* 不因个人偏好改变无关代码风格。
* 修改已有逻辑时尽量保持原有接口和行为，除非任务明确要求改变。
* 避免为了“顺便优化”而扩大变更范围。

### 8. 测试优先

* 核心业务逻辑、数据处理、接口和容易回归的功能优先采用 TDD 或测试先行方式。
* UI、样式、简单脚本、配置文件等根据实际情况决定测试方式。
* 新增功能应至少覆盖关键路径。
* 修复 Bug 时，应优先补充能够复现该 Bug 的测试，防止回归。
* 不为了形式上的“测试覆盖率”编写低价值测试。

### 9. 依赖与配置

* 优先使用项目现有依赖解决问题。
* 新增依赖前确认没有合适的现有依赖。
* 不擅自升级无关依赖。
* 不修改与当前任务无关的环境变量、构建配置或部署配置。

### 10. Git

* 不擅自创建提交。
* 不修改、覆盖或删除用户已有提交和工作区修改。
* 不使用危险 Git 操作清理工作区，除非用户明确要求。
* 提交信息如由 Agent 创建，必须使用中文并准确描述实际变更。

---

## 开发后

### 1. 验证

完成修改后，必须执行与当前改动直接相关的最小验证集。

根据项目情况选择：

* 单元测试。
* 集成测试。
* 类型检查。
* Lint。
* 构建。
* 启动检查。
* API 请求验证。
* 关键功能手动验证。

验证失败时不得宣称任务已经完成。

### 2. 检查修改范围

完成后检查：

* 是否修改了不必要的文件。
* 是否产生临时文件。
* 是否引入无必要依赖。
* 是否出现与任务无关的代码变化。
* 是否误修改用户已有内容。

### 3. 文档同步

* 项目功能、结构或使用方式发生变化时，同步更新 `README.md`。
* 项目架构发生变化时，同步更新项目架构图。
* README 和架构图只保留当前有效信息，不保留过时内容。
* 文档更新以维护准确性为目的，不为了“有修改”而修改文档。

### 4. 清理 Agent 启动的进程

* 对话结束前，把本次为验证/调试启动的进程全部 kill（dev server、静态服务、无头浏览器等）。
* 用户自己启动的进程（如已在跑的后端 / 前端）不要动。
* 用 `pgrep -af "nex[t] dev"` 这类不自匹配的写法定位进程，避免 `pkill -f` 把自己所在的 shell 一起杀掉。

### 5. 最终输出

最终输出应直接可交付，重点说明：

* 完成了什么。
* 验证是否通过。
* 必要的使用方式或注意事项。

不要输出冗长的开发过程、内部思考过程或无关修改说明。

---

# 禁止事项

* 禁止使用 PowerShell / CMD。
* Python 项目禁止使用 `pip` 管理依赖。
* 禁止覆盖或撤销用户未提交的修改。
* 禁止擅自扩大任务范围。
* 禁止无需求升级依赖或技术栈。
* 禁止为了满足形式规则进行无意义重构。
* 禁止在验证失败时声称任务完成。
* 禁止把 Agent 启动的进程留在后台（用户明确要求：每次对话结束全部 kill）。
* 禁止将临时文件散落到正式项目目录。
* 禁止把一次性开发错误无差别堆积进 README。
* 禁止为了体现“高级架构”而增加不必要复杂度。
