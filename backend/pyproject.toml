[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "hsauihh", email = "woshidairuiqi@outlook.com" }
]
requires-python = ">=3.13"
dependencies = [
    "bcrypt>=4",
    "fastapi[standard]>=0.141.1",
    "fastembed>=0.8.0",
    "openai>=1.60",
    "pypinyin>=0.55.0",
    "python-dotenv>=1.0",
    "requests>=2.34.2",
    "snownlp>=0.12.3",
    "tzdata>=2026.3",
]

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
